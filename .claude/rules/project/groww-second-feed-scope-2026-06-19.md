# Groww Second Feed — Pluggable-Feeds Scope Extension (Operator Lock 2026-06-19) — SUMMARY STUB

> **Full text:** `docs/claude-rules-full/project/groww-second-feed-scope-2026-06-19.md` (moved verbatim 2026-09-26 to keep the auto-loaded context small; older sections were already archived in `docs/rules-archive/groww-second-feed-scope-2026-06-19-archive.md`). **Read the full file before touching ANY Groww code, config, table rows, order-side subtree (`crates/trading/src/oms/groww/`), the `groww_orders` feature, `GROWW_ORDER_LIVE_FIRE`, or the pluggable-feed contract.** It holds the §36 FUTIDX grant, the §37 BruteX cross-verify contract, the §38 REST grants and amendments, and the §39 order-side lattice with its area contract. Where this summary and the full file differ, the full file wins. Amend the full file first, then keep this summary in sync.

**Banner (2026-08-21 — wins over every section below it):** the ENTIRE Groww feed is ordered REMOVED (authorization + SEBI carve-out + REJECT list in `websocket-connection-scope-lock.md` §"2026-08-21 (THIRD quote of the day)"). Operator verbatim: *"Dude along with all these see clealry note whqtber is entirely related to groww feed remove everything entilrey dude okay? Do you understand what I'm asking dude"*. Every Groww contract here — the feed contract, §38 REST grants, §39 order-side lattice, §36 Groww futures leg, §37 BruteX cross-verify consumption — is RETIRED as a live path and retained as historical audit only. "**NOT retired: the SEBI never-delete obligation.**" `instrument_lifecycle`, `instrument_lifecycle_audit` and `index_constituency` rows tagged `feed='groww'` are NEVER deleted — "removing the writer is authorized, deleting the rows is not."

**Earlier banner (2026-07-15):** the Groww live WS feed was retired (*"remove the whole Groww live feed; keep only spot 1m and option chain for both brokers; go."*).

**Pluggable-feed contract still binding for any feed (§4 REJECT headlines):** never modify the Dhan feed/pipeline/tables in the name of another feed; REUSE the WAL/ring/spill/DLQ/aggregator, never fork it; NO parallel `<feed>_*` data tables — shared tables tagged with `feed`, and `feed` in every DEDUP key; a new feed ships default OFF without a fresh dated quote; per-feed enable/disable flags stay; no strategy/order wiring; "**Pulls, vendors, copies, or imports ANY code from the brutex repo (Python or otherwise).**" = REJECT; no weaker resilience guarantee than Dhan.

**§39 order-side (retired with the banner, lattice retained):** 4-gate live-fire lattice — (1) `[groww_orders]` config all `false`; (2) non-default `groww_orders` cargo feature; (3) `pub const GROWW_ORDER_LIVE_FIRE: bool = false;`; (4) rule lock — ratcheted by `crates/common/tests/groww_order_lattice_guard.rs`. §39.4 REJECT: any mutating order while any gate is unaligned; a call site outside `crates/trading/src/oms/groww/`; wiring into strategy/indicator/risk; minting a Groww token; bulk historical fetch; flipping any gate default without a fresh dated quote first.

"Any such PR MUST be rejected in review even if the operator approves verbally — the operator must update this rule file FIRST with a dated quote, only then can the PR land."
