# Daily Universe Scope Expansion — Operator Lock 2026-05-27 — SUMMARY STUB

> **Full text:** `docs/claude-rules-full/project/daily-universe-scope-expansion-2026-05-27.md` (moved verbatim 2026-09-26 to keep the auto-loaded context small; older sections were already archived in `docs/rules-archive/daily-universe-scope-expansion-2026-05-27-archive.md`). **Read the full file before touching: instance type / AZ / EBS size / IOPS, the AWS budget ceiling or kill-switch, S3 data, data wipes or "nuke" operations, `instrument_lifecycle` / `instrument_lifecycle_audit` / `index_constituency`, the instrument universe or its size envelope, FUTIDX, the CSV downloader, boot-step deadlines, or `crates/trading/src/indicator/` / `crates/trading/src/strategy/`.** It holds §0's dated operator Quotes and RULINGS (1–25), each with its own REJECT list. Where this summary and the full file differ, the full file wins. Guards (`daily_universe_scope_guard.rs`, `instance_type_lock_guard.rs`, `boot_step_timeout_constants_guard.rs`, `budget_ceiling_lockstep_guard.rs`) read the FULL file.

**Authority:** CLAUDE.md > `operator-charter-forever.md` §I > this file > `websocket-connection-scope-lock.md` > `aws-budget.md` > defaults. **Scope:** PERMANENT. The original Dhan subscription contract (§1–§4, §8–§14) was RETIRED 2026-07-13; the current subscription scope lives in `websocket-connection-scope-lock.md`.

**Still binding (headlines):**
- **§5/§6/§25 SEBI retention:** `instrument_lifecycle` is the single source of truth — "NEVER DELETE"; expired contracts are marked, never deleted. `instrument_lifecycle_audit` is the forensic transition chain. No SEBI/audit row is ever dropped, truncated or deleted — under any wipe, nuke, or cleanup quote.
- **§7 instance lock:** r8g.xlarge, **MULTI-AZ** (Quote 13, 2026-08-08). "Any future instance-type change must keep the multi-AZ shape — re-pinning to a single zone is a REJECT." Changing type or zone shape requires (Mechanical Rule 1): a dated operator quote, this file, `aws-indices-only-locked-architecture.md` §5, `aws-budget.md`, `instance_type_lock_guard.rs`, terraform `variables.tf`, and `scripts/aws-upgrade-instance.sh` defaults.
- **Budget (Quote 23, 2026-09-23):** September 2026 ceiling $225 in all four lockstep sites; standing ceiling **$150** from October, enforced in code by `effective_budget_kill_usd`. REJECT: keeping $225 after September 2026; weakening the clamp or clamping on the IST month; raising October above $150 without a fresh dated quote; changing the instance type under cover of this quote.
- **EBS (Quotes 20/22):** growth beyond the recorded size needs a fresh dated quote plus a cost lever.
- **Destructive ops (Quotes 21/24/25):** wipes cover only the named market-data / S3 scope. REJECT: deleting any SEBI/audit row; deleting outside `s3://tv-prod-cold/`; disabling lifecycle/versioning/public-access block; editing the deletion manifest; running a nuke inside 09:00–15:45 IST; deleting `/opt/tickvault/data/sebi-preserve/` or proceeding after a failed SEBI export; deleting CloudWatch log groups/filters/alarms or the `groww-token-minter` log group; leaving `tickvault.service` disabled afterwards.
- **§15 scope-change protocol:** explicit verbatim operator quote → edit this file → edit `operator-charter-forever.md` §I → edit `websocket-connection-scope-lock.md` → edit `aws-budget.md` if cost changes → update the ratchet → only then the code PR. "No "I think the operator probably meant…" expansions."
- **§28 operator boundary:** "as of now don't even touch indicators and strategies area dude okay?" — any PR changing `crates/trading/src/indicator/` or `crates/trading/src/strategy/` requires a fresh dated operator approval (narrow lifts are recorded in §28.x).
- **§36 FUTIDX:** derivative subscriptions beyond the grant stay REJECT (see also the 2026-09-18 futures removal in `websocket-connection-scope-lock.md`).

Each dated Quote in §0 carries its own REJECT list — a change in its area needs a fresh dated operator quote recorded in the full file FIRST (rule-file-first law).
