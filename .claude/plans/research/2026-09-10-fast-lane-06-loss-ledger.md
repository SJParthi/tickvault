# Fast-lane 06 — LOSS AND DELAY LEDGER (Dhan socket → queryable QuestDB row / RAM state)

Hostile read-only review, 2026-09-10. No cargo run. Labels: **V** = Verified (file:line), **A** = Assumed, **U** = Unknown.
Alarm column = grep of `deploy/aws/terraform/*.tf` metric_name / errcode map keys (`error-code-alarms.tf:188-852`). EMF = in `deploy/aws/cloudwatch-agent.json` (no EMF ⇒ local `/metrics` only).

| # | Point (mechanism) | Bound | Counted | Alarmed | Recoverable / time | Worst delay → queryable |
|---|---|---|---|---|---|---|
| 1 | Vendor skip-forward: Dhan skips a slow consumer to "latest state", no seq no (`tick_persistence.rs:2614`, **A** — vendor quote not located in `docs/dhan-ref` tonight) | none | NO | NO | NO — permanent, invisible | n/a |
| 2 | Reconnect / redial gap (park, ghost redial, 300 s frame-silence redial `pool_supervisor.rs:168`, `FRAME_SILENCE_REDIAL`) — no snapshot-on-subscribe | backoff ladder | `tv_dhan_ws_reconnect_total` (EMF) | park YES (`live-lane-alarms.tf:165`); reconnect count **NO** | NO — frames during gap never arrive | permanent |
| 3 | Socket frame cap: frame > `max_frame_bytes` refused (`connection.rs:356,364`; read buf 16 KiB `:197`) | `MAIN_FEED_MAX_FRAME_BYTES`, depth-200 512 KiB `:175` | `tv_dhan_ws_frame_refused_total` (EMF) | **NO** | NO | permanent |
| 4 | WAL queue refused (`ws_frame_spill.rs:1145-1160`) → since 2026-09-05 frame still goes to ring = `CapturedLiveOnly` (`pool_supervisor.rs:2509,3070-3080`) — folded live, NOT replayable | 524,288 records `:358`; bytes min(RAM/16, 2 GiB), floor 256 MiB `:361-372` | `tv_dhan_ws_wal_dropped_total`, `tv_ticks_lost_total{spill_drop_critical}` | YES (`live-lane-alarms.tf:466,530`; `ws-spill-02`) | only if flush lands; a crash before flush = permanent | 0 (live) |
| 5 | WAL refused **and** ring refused = `WalDropped` (`pool_supervisor.rs:3100-3113`) | as 4 + ring | same counters | YES | NO — permanent | permanent |
| 6 | WAL write failure on disk (ENOSPC; 2026-09-04: 2,000,238 frames) — writer drains + discards, `accept` still says Spilled | disk | `tv_ws_frame_spill_write_errors_total` | YES (`loss-and-retention-alarms.tf:316`, `ws-spill-01`) | NO for those frames | permanent |
| 7 | Ring full (count `FRAME_RING_CAPACITY` 65,536 `dhan_feed_stack.rs:3875`; bytes 2 % RAM, floor 256 MiB, cap 2 GiB `:4083-4094`, main 3/4 share `:4229`) → `note_unapplied` (`pool_supervisor.rs:3096,3110`) | 65,536 frames / ~640 MiB | `tv_dhan_ws_ring_full_total`, `_bytes_full_total` | YES (`live-lane-alarms.tf:285,311`) | YES — catch-up drain (300 s budget, 120 rounds, RSS 60 % stop `dhan_feed_stack.rs:3970-4031`) else next boot | in-session ≤ minutes; else next boot (~1 trading day) |
| 8 | Ring dwell (frame waits for the fold) | ring overflows at ~5.2 s @12.5k fps | `tv_dhan_feed_ring_dwell_max_ms` gauge | YES ≥ 2000 ms (`live-lane-alarms.tf:1341`) | delay only | ≤ ~5 s then #7 |
| 9 | Frame walk give-up (unknown code / partial packet) | `MAX_PACKETS_PER_FRAME` 70,000 `constants.rs:4942` | `tv_dhan_feed_abandoned_bytes_total`, `drain_frames_total` (EMF) | **NO** | NO | permanent (rest of frame) |
| 10 | Aggregator HARD refusal (NaN/∞ price, ts outside 30-yr band) — row NOT written (`dhan_feed_stack.rs:2652`) | — | `tv_aggregator_tick_refused_total{price,timestamp}`, `tv_dhan_feed_ingest_refused_total` | ratio alarm ≥25 % (`live-lane-alarms.tf:1284`, market-hours gated) | NO (by design — corrupt) | permanent |
| 11 | Aggregator CANDLE-ONLY refusals (`out_of_session`, `untraded_sentinel`, `stale_trading_day`, `out_of_band_timestamp`, `future_trading_day`) — **row still written** (`:2593-2680`) | — | same counter, reasons `fold_counters.rs:165-190` | same ratio alarm | candle absent; tick row intact | 0 |
| 12 | Aggregator slot cap: instrument > 25,000 gets NO candles for process life (`multi_tf_aggregator.rs:73,523`); rows kept | `AGGREGATOR_MAX_SLOTS` 25,000 vs ~24,600 universe | `tv_aggregator_slot_exhausted_total` | YES (`live-lane-alarms.tf:1031`) | candle: NO; tick: yes | 0 |
| 13 | Late tick > 1 bucket late discarded from candle (`aggregator_cell.rs:73-99`, default Refold covers 1 bucket) | — | `tv_candle_tick_discarded_late_total` (EMF) | **NO** | candle only | 0 |
| 14 | Persistence ts-window gate: LTT outside [09:00, 15:40) IST → `Ok(())`, **not** marked unapplied (`tick_persistence.rs:2136-2137`; `session_window.rs:133-149`); receipt-late only counted | — | `tv_ticks_out_of_window_refused_total` (local only) | **NO** | NO — permanent by design (incl. any post-15:40 print) | permanent |
| 15 | Tick offload queue full → retain ≤ `MAX_RETAINED_FLUSH_SPANS` 2 / 32 MiB, then `discard_pending` → rescue thread → spill (`tick_persistence.rs:2519-2580`, `:2879-2945`) | `FLUSH_QUEUE_DEPTH` 4 | `tv_tick_flush_queue_full_total`, `_width_capped_total` (local); spilled/dropped | YES via `tv_ticks_dropped/spilled` (`live-lane-alarms.tf:231,243,772`; `hot-path-02`) | YES — spill replay every 300 s, 60 s req timeout (`tick_spill_replay.rs:1034-1044`) | ≤ ~6 min |
| 16 | ILP flush failure on writer thread (5,000 ms `request_timeout` `:1482`) → spill; `dropped == spilled` is the proof (`:2743-2745`) | 5 s/req | `tv_ticks_dropped_total`, `tv_ticks_spilled_total` | YES | YES — as 15 | ≤ ~6 min |
| 17 | `discard_pending` inline fallback: rescue queue full/dead → synchronous `df` fork + ≤32 MiB write **on the drain** (`:2589-2660`) | — | `tv_tick_rescue_inline_fallback_total` | **NO** | delay → feeds #7/#8 | seconds |
| 18 | Spill cap / free headroom: dir ≥ cap or free < 2 GiB → rows REFUSED, `spilled` stays 0 (`:660-720`, 2026-08-25 1,695,983 rows) | max(512 MiB, vol/32) | `tv_ticks_dropped_total` alone; `over_soft_ceiling` (EMF) | YES (dropped ≠ spilled) | NO — permanent | permanent |
| 19 | Depth mirror of 15-18: `DEPTH_FLUSH_QUEUE_DEPTH` 4, `DEPTH_RESCUE_QUEUE_DEPTH` 2, 32 MiB, 2 spans, spill 512 MiB floor (`depth_persistence.rs:607,2054-2282`); `append_row` Err → `note_unapplied` `:1397` | as tick | `tv_depth_rows_dropped/spilled_total`, `_spill_write_errors_total` | YES (`loss-and-retention-alarms.tf:217,231`) | YES — same 300 s replay (depth dir `tick_spill_replay.rs:343`) | ≤ ~6 min |
| 20 | Offload-writer shutdown abandon (12 s grace `dhan_feed_stack.rs:4656`; up to 4 batches + in-flight lost); rescue thread abandon; WAL spill 10 s (`ws_frame_spill.rs:631`) | 12 s / 10 s | `tv_offload_writer_shutdown_incomplete_total`, `tv_{tick,depth}_rescue_abandoned_total`, `tv_wal_spill_shutdown_incomplete_total` | YES (`live-lane-alarms.tf:1147-1229`) | handed-off≠acked since 2026-09-05 → WAL replay next boot **if** the frame was WAL'd | next boot |
| 21 | QuestDB WAL suspension: table ACKs, never applies (2026-08-25: 15 tables) | — | `tv_questdb_wal_suspended_tables` (blind = −1), `probe_failed_total` | YES ≥1 (`app-alarms.tf:652`, `wal-suspend-01`, probe `:746`) | rows sit in QuestDB WAL until `RESUME WAL`; watermark parks acks as suspect (`dhan_feed_stack.rs:10672-10700`) | until operator resumes (hours) |
| 22 | QuestDB WAL apply lag (2026-09-08: 11,503→27,089 txn, 30 min blind for selector) | unbounded | `tv_questdb_wal_apply_lag_max` | YES ≥10,000 txn ×2 periods (`app-alarms.tf:698-711`) | delay only; RAM `SpotPriceStore` bypasses for selection | tens of minutes, unbounded |
| 23 | Disk full — shed levels never shed ticks (`ingest_shed.rs:164,821`); prune deletes ACTIVE WAL segments under byte pressure (`ws_frame_spill.rs:3971-3993`) | `SHED_*_BELOW_FREE` 15/8/4 % | `tv_spill_dir_free_bytes`, `tv_ws_wal_active_pruned_under_pressure_total` (local) | free ≤20 GiB YES (`app-alarms.tf:604`); prune via `ws-spill-01` code filter | pruned RingFull-only frames = permanent | permanent |
| 24 | Boot WAL replay caps: 512 MiB / 3 M frames / 40 GiB floor / RSS 60 % (`ws_frame_spill.rs:2707-2801`); segments stay in `replaying/` | per boot | `tv_wal_replay_deferred_segments_total` (EMF), skipped/floor/cap stops (local) | catch-up exhausted YES (`live-lane-alarms.tf:1104`); deferred **NO** | YES, next boot; 48 h age prune then deletes | 1–2 trading days |
| 25 | Mid-session restart: applied watermark IS built (`wal_applied_watermark.rs:1-53`, `dhan_feed_stack.rs:10672`) — contradicts Quote 21 "NOT built"; replays only unacked/unapplied. Losses: socket gap (#2) + `CapturedLiveOnly` frames unflushed at kill (#4) | — | replay counters | partial | mostly YES | boot + replay (minutes) |
| 26 | MemoryHigh=20G throttle / OOM kill (`tickvault.service:323,354`; `WAL_REPLAY_RSS_STOP_PCT` 60) | 20 GiB | `tv_process_rss_bytes`, `tv_oom_kills_total` | `resource-02`, `proc-01` code alarms YES | queued batches unacked → replayed next boot; ring unflushed live-only frames lost | restart + replay |
| 27 | Seal chain: 600,000-seal ring → escalation queue 4,096 (`seal_writer_runner.rs:194`) → inline fallback → spill → DLQ; lost/abandoned | 4,096 queue | `tv_dhan_feed_seals_rescued/dropped_total`, `tv_seal_escalation_lost/abandoned_total`, `tv_seal_writer_drain_dropped_total` | rescued YES, drain-dropped YES, `aggregator-drop-01`; `escalation_lost/abandoned` + `seals_dropped` **NO** | candle re-derivable from `ticks` only by hand | candle: minutes–never |
| 28 | Pre-open 09:00–09:15: candle grid opens 09:00; ticks < 09:00 LTT refused by #14; day-OHLC gate 09:15 (open from packet field) | — | `out_of_session` | ratio alarm | by design | 0 |
| 29 | 15:40 capture close: ts ≥ 15:40 refused (#14); frame-silence watchdog gated; post-close prints never stored | — | `tv_ticks_out_of_window_refused_total` (local) | **NO** | NO by design | permanent |
| 30 | Ranking side: `VolumeLeaderboard` cap 25,000 / non-monotonic refused (`volume_leaderboard.rs:153,416-419`); unmapped contract (not in owner map) never observed; `top_volume_rank` append fail | 25,000 | `tv_volume_leaderboard_refused_total`, `tv_top_volume_rank_append_failed_total` (both local only) | **NO** | RAM only; rank row absent | rank never |

## Summary

| Class | Points | Unalarmed today |
|---|---|---|
| **Permanent loss** | 1 vendor skip · 2 reconnect gap · 3 frame cap · 5 WalDropped · 6 WAL disk write fail · 9 frame walk give-up · 10 hard refusals · 14/29 ts-window refusal · 18 spill cap · 23 pressure prune · 27 seal lost/abandoned | **1, 2 (reconnect count), 3, 9, 14/29, 27 (`escalation_lost`, `escalation_abandoned`, `seals_dropped`), 30** |
| **Recoverable (bounded delay)** | 7 ring full → catch-up/next boot · 15/16/19 spill → 300 s replay · 20 shutdown → next-boot replay · 24 deferred segments · 25 restart · 26 OOM (unacked batches) | 24 deferred (`deferred_segments_total` EMF, no alarm), 17 inline fallback |
| **Delay-only** | 8 dwell · 21 WAL suspension · 22 apply lag · 26 MemoryHigh throttle · 13 late-tick candle | 13 (`discarded_late`, candle only) |
| **Not-a-loss by design (row kept)** | 11 candle-only refusals · 12 slot cap · 28 pre-open | — |
| **Watermark verdict** | Quote 21's "watermark NOT built" is STALE: `wal_applied_watermark.rs` exists and is wired (`pool_supervisor.rs:3096`, `tick_persistence.rs:2146,2371`, `dhan_feed_stack.rs:10672`). Residual restart loss = #2 + #4 (live-only frames unflushed at kill) | — |

**Unalarmed permanent-loss points (action list):** `tv_dhan_ws_frame_refused_total`, `tv_dhan_feed_abandoned_bytes_total`, `tv_ticks_out_of_window_refused_total` / `tv_depth_rows_out_of_window_refused_total` (not even EMF), `tv_seal_escalation_lost_total`, `tv_seal_escalation_abandoned_total`, `tv_dhan_feed_seals_dropped_total`, `tv_dhan_ws_reconnect_total`, `tv_volume_leaderboard_refused_total`, `tv_top_volume_rank_append_failed_total`, `tv_ws_wal_active_pruned_under_pressure_total` (pages only through the bare `ws-spill-01` filter). Vendor skip-forward (#1) is unalarmable — only the 15:41 cross-verify `compared` count can bound it.
