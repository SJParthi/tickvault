# Fast-lane 05 fix review — hostile pass on the uncommitted diff (2026-09-10)

Scope: `git diff` + `crates/app/tests/contract_map_selected_first_guard.rs`. Read-only. Both pub-fn hooks run: `pub-fn-test-guard.sh` exit 0, `pub-fn-wiring-guard.sh` exit 0.

| Sev | Fix | file:line | Finding | Concrete failing input |
|---|---|---|---|---|
| **CRITICAL** | C | `crates/app/src/dhan_feed_stack.rs:17308` vs `:23560` | `the_dead_class_rollup_is_wired_into_the_live_silence_sweep` lives in `mod tests` (13393–19863) and reads `IN_SESSION_1000_IST_MILLIS`, a PRIVATE `const` declared only inside sibling `mod connection_delivery_tests` (`:23556-23560`). `use super::*` (`:14068`) cannot reach a sibling module's private item. | `cargo test -p tickvault-app` → E0425. The app test binary does not compile; every fix in this diff ships untested. Hoist the three consts to module scope. |
| **HIGH** | A | `crates/core/src/websocket/types.rs:235`, `:870-877` | Wire-type doc still says `25 = unsubscribe`; `test_two_hundred_depth_unsubscribe_request_code_25` hardcodes 25 and asserts `"RequestCode":25`. It PASSES (never reads the constant), so a green test in the wire-type module certifies the ignored code. Violates the diff's own REJECT row (`websocket-connection-scope-lock.md:2947`). | grep `RequestCode":25` → a passing test. |
| **HIGH** | A | `.claude/rules/project/websocket-connection-scope-lock.md:2670-2672` | 2026-09-08 (THIRD) item 4 still reads *"…is the evidence that 25 works"* — un-annotated, while `:2644` and `:2764` were annotated. Same REJECT row, same file. | Any reader of that item. |
| MEDIUM | B | `crates/app/src/dhan_contract_universe.rs:1887-1904` | `selected_first == 0` with `selection.instruments` non-empty (segment-derivation drift, id-sign change) is only an `info!` field — no counter, no coded error. The 2026-09-10 defect would return under a green log line. Unit tests build both sides from the same `FNO` literal, so they do not prove the production derivations agree. | `instruments.len() > 0 && selected_first == 0` → silent. |
| MEDIUM | A | `crates/app/src/depth200_ranked_steer.rs:32`, `crates/app/tests/drain_loss_series_seeding_guard.rs:93` | Still "unverified live (24-vs-25 split)". 25 is now verified IGNORED; only 24 is unverified. Stale, reassuring direction. | — |
| MEDIUM | D | `deploy/aws/terraform/dhan-token-minter-lambda.tf:97-104` | The 82 s budget counts only the mint path; 3 SSM reads with SDK retries, the SSM write and cold start are unquantified. 120 s holds, the comment under-states. | cold start + SSM retries (~15 s) + 82 s ≈ 100 s. |
| LOW | D | `crates/aws-lambdas/src/dhan_token_minter.rs:609-616` | `reason = %message` logs Dhan's `message` untruncated (`parse_mint_response:209` skips `capture_body`). No secret — Dhan's envelope does not echo PIN/TOTP and `Display` already prints it — but it is an unbounded vendor string in CloudWatch. | a 1 MB `message`. |
| LOW | D | `dhan_token_minter.rs:502-504` | Substring match: `"Invalid PIN/TOTP"` earns one extra login. Bounded by `MAX_TOTP_ATTEMPTS = 2`. | `"Invalid PIN or TOTP"`. |
| LOW | A | `.claude/plans/active-plan-feed-hardening.md:219`, `docs/dhan-ref/verification-2026-04-13.md:82` | Active plan / dated record still say `unsubscribe(25)`. Prose, no gate. | — |

## Verified clean
- **A**: constants, `schema_validation.rs`, `subscription_builder.rs` tests agree on 24; docs 03/04/08, `annexure-enums.md`, `websocket-enforcement.md`, `dhan_api_v2_reference.md` annotated; no "There is NO code 24" outside `docs/rules-archive`.
- **B**: `partition` is stable; composite `(u64, ExchangeSegment)` match; negative ids fall to the unselected half; empty selection = old order; top-ups (`top_up_late_contracts_acked:8687`) draw from the same `selection.instruments`; guard anchors are unique (`:1861`, `:1888`, `:1890`).
- **C**: `ist_secs_of_day_from_millis(u64::MAX)` cannot overflow; IST midnight → 0; gate is 09:15 (`CONTINUOUS_SESSION_START_SECS_OF_DAY_IST:13356`), so the 09:00 false fire is also closed; latch cleared, not held.
- **D**: `Fn(u64) -> Fut`, `Fut: Send` satisfies `TokenMinter::mint`'s `+ Send` (`:379`); bind-by-move in the guard is legal; `secs_to_sleep_for_next_totp_step` always crosses a boundary (truncated seconds only lengthen the wait); PIN rejection never retries; the sequenced loopback server refuses a third connection, so "no loop" is proven.
