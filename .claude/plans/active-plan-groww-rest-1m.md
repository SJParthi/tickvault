# Implementation Plan: Groww per-minute REST pipeline — spot 1m + option chain + per-contract 1m

**Status:** IN_PROGRESS
**Date:** 2026-07-13
**Approved by:** Parthiban (operator) — directive relayed verbatim via the coordinator session, 2026-07-13 (quotes preserved in this plan + the rule amendments: `groww-second-feed-scope-2026-06-19.md` §38, `no-rest-except-live-feed-2026-06-27.md` §9).

## The verbatim operator authorization (2026-07-13 — preserve exactly, typos included)

**Quote 1 (the directive):**
> "can we implement the same Groww one min fetch which is precisely very similar to the same Dhan — REST api pull ohlcv entirely and even then instantly option chain api also... for Groww live feed and now we planned to add this live REST which is very similar to Dhan. That's it."

**Quote 2 (latency visibility):**
> "always clearly note within a second — or within how many seconds precisely — we are fetching this live real OHLCV, along with the option chain API."

**Context 3 (verbatim intent, not a quote — labeled as such):** the purpose is backtest↔live parity of the operator's fill model. BruteX backtests on Groww 1-minute candles with a worst-case fill rule: signal on the current candle → fill at the NEXT minute's worst-case high/low, for BOTH the underlying AND the option leg. Live must recreate exactly that combo from the same data source; therefore per-contract 1m candles for selected option contracts are in scope (the chain endpoint serves strike discovery/liquidity), and the close-to-data latency measurement is load-bearing (the signal→next-minute-fill window depends on it).

## Crates touched

`tickvault-common` (`crates/common/` — constants, config sections, ErrorCode decisions), `tickvault-app` (`crates/app/` — the per-minute schedulers + scorecard surfacing), `tickvault-storage` (`crates/storage/` — persistence writers, the new per-contract table), plus a one-line visibility change in `tickvault-core` (`crates/core/` — expose `stable_index_security_id`, see PR-2).

## Plan Items

- [x] PR-1 (this PR) — rule amendments + this plan + plan archiving (docs-only)
  - Files: .claude/rules/project/groww-second-feed-scope-2026-06-19.md (§33 dated notes + new §38), .claude/rules/project/no-rest-except-live-feed-2026-06-27.md (banner + §3 KEEP rows + §6 triggers + new §9), .claude/plans/active-plan-groww-rest-1m.md (NEW), .claude/plans/archive/2026-07-08-futidx-4.md + .claude/plans/archive/2026-07-10-futidx-allmonths.md (archived from active per plan-enforcement rule 7)
  - Tests: N/A — docs-only (plan-gate exempt class; no `crates/**/src` change)

- [x] PR-2 — Groww spot-1m leg (per-minute scheduler mirroring `crates/app/src/spot_1m_rest_boot.rs`, with the #1499 lessons (#1499, pending merge) baked in from day one: day-granular request window + client-side minute filter — for Groww a `start_time`/`end_time` DAY window in `yyyy-MM-dd HH:mm:ss` (IST Assumed — Indian-exchange convention, confirm live), filtering for the target minute client-side; flush-confirmed persist watermark; one-minute-lookback backfill in every ladder outcome arm; one bounded 15:31 post-session sweep). SEQUENCING (dated note 2026-07-13, hostile round 1 item 3): #1499 was still OPEN (blocked) at PR-2 authoring — the two diffs overlap in `rest-1m-pipeline-error-codes.md` + `config/base.toml` context lines; whichever merges second rebases — coordinated at push time. Endpoint `GET https://api.groww.in/v1/historical/candles`, `candle_interval="1minute"`, `groww_symbol` identity (`NSE-NIFTY` / `NSE-BANKNIFTY` / `BSE-SENSEX`, segment `CASH`), `Authorization: Bearer <shared-minter token via the existing fetch_groww_access_token SSM read-only path>` + `x-api-version: 1.0`. Rows land in the EXISTING `spot_1m_rest` table tagged `feed='groww'` (the §2 same-tables + feed-in-DEDUP-key model; the key already carries `feed`), using the SAME `security_id` values the Groww live lane uses for these indices (`stable_index_security_id` — currently PRIVATE in crates/core/src/feed/groww/instruments.rs; PR-2 exposes it via pub(crate)/a public accessor to reuse the exact live-lane ids). Per-fetch close-to-data latency: per-row `close_to_data_ms` column + histogram `tv_groww_spot1m_close_to_data_ms`. Config: `[groww_spot_1m]` gated, serde default OFF, base.toml opts in.
  - SHIPPED 2026-07-13 (this PR) with the #1499 patterns from day one (day-granular window + client-side minute filter incl. STALE-body detection, flush-confirmed `PersistTracker` watermark, one-minute-lookback backfill in every ladder arm, bounded ~15:31 post-session sweep) PLUS the 2026-07-13 operator scope additions: (a) the NEW `rest_fetch_audit` per-fetch forensics table — one row per (target minute, symbol, feed, leg), success AND failure (attempts / final_http_status / fetch_latency_ms / close_to_data_ms / rate_limited_count / outcome / error_class), best-effort writes that NEVER affect the fetch loop; the DHAN leg's emit sites are a FAST FOLLOW-UP after #1499 merges (no Dhan module touched here); a sweep-unrecovered minute = a NAMED GAP row (`error_class="named_gap"`), never a silent hole; (b) the exhaustive failure-mode matrix (Failure Modes below); (c) the schema-grounding statement (Design below). Scorecard aggregates over this table (attempts p99, rate-limit hits/day, per-leg close-to-data lines) land in PR-5. Deviation from the Dhan leg, flagged: `GROWW_SPOT_1M_SYMBOL_BUDGET_SECS = 18` (not 20) because the 3 symbols fetch SEQUENTIALLY (pacing rule) and 3 × budget + fire delay must still finish inside the minute (const-asserted).
  - HOSTILE ROUND 1 APPLIED (2026-07-13, 12 findings): (1) CRITICAL — the spawn now runs on BOTH boot arms via the `spawn_groww_spot_1m_leg` helper (the scoreboard dual-site pattern; the fast crash-recovery arm previously never ran the leg or its sweep) — dual sites pinned by the extended wiring guard; (2) HIGH — mid-session-boot pre-boot window [session_first, first_covered) is named AUDIT-ONLY at sweep time (`pre_boot_gap_minutes`, outcome `named_gap` class `pre_boot`, NO fetch — §38 forbids bulk backfill; same mechanism covers respawn); (4) sweep flush-fail names the staged-but-lost minutes (`flush_failed`); (5) `outcome` added to the rest_fetch_audit DEDUP key (transition rows BOTH survive — phase-0 rule 3); (6) distinct `named_gap` outcome slug, actual-or-0 final_http_status; (7) boundary-skip forensics skip on IST-midnight-crossing wakes (iteration-start date capture); (8) honest overrun doc (SSM/flush legs outside the const-asserted ladder budget); (9) implausible-OHLC counter + one coded line per fired minute (persisted verbatim); (10) explicit zero-width-window comment on the `succ_opt` fallback; (11) duplicate target-minute candles counted + debug-logged; (12) auth-class reject short-circuits the ladder AND the remaining symbols per fire (token cache dropped immediately; mock-401 + pure-row unit tests).
  - Files: crates/app/src/groww_spot_1m_boot.rs (NEW), crates/app/src/main.rs (process-global spawn — NOT the Dhan-gated seam), crates/app/src/lib.rs, crates/app/tests/groww_spot_1m_wiring_guard.rs (NEW), crates/common/src/constants.rs (GROWW_SPOT_1M_* block), crates/common/src/config.rs (GrowwSpot1mConfig), crates/core/src/feed/groww/instruments.rs (stable_index_security_id → pub), crates/core/src/notification/events.rs (GrowwSpot1mFetchDegraded/Recovered), crates/storage/src/spot_1m_rest_persistence.rs (feed-parameterized writer), crates/storage/src/rest_fetch_audit_persistence.rs (NEW), crates/storage/src/lib.rs, config/base.toml, .claude/rules/project/rest-1m-pipeline-error-codes.md (Groww-emitter + rest_fetch_audit dated notes), crates/common/src/error_code.rs (archived-plan comment path sync, deferred from PR-1)
  - Tests: test_groww_spot_1m_constants_pinned (constants), test_groww_spot_1m_config_defaults_off_and_round_trips (config), test_stable_index_security_id_public_contract_for_rest_leg (core id), test_groww_spot_1m_fetch_degraded_is_high_with_action_lines + test_groww_spot_1m_fetch_recovered_is_info_positive_ping (events), test_append_row_groww_writer_stamps_groww_feed_and_source (storage writer), rest_fetch_audit_persistence tests (DDL/DEDUP-key/outcome wire strings/failure sentinels/discard-pending/ILP-HTTP conf/mock-HTTP ensure arms), groww_spot_1m_boot in-module tests (day window + month boundary, proven-day-window query regression lock, dual ts parse epoch/millis/IST-string fixtures + garbage rejection, envelope/malformed-row parse, STALE-body target-absent-but-backfills, backfill_minute_nanos, sweep_missing_minutes, PersistTracker max-merge, retry deltas, token 60s pacing, error-class slugs, audit outcome mapping + row builder sentinels, distinct live-lane ids), ratchet_groww_spot1m_* wiring guards (spawn gated + process-global, stub-guard, panic-free production region)

- [x] PR-3 — Groww option-chain leg: `GET /v1/option-chain/exchange/{exchange}/underlying/{underlying}?expiry_date=...`, 3 underlyings (NIFTY/BANKNIFTY NSE, SENSEX BSE; the `underlying` path param is the PLAIN symbol — NOT groww_symbol), CURRENT expiry resolved from the ALREADY-INGESTED daily Groww instruments CSV (no new expiry endpoint needed — zero rate cost), sequenced after the spot leg (watch-signal + fallback timer, the Dhan pattern), rows into the EXISTING `option_chain_1m` table `feed='groww'`, own min-gap rate limiter, latency histogram `tv_groww_chain1m_close_to_data_ms` + per-row latency column. Config `[groww_option_chain_1m]`, default OFF pending the first live probe (the endpoint is documented-available, unlike Dhan's entitlement question, but first-live-session verification still gates the default).
  - SHIPPED 2026-07-13 (this PR). Key implementation points: (a) expiry discovery = `select_current_option_expiry` (crates/core/src/feed/groww/instruments.rs — segment=FNO, CE/PE, canonical-underlying match, nearest ≥ today, never-roll; T-0 holds through the session, T+1 rolls — unit-pinned) over `download_groww_master_rows` (reuses the hardened CSV client) with bounded 3s/6s warmup retry; a master with no usable FNO rows degrades THAT underlying only (CHAIN-02 `stage="expiry_unresolved"` + `tv_groww_chain1m_expiry_unresolved_total` + typed HIGH `GrowwChain1mExpiryUnresolved`), never guessed, never a day-kill; (b) sequencing = the spot leg publishes `minute_done_tx` (watch::Sender) UNCONDITIONALLY after every fire (success or failure) and the chain waits via the shared `wait_for_signal_or_fallback` (pub(crate) from option_chain_1m_boot.rs) with `GROWW_CHAIN_1M_FALLBACK_DELAY_MS = 2500`; one request per underlying per minute, sequential, defensive `GROWW_CHAIN_1M_MIN_GAP_MS = 1000` (Groww documents no chain-specific rule); (c) response has NO timestamp — `close_to_data_ms` (measured at parse-complete) is the ONLY freshness signal, stamped per row + histogram (module-doc noted); (d) persistence = EXISTING `option_chain_1m` table, feed-parameterized writer (`new_with_feed`, `append_row_ext`), TWO new nullable columns via ALTER-ADD self-heal: `rho` (Groww greeks carry it; Dhan doesn't) + `close_to_data_ms`; Dhan emit path untouched (leaves both NULL); parse-only strike invariant preserved (strikes come from response string keys, never computed); (e) forensics = one `rest_fetch_audit` row per (minute, underlying) with `leg='chain_1m'` (`REST_FETCH_LEG_CHAIN_1M`), attempts=1 (no re-poll ladder — live-snapshot semantics), success AND failure; (f) failure edge = chain-specific persist-gated 3-consecutive `FailureEdge` (`GROWW_CHAIN_1M_CONSECUTIVE_FAIL_PAGE_THRESHOLD = 3`) with `GrowwChain1mFetchDegraded`/`Recovered` typed events; (g) error codes REUSED: CHAIN-02/03 with `feed = "groww"` on every emit; NO CHAIN-01/04 analog (no entitlement question, no expirylist endpoint) — probe failures report CHAIN-02 `stage="probe"`; rule-file dated notes added in the SAME PR (crossref + tag guards green); (h) hostile parsing: envelope-tolerant (payload-wrapped or bare), `MAX_STRIKES_PER_CHAIN` cap (truncated + counted, `stage="strikes_truncated"`), invalid-strike counting, 8 MiB body cap, 15s per-underlying budget, auth-class short-circuit (token cache dropped, remaining underlyings skipped for the fire); (i) probe-only path (`enabled=false` + `probe_and_report=true` default): one bounded chain GET at boot, verdict via Info Telegram `GrowwChain1mProbeVerdict`, persists nothing.
  - Files: crates/app/src/groww_option_chain_1m_boot.rs (NEW), crates/app/src/groww_spot_1m_boot.rs (minute_done_tx publish + chain-leg token-cache routing), crates/app/src/option_chain_1m_boot.rs (wait_for_signal_or_fallback → pub(crate)), crates/app/src/main.rs (spawn_groww_spot_1m_leg helper extended — watch channel + chain spawn/probe on BOTH boot arms), crates/app/src/lib.rs, crates/app/tests/groww_chain_1m_wiring_guard.rs (NEW), crates/common/src/constants.rs (GROWW_CHAIN_1M_* block + GROWW_OPTION_CHAIN_URL_PREFIX + GROWW_CHAIN_1M_UNDERLYINGS), crates/common/src/config.rs (GrowwOptionChain1mConfig), crates/core/src/feed/groww/instruments.rs (select_current_option_expiry + download_groww_master_rows), crates/core/src/notification/events.rs (GrowwChain1mFetchDegraded/Recovered/ExpiryUnresolved/ProbeVerdict), crates/storage/src/option_chain_1m_persistence.rs (feed-parameterized writer + rho + close_to_data_ms columns), crates/storage/src/rest_fetch_audit_persistence.rs (REST_FETCH_LEG_CHAIN_1M), config/base.toml, .claude/rules/project/rest-1m-pipeline-error-codes.md (Groww chain-emitter dated notes)
  - Tests: test_groww_chain_1m_constants_pinned (constants), test_groww_option_chain_1m_config_defaults_off_probe_on_and_round_trips (config), test_select_current_option_expiry_nearest_at_or_after_today + test_select_current_option_expiry_expiry_day_t0_holds_t1_rolls (core expiry selection), 4 event tests (Degraded HIGH w/ action lines, Recovered Info, ExpiryUnresolved HIGH, ProbeVerdict Info), test_chain1m_per_feed_counter_names + test_chain1m_append_row_ext_groww_stamps_feed_rho_and_latency (storage writer), test_rest_fetch_leg_wire_strings_stable_and_distinct (audit leg), groww_option_chain_1m_boot in-module tests (URL builder, hostile/bare/wrapped/strike-cap/invalid-strike parse, min-gap wait math, error-class slugs, audit-row mapping, target resolution incl. degrade detail, FailureEdge, probe detail formatting, sequencing tokio test), ratchet_groww_chain1m_* wiring guards (spawn + probe config-gated inside the dual-arm helper, sequencing channel + unconditional spot publish, stub-guard, panic-free production region, parse-only strike invariant)

- [x] PR-2b (2026-07-13 addendum — operator scope, relayed via the coordinator session) — INDIA VIX joins the Groww SPOT leg (3 → 4 targets; SPOT ONLY — no chain, no contracts). Authorization recorded FIRST in `groww-second-feed-scope-2026-06-19.md` §38.7 (per §38.4) + mirrors in `no-rest-except-live-feed-2026-06-27.md` §9.2 (spot row 3→4) and `rest-1m-pipeline-error-codes.md` (SPOT1M feed=groww scope note). VIX identity RUNTIME-resolved from the day's watch file via the house canonicalize path (new additive `INDIAVIX → INDIA VIX` alias grounded in the live 2026-06-28 master capture) — never a guessed literal; fail-SOFT skip when unresolved (coded warn, edge-latched, + counter). Per-SID independence: per-target hard budget (re-derived 18s → 14s so 4 sequential budgets + fire delay fit the minute, const-asserted); the 3-minute escalation edge keys on the ORIGINAL 3 CORE indices (`MinuteEdgeTally`) — VIX-only failure never pages; core-all-failed pages even when VIX succeeded. VIX-not-served visibility: normal per-minute forensics rows + `tv_groww_spot1m_vix_empty_total`, and ONE per-day coalesced coded warn at the sweep (`stage="vix_not_served"`) + `tv_groww_spot1m_vix_not_served_total` when zero VIX minutes persisted while cores did. Sweep/backfill/forensics/latency machinery inherited unchanged.
  - Files: crates/app/src/groww_spot_1m_boot.rs (GrowwSpotTarget + runtime resolution + core-keyed edge + VIX latch), crates/common/src/constants.rs (GROWW_SPOT_1M_VIX_SYMBOL + budget 18→14 + arity/budget const-assert comments + INDIAVIX alias), crates/core/src/feed/groww/native/watch_reader.rs (optional index_name/symbol_name provenance fields), crates/app/tests/groww_spot_1m_wiring_guard.rs (VIX needles), .claude/rules/project/groww-second-feed-scope-2026-06-19.md (§38.7 + §38.1 qualifier), .claude/rules/project/no-rest-except-live-feed-2026-06-27.md (§9.2 spot row), .claude/rules/project/rest-1m-pipeline-error-codes.md (scope note), .claude/plans/active-plan-groww-rest-1m.md (this row)
  - Tests: test_core_spot_targets_pin_the_three_core_indices, test_vix_target_resolves_via_token_alias_and_display_name, test_vix_target_from_watch_doc_fail_closed_arms, test_minute_edge_tally_vix_failure_never_pages_core_failure_does, test_vix_not_served_verdict_arms (app); test_groww_spot_1m_constants_pinned + test_index_symbol_aliases_cover_groww_dhan_spelling_bridges updated (common); watch_reader provenance-field parse assertions (core); extended wiring-guard stub needles (app tests)

- [x] PR-4 — per-contract 1m candle leg (the fill-model leg): same `/v1/historical/candles` endpoint with `segment=FNO` and `groww_symbol` like `NSE-NIFTY-04Jan24-19200-CE`, for a BOUNDED selected set of active contracts (selection fed by the chain snapshot / instruments master; envelope cap on contracts per minute), NEW table `option_contract_1m_rest` with `feed` in the DEDUP key + retention/partition-manager registration. Config-gated default OFF.
  - SHIPPED 2026-07-13 (this PR). Key implementation points: (a) SELECTION = contract BOOK (warmup, day-keyed respawn cache: `select_current_option_expiry` + the NEW core `select_option_contract_rows`, strike/leg PARSED from each master row's own `groww_symbol` — parse-only, never computed; non-numeric tokens / type-mismatched rows skipped + counted) × per-minute ATM ANCHOR handed IN-MEMORY from the chain leg (`GrowwChainAnchorStore` — the chain fire's `underlying_ltp`, sticky across minutes; no QuestDB read) → ATM ± `strikes_each_side` (config, default 2) × CE+PE × 3 underlyings, round-robin interleaved by ATM-distance rank, HARD-capped at `GROWW_CONTRACT_1M_MAX_PER_MINUTE = 30` (default window fills the cap EXACTLY — const-asserted); over-cap truncates deterministically nearest-ATM-first + counter + one coded warn; a missing anchor skips THAT underlying (counted, never a guessed ATM); (b) SEQUENCING = the chain leg now publishes its own `minute_done_tx` watch signal UNCONDITIONALLY after every fire (the spot→chain semantics mirrored) and the contract leg waits via the reused `wait_for_signal_or_fallback` with `GROWW_CONTRACT_1M_FALLBACK_DELAY_MS = 8000` (trails the chain's normal completion); (c) FETCH = ONE request per contract per minute (NO in-minute ladder — 30 × a ladder blows the minute), day-granular window + client-side minute filter via the SHARED spot parser (refactored `parse_groww_1m_candle_rows` now yields tuple[6] `oi` — the spot leg maps it away, the contract leg persists it), one-minute-lookback backfill mined from the SAME body, `GROWW_CONTRACT_1M_MIN_GAP_MS = 300` sequential pacing (worst-overlap burst spot+chain+contract ≤ 5 req/s < the 6 req/s ceiling — const-asserted; worst-case 48 req/min across all three Groww legs ≤ 20% of the 300/min budget — const-asserted), HARD `GROWW_CONTRACT_1M_FIRE_BUDGET_SECS = 45` deadline (unreached contracts SKIPPED loudly with forensics rows); (d) NO 15:31 sweep for contracts (selection is minute-scoped ATM — "which contracts belong to minute M" is only knowable at M; an unrecovered minute is a NAMED absence via its `rest_fetch_audit` row, honest-envelope documented); (e) persistence = NEW `option_contract_1m_rest` (DEDUP `(ts, security_id, exchange_segment, feed)`; security_id = the contract exchange_token, segment NSE_FNO/BSE_FNO in-key per I-P1-11 — tokens are only exchange-unique; float `strike` deliberately NOT in-key; `groww_symbol` STRING column; registered in the partition manager DAY sweep — the retention coverage guard forces it); (f) error codes REUSED: SPOT1M-01/02 with `feed="groww"` + `leg="contract_1m"` on every emit (contract stages: selection_unresolved / selection_truncated / book_unresolved / fire_budget / implausible_ohlc + the spot taxonomy) — rule-file dated notes in the SAME PR; typed events `GrowwContract1mFetchDegraded`/`Recovered` (persist-gated 3-minute edge) + `GrowwContract1mBookUnresolved` (one HIGH/day); (g) token via the leg-routed `GrowwTokenCache::new_contract()` (the chain_leg bool → 3-variant leg enum refactor; auth-class reject short-circuits the fire + drops the cache, never a mint); (h) gating: `[groww_contract_1m]` serde default OFF, base.toml `enabled = false` (DEPENDS on the chain leg's anchors — enabled-without-chain is refused with a loud warn at spawn, pinned by the wiring guard); spawned inside the SAME dual-arm `spawn_groww_spot_1m_leg` helper (both boot arms inherited). DECISION-FRESHNESS GATE recorded with this PR: `groww-second-feed-scope-2026-06-19.md` §38.8 (operator verbatim-intent — backfill is record-completeness only, never a trading-decision input; future consumers fail closed on staleness via `close_to_data_ms`) + mirrors in `no-rest-except-live-feed-2026-06-27.md` §9.3 and `rest-1m-pipeline-error-codes.md` §3.
  - Files: crates/app/src/groww_contract_1m_boot.rs (NEW), crates/storage/src/option_contract_1m_rest_persistence.rs (NEW), crates/app/tests/groww_contract_1m_wiring_guard.rs (NEW), crates/app/src/groww_spot_1m_boot.rs (shared parser + leg-routed token cache), crates/app/src/groww_option_chain_1m_boot.rs (minute_done_tx publish + GrowwChainAnchorStore), crates/app/src/main.rs (contract spawn in the dual-arm helper), crates/app/src/lib.rs, crates/core/src/feed/groww/instruments.rs (select_option_contract_rows NEW), crates/core/src/notification/events.rs (3 GrowwContract1m* variants), crates/storage/src/partition_manager.rs (retention registration), crates/storage/src/rest_fetch_audit_persistence.rs (REST_FETCH_LEG_CONTRACT_1M), crates/storage/src/lib.rs, crates/common/src/constants.rs (GROWW_CONTRACT_1M_* block + const asserts), crates/common/src/config.rs (GrowwContract1mConfig), config/base.toml, .claude/rules/project/rest-1m-pipeline-error-codes.md, .claude/rules/project/groww-second-feed-scope-2026-06-19.md (§38.8), .claude/rules/project/no-rest-except-live-feed-2026-06-27.md (§9.3 note)
  - Tests: test_option_contract_1m_rest_create_ddl_contains_expected_columns + test_option_contract_1m_rest_dedup_key_ts_first_segment_and_feed_in_key + writer append/flush/discard/ILP-HTTP/mock-ensure tests (storage), test_groww_contract_1m_constants_pinned (constants + burst/budget math), test_groww_contract_1m_config_defaults_off_and_round_trips (config), test_select_option_contract_rows_exact_expiry_ce_pe_only (core), 3 event tests (Degraded HIGH w/ action lines, Recovered Info, BookUnresolved HIGH + escape), groww_contract_1m_boot in-module tests (symbol parse hostile battery, book build dedup/skip/sort, atm_index ties, atm_window rank order + edge clamp, selection determinism + cap truncation + missing-anchor, min-gap math + midnight wrap, implausible OHLC, composite-key persist tracker, row/audit builders, mock-server 200-with-oi / 401-auth / 429-rate-limit / transport arms, persist-gated verdict), ratchet_groww_contract1m_* wiring guards (spawn gated inside the dual-arm helper + chain-dependency binding + refusal warn, sequencing channel + unconditional chain publish + anchor handoff, stub-guard, panic-free production region, strike parse-only)

- [ ] PR-5 — latency visibility surfacing (Quote 2): plain-English per-feed per-leg line on the 15:45 dual-feed scorecard / EOD digest ("Groww spot 1m: fetched median X.Xs / p99 Y.Ys after close; chain: median Z.Zs" — and the Dhan leg's line for parity), + dashboard gauge. Insertion point: `crates/app/src/feed_scoreboard_boot.rs` (summary struct ~:2428/:2496, `DualFeedDailyScorecard` variant + render arm in events.rs), following the PR-C keep-better/−1-sentinel honesty conventions.
  - Files: crates/app/src/feed_scoreboard_boot.rs, crates/core/src/notification/events.rs
  - Tests: scorecard-line render tests, keep-better guard tests, −1-sentinel honesty tests

## Design

Mirror the Dhan per-minute REST pipeline (spot_1m_rest_boot.rs + option_chain_1m_boot.rs + their persistence modules — the proven template incl. the #1499 hotfix lessons) onto Groww, as a cold-path scheduled fetcher family in `crates/app/`, persistence in `crates/storage/`, constants/config in `crates/common/`.

- **Rate budget:** Groww documents type-level limits — **Live Data 10/sec + 300/min** (Orders 10/s+250/min; Non Trading 20/s+500/min; NO per-day cap documented). The `/historical/*` and `/option-chain/*` buckets are UNNAMED in the docs table → conservatively ASSUMED Live Data. Our in-session load ≈ 3 spot + 3 chain + a bounded contract set ≈ **~6–12 requests/min** — far inside 10/sec + 300/min even on the shared bucket. Per-key vs per-token vs per-account limit scoping is officially UNSTATED (Unknown); both BruteX and TickVault present the ONE shared daily minter token, so the bucket is EFFECTIVELY shared for us either way (Assumed inference from the single shared token, not a documented fact). BruteX's bulk historical pulls are nightly/post-market — time-disjoint from our in-session per-minute pulls (Assumed; coordinate before relying on tighter headroom).
- **Capacity verdict (2026-07-13 dedicated docs-research session — 26 official pages + growwapi 1.5.0 wheel; cite the refreshed `docs/groww-ref/` pack for constants once its PR merges):**
  - Rate buckets are TYPE-LEVEL POOLED — exhausting one API throttles the whole type. Numbers re-confirmed: Live Data 10/s + 300/min; Orders 10/s + 250/min (changed 15→10 between Dec'25 and Mar'26 — the numbers CAN change, re-verify on the box); Non-Trading 20/s + 500/min; NO per-day cap officially.
  - 429 semantics: the only official guidance is "throttle the request rate" — NO documented Retry-After / ban / cooldown. The SDK has ZERO client-side throttling and infinite default timeouts, so pacing + timeouts are entirely OURS.
  - Minute-boundary pacing rule (binds PR-2/3/4): spread each boundary burst to ≤6 req/s against the shared 10/s ceiling (BruteX co-tenancy on the same token — its ~3 req/s sustained is an operator-relayed empirical anchor; its nightly timing is Assumed, not verified).
  - Worst-case load ~18 req/min (3 spot + 3 chain + ~12 contracts) ≈ 6% of the 300/min budget solo; ~66% if BruteX runs in-session — still inside.
  - Token: the ~06:00 IST daily expiry is OFFICIALLY documented (upgraded from assumption).
  - Historical 1m per-request range cap = 30 days — our day-granular windows are far inside.
- **Sequencing (dated 2026-07-13):** the day-window + backfill + sweep patterns PR-2 mirrors live in Dhan hotfix PR #1499 — still OPEN (blocked) at PR-2 authoring, and the two diffs overlap in `rest-1m-pipeline-error-codes.md` + `config/base.toml` context lines; whichever merges second rebases — coordinated at push time (hostile round 1 item 3).
- **Live-probe list (the PR-2/3/4 instrumentation MUST capture — mirrored in Observability):** (a) just-closed-minute freshness distribution per index (poll-until-present ladder latency); (b) in-progress-day serving confirmation; (c) which bucket `/v1/historical/*` and `/v1/option-chain/*` actually share (429 correlation vs live-data volume — undocumented, verified-by-omission); (d) per-endpoint response-time histograms + chain payload byte size / strike count per underlying; (e) every 429's timestamp / endpoint / body shape / Retry-After presence / recovery time; (f) minute-boundary burst tolerance (does 10-in-1s pass); (g) per-API-key vs per-account limit scoping.
- **UNVERIFIED-LIVE items (the first live session is the probe):** (1) just-closed-minute freshness — no documented availability delay for the sealing minute; NEVER assert "within a second", always show the MEASURED close-to-data number (Quote 2); (2) the V2 candle timestamp type (`yyyy-MM-dd HH:mm:ss` string vs epoch — parse defensively for both); (3) which rate bucket `/historical/*` + `/option-chain/*` count against (watch for 429 / `GrowwAPIRateLimitException` behavior).
- **Token:** shared-minter SSM READ-ONLY via the existing `fetch_groww_access_token` (`/tickvault/<env>/groww/access-token`) — NEVER mint, never cache past an auth failure, per `groww-shared-token-minter-2026-07-02.md`. Headers: `Authorization: Bearer <token>` + `x-api-version: 1.0`.
- **Identity:** the CANDLE legs (spot + contracts) take `groww_symbol` (NOT exchange_token, NOT bare trading symbol): indices `NSE-NIFTY` / `NSE-BANKNIFTY` / `BSE-SENSEX` segment `CASH`; contracts `NSE-NIFTY-04Jan24-19200-CE` segment `FNO`. The CHAIN endpoint's `underlying` path param is the PLAIN symbol (`NIFTY` / `BANKNIFTY` / `SENSEX`), NOT groww_symbol. Persisted `security_id` = the SAME ids the Groww live lane uses (`stable_index_security_id` for indices; `exchange_token` for contracts) so cross-source joins work; `feed='groww'` on every row, `feed` in every DEDUP key (I-P1-11 + feed-in-key).
- **Error codes:** REUSE the SPOT1M-01/02 + CHAIN-02/03 code strings with a `feed` field on the emit where the semantics match (same stage taxonomy, same 3-minute FailureEdge pager); NEW GROWW-specific variants ONLY where semantics diverge (e.g. there is NO Dhan-style entitlement probe — the Groww chain endpoint is documented-available, so no CHAIN-01 analog is expected). Final call lands in PR-2/PR-3 with any new variant's rule-file mention in the SAME PR (the `error_code_rule_file_crossref.rs` contract).
- **Semantics parity (stated explicitly, 2026-07-13 operator addition):** the Groww leg carries IDENTICAL retry/backfill/page semantics to the Dhan #1490/#1499 leg: in-minute bounded ladder → previous-minute backfill sweep → bounded 15:31 post-session sweep → named gap + edge-triggered page. No silent holes.
- **Sequencing semantic (Dhan-identical, conscious mirror):** the chain leg (PR-3) is SEQUENCED after the spot fire via the watch signal + bounded fallback timer — it is NOT hard-gated on spot success (deliberate: a dead spot leg must not silently kill chain capture; the Dhan design does the same — the spot leg publishes its minute-done signal unconditionally, success or failure).
- **Schema grounding (2026-07-13 operator addition — what each persisted column is grounded in):** (a) the endpoint/params/headers and the candle-tuple SHAPE `[ts, o, h, l, c, volume, oi]` are grounded in the citable reference pack — `docs/groww-ref/11-historical-candles.md` (endpoint/intervals/range caps + "current-day serving documented, minute-freshness UNDOCUMENTED"), `docs/groww-ref/15-rate-limits-and-capacity.md` (official limits + capacity math + the §6 live-probe measurement plan), `docs/groww-ref/17-token-lifecycle.md` (documented ~06:00 IST daily token expiry), `docs/groww-ref/99-UNKNOWNS.md` (the [U-n] probe list), `docs/groww-ref/README.md` (reconciled claims table) — + the `growwapi` 1.5.0 wheel source (client.py:875-917, :1362-1378) AND in BruteX PRODUCTION responses — BruteX calls these same two endpoints daily on this account and its CSVs (real response data: `symbol,timestamp_ist,o,h,l,c,volume,oi`) are in our S3 (the §37 crossverify pack) — so `open/high/low/close/volume` (and `oi` for the PR-4 contract leg) are production-grounded; (b) the ts WIRE FORMAT (epoch seconds vs `yyyy-MM-dd HH:mm:ss` IST string) is **Assumed → confirm-live**: the dual parser accepts both and `tv_groww_spot1m_ts_form_total{form}` records which form the live server sends (epoch numbers are Assumed UTC-epoch; strings Assumed IST wall-clock); (c) derived columns (`close_to_data_ms`, `fetched_at`, `security_id` = the live lane's `stable_index_security_id`, `feed`/`source`/`symbol` labels, and every `rest_fetch_audit` column) are OUR measurements/stamps, not vendor fields — no vendor-field column is populated from anything unseen in a real response.
- **Forensics (2026-07-13 operator addition):** every (minute, symbol) fetch — success AND failure — writes one `rest_fetch_audit` row (attempts, which rung succeeded via attempts count, 429 count, final HTTP status, final-attempt round-trip ms, close-to-data ms, typed outcome, bounded error_class). Dhan-side emits = fast follow-up after #1499 merges. Scorecard aggregates (attempts p99, rate-limit hits/day) land in PR-5.
- **Out of scope:** GDF (parked — its own plan/trial per `gdf-third-feed-scope-2026-07-13.md`); any bulk/backtest history sweep (§33's ban otherwise stands — §38.1); volume classification in any verdict; strategy/order wiring (§28 boundary). Box boot MID-SESSION: earlier minutes are NOT bulk-backfilled (the §38 grant forbids bulk sweeps) — they stay named gaps; the first fire targets the just-closed minute only.

## Edge Cases

- Just-closed minute not yet sealed at first poll → bounded in-minute re-poll ladder (spot leg), day-window + client-side minute filter (#1499 pattern) so a late-sealing vendor is recovered by the next fire's one-minute backfill + the 15:31 sweep.
- Zero-trade minutes on thin option strikes: whether Groww emits gap-filled or ABSENT candles is Unknown — a missing contract minute is counted + reported, never fabricated.
- Timestamp format ambiguity (string vs epoch) → defensive dual parse, fail-loud on neither matching.
- Groww's ~06:00 IST daily token reset mid-warmup → SSM re-read on auth-class failure at ≥60s pacing (never mint).
- Expiry day: current expiry from the instruments CSV holds through the session (nearest ≥ today; never-roll — the index_futures precedent).
- Duplicate rows + 21-column header in the instruments CSV → parse by header name, dedup on `(exchange, exchange_token)` (already the ingested master's discipline).
- 429 / rate-limit: counted, never retried past the ladder; min-gap pacing on the chain/contract legs.
- Clock steps / suspend: `fire_is_fresh` staleness gate + missed-boundary accounting (the Dhan scheduler's H1/H2 invariants, reused).

## Failure Modes

Exhaustive failure-mode MATRIX (2026-07-13 operator addition; spot leg =
shipped code paths, chain/contract-specific rows marked PR-3/PR-4).
Columns: **Detect** (how it is seen) | **Retry** (in-minute) | **Recover**
(cross-minute) | **Page** (operator signal) | **Forensics row**
(`rest_fetch_audit` outcome/error_class).

| Case | Detect | Retry | Recover | Page | Forensics row |
|---|---|---|---|---|---|
| Slow response >1s | `tv_groww_spot1m_fetch_duration_ms` histogram + per-row `fetch_latency_ms` | ladder continues normally | n/a (latency only) | none | latency stamped on the row |
| Slow response >5s | per-request 5s timeout fires (`GROWW_SPOT_1M_REQUEST_TIMEOUT_SECS`) | next ladder rung | backfill/sweep if the whole ladder misses | edge at 3 consecutive full-fail minutes | `error`/`transport` |
| Slow response >30s (stall) | per-symbol 18s hard budget (`tokio::timeout`) — can never reach 30s | budget kills the ladder | next fire's backfill + 15:31 sweep | edge | `error`/`budget_exceeded` |
| 2xx with EMPTY body | parse → 0 candles → target absent | ladder re-polls | backfill + sweep | edge | `empty`/`target_absent` |
| 2xx with STALE candle (previous minute served) | CLIENT-SIDE minute filter — exact IST-minute match only (tested: `test_stale_body_serving_previous_minute_is_target_absent_but_backfills`) | ladder re-polls; the stale body's PREV minute is still mined as backfill | backfill + sweep | edge | `empty`/`target_absent` |
| Partial/malformed JSON | typed parse degrade — malformed rows skipped + `tv_groww_spot1m_parse_malformed_rows_total`; never a panic | ladder re-polls | backfill + sweep | edge | `empty` or `error` per final rung |
| Wrong-timestamp-convention rows | dual parser (epoch UTC secs/millis + IST string) + `tv_groww_spot1m_ts_form_total{form}`; BOTH-fail = malformed row (counted) | ladder re-polls | backfill + sweep | edge | `empty`/`target_absent` |
| 429 / adaptive throttle | REAL StatusCode check → `tv_groww_spot1m_rate_limited_total` + one bounded `warn!` with endpoint/Retry-After presence/sanitized body (live-probe e) | rides the ladder like any error — never retried past it, never bursts past the sequential pacing | next fire (60s later) | edge | `rate_limited`/`rate_limited` + `rate_limited_count` |
| 401 token expired mid-session (~06:00 reset class) | 401/403 on the REAL StatusCode | ladder finishes; cache DROPPED post-fire | SSM re-read at ≥60s pacing next fire — NEVER mint (minter lock) | edge if 3 minutes fail | `error`/`auth` |
| DNS / TLS / conn-reset | transport error arm (status 0) | ladder re-polls | backfill + sweep | edge | `error`/`transport` |
| Minute-boundary clock skew | BOOT-03 owns host clock (±2s halt); scheduler uses the SAME IST wall-clock math as the Dhan leg; `fire_is_fresh` 30s staleness gate | skip stale wake | missed boundaries counted + backfill/sweep | edge (skips feed it) | `skipped`/`boundary_skipped` |
| 09:16 first-fire edge | targets the 09:15 candle; NO backfill (previous minute is pre-open — session-first gate, tested) | normal ladder | n/a | normal | normal |
| 15:30 last-fire edge | targets 15:29; no NEXT fire exists → | normal ladder | the 15:31 post-session sweep is 15:29's ONLY repair path | edge | swept `ok` (honest >60s delay) or `error`/`named_gap` |
| QuestDB down during persist | append/flush Err via the ILP-HTTP per-request ACK | none (persist is not retried in-minute) | discard-pending; success is PERSIST-gated (fetch-ok-but-unpersisted = failed minute); DEDUP-idempotent re-append via backfill/sweep/manual re-run | edge (M1 persist-gated) — a day-long outage pages | row itself best-effort (may be lost with the outage — counted discards) |
| Process restart mid-minute | missed boundary → `boundary_skipped` accounting on the respawned run | none | next fire's one-minute backfill recovers the previous minute; watermark rebuilt from the flush-confirmed tracker (empty after restart → sweep covers the tail) | edge | `skipped`/`boundary_skipped` |
| Box boot mid-session | first fire targets the just-closed minute | normal | earlier minutes NOT bulk-backfilled (§38 forbids bulk sweeps) — NAMED GAPS via the sweep's above-watermark walk (watermark empty → sweep re-fetches the day window once and repairs what the vendor still serves; whatever stays absent is named) | edge only if ongoing minutes fail | `named_gap` rows for the unrecoverable |
| Minute closes unfetched after the ladder | own-fire `empty`/`error` verdict (honest — never flipped by a later repair) | none | previous-minute BACKFILL in every subsequent ladder arm; then the 15:31 sweep; if STILL unrecovered → NAMED GAP | 3-consecutive edge page — never a silent hole | final state: `ok` (repaired, honest delay) or `error`/`named_gap` |
| Task death | supervisor `classify_join_exit` + `tv_groww_spot1m_task_respawn_total{reason}` | n/a | bounded 30s respawn (release `panic="abort"` honesty: panic-respawn is unwind-build only) | coded `task_respawn` error | n/a |
| Chain-specific: expiry-day rollover | SHIPPED PR-3 — expiry resolved ONCE at warmup via `select_current_option_expiry` (nearest ≥ today); on T-0 the same-day expiry HOLDS through the session (never-roll, unit-pinned `test_select_current_option_expiry_expiry_day_t0_holds_t1_rolls`); T+1's boot rolls automatically | n/a (a day-start decision, never retried intra-day) | next trading-day warmup re-resolves; an unresolvable underlying degrades for the day (`stage="expiry_unresolved"` + `tv_groww_chain1m_expiry_unresolved_total`) | typed HIGH `GrowwChain1mExpiryUnresolved` ONCE per underlying per warmup (edge — never per-minute spam) | normal chain rows carry the day's expiry; an unresolved underlying simply has NO rows (named by the page + counter, never fabricated) |
| Chain-specific: chain-ok-but-spot-failed | SHIPPED PR-3 — the chain is NOT hard-gated on spot success: `minute_done_tx` (watch signal) is published UNCONDITIONALLY after every spot fire (success or failure — ratchet-pinned in the wiring guard), and the `GROWW_CHAIN_1M_FALLBACK_DELAY_MS = 2500` timer covers a dead/disabled/slow spot leg entirely | normal chain fire (one request per underlying, sequential, 1 s min-gap) | chain minutes are independent of spot minutes — a spot outage never creates chain gaps | chain-only `FailureEdge` (3 consecutive persist-gated fails) via `GrowwChain1mFetchDegraded`; the spot leg pages its own edge independently | one `rest_fetch_audit` row per (minute, underlying), `leg='chain_1m'` — the leg column splits the two pipelines' forensics |
| Contract-specific: zero-trade minutes on thin strikes | SHIPPED PR-4 — absent vs gap-filled candles is UNVERIFIED-LIVE; a target-absent contract minute is `outcome="empty"` (counted via `tv_groww_contract1m_fetch_total{outcome="empty"}`), never fabricated | none (one request per contract per minute — no ladder) | the one-minute-lookback backfill mined from the next fire's day-window body; NO 15:31 sweep (selection is minute-scoped ATM — an unrecovered minute is a NAMED absence) | empty minutes do NOT feed the edge when ≥1 contract persisted (persist-gated verdict); a fully-failed minute streak pages `GrowwContract1mFetchDegraded` at 3 | `empty`/`target_absent` per contract |
| Contract-specific: no chain anchor (chain leg failed/late) | SHIPPED PR-4 — `stage="selection_unresolved"` + `tv_groww_contract1m_selection_unresolved_total`; anchors are STICKY across minutes so one failed chain minute never blanks the selection | n/a (an ATM is never guessed) | the next chain fire refreshes the anchor | a whole-minute selection blank = fully-failed minute → the 3-minute edge (the chain leg pages its own edge independently) | no per-contract rows exist (no selection was made); the verdict log names the unresolved underlyings |

## Test Plan

- Pure-fn unit tests for every scheduler/window/filter/backfill/selection/parse primitive (the spot_1m_rest_boot test pattern); proptest on the response parsers (hostile bodies).
- Wiring guard tests per leg (spawn-site + no-TCP-ILP + config-gate pins — the existing `*_wiring_guard.rs` pattern in crates/app/tests/).
- DDL/DEDUP tests for the new `option_contract_1m_rest` table (dedup_segment_meta_guard compliance: ts first, segment + feed in key).
- Config default-OFF tests for all three `[groww_*]` sections.
- ErrorCode cross-ref + tag-guard tests pass in the same PR as any new variant.
- Scoped per `testing-scope.md`: `cargo test -p tickvault-app -p tickvault-storage`; `crates/common/` changes escalate to workspace.

## Rollback

- PR-1 (docs): `git revert` of the single docs commit restores the §33/§1 bans verbatim (dated-note pattern — no text was deleted).
- PR-2..PR-5: each leg is config-gated serde-default-OFF — flipping the base.toml key OFF restores byte-identical prior behavior with zero code change; full rollback = `git revert` of the leg's commits (new tables are additive; no schema mutation of existing tables beyond feed-tagged rows, which DEDUP-isolate).
- No feature interlocks: spot / chain / contract legs are independently disable-able; the Dhan legs are untouched.

## Observability

- Histograms: `tv_groww_spot1m_close_to_data_ms`, `tv_groww_chain1m_close_to_data_ms` (+ per-request fetch-duration histograms) — the Quote 2 mandate; per-row `close_to_data_ms` columns stamp the real delay including backfilled/swept rows (never sampled into the own-fire histogram — the #1499 semantics split).
- Counters per leg: fetch outcomes {ok, empty, error, rate_limited}, rows written/discarded, backfilled/sweep counters, task respawns {reason}, boundary-skipped.
- Coded `error!` with stage taxonomy + the edge-triggered HIGH Telegram pages (typed events) + Info recovery events; delivery boundary honest: log-sink-only for the codes, the typed Telegram is the operator page (the §3 rest-1m precedent).
- PR-5: plain-English daily latency lines on the 15:45 scorecard + EOD digest (per-feed, per-leg, median/p99 seconds after close) + dashboard gauge — the operator-visible answer to "within how many seconds precisely".
- Forensic ground truth: the `spot_1m_rest` / `option_chain_1m` / `option_contract_1m_rest` tables themselves (`questdb_sql` counts).
- **Live-probe instrumentation (mandatory in PR-2/3/4 — the Design live-probe list):** (a) just-closed-minute freshness distribution per index (poll-until-present ladder latency); (b) in-progress-day serving confirmation; (c) the actual rate-bucket sharing of `/v1/historical/*` + `/v1/option-chain/*` (429 correlation vs live-data volume); (d) per-endpoint response-time histograms + chain payload byte size / strike count per underlying; (e) every 429's timestamp / endpoint / body shape / Retry-After presence / recovery time; (f) minute-boundary burst tolerance (does 10-in-1s pass); (g) per-API-key vs per-account limit scoping.

## Per-Item Guarantee Matrix

The canonical 15-row + 7-row matrices from `per-wave-guarantee-matrix.md` apply to EVERY item in this plan; the project-specific fills:

### 15-row 100% Guarantee Matrix

| Demand | This plan's proof artefact |
|---|---|
| 100% code coverage | ratcheted per-crate floors (`quality/crate-coverage-thresholds.toml`); PR-2..5 add tests with every module |
| 100% audit coverage | rows in `spot_1m_rest` / `option_chain_1m` / `option_contract_1m_rest` with DEDUP UPSERT KEYS incl. `feed` |
| 100% testing coverage | unit + proptest + wiring-guard + config-gate tests per leg (22-category mapping declared per PR) |
| 100% code checks | banned-pattern + pub-fn-test + pub-fn-wiring + plan-verify + secret-scan gates on every push |
| 100% code performance | cold-path only — NO hot-path change; no DHAT/Criterion delta claimed (N/A — flagged honestly) |
| 100% monitoring | histograms + counters + gauges per leg (Observability section) |
| 100% logging | every failure path `error!` with `code = ErrorCode::X.code_str()` + stage field |
| 100% alerting | edge-triggered typed HIGH Telegram at the 3-minute escalation edge + Info recovery |
| 100% security | token via SSM read-only `Secret` path; never logged, never in URLs; secret-scan gates |
| 100% security hardening | body caps + redirect Policy::none() + host pinning (the csv_downloader §18 / socket_token patterns) |
| 100% bugs fixing | adversarial 3-agent review before + after each code PR |
| 100% scenarios covering | Edge Cases + Failure Modes sections enumerate the failure classes; sweep/backfill close the vendor-late class |
| 100% functionalities covering | every new pub fn has a call site + test (pre-push gates 6+11) |
| 100% code review | 3-agent pass on each PR diff, before AND after impl |
| 100% extreme check | ratchet tests (wiring guards, DEDUP meta-guard, config-default pins) fail the build on regression |

### 7-row Resilience Demand Matrix

| Demand | This plan's honest envelope |
|---|---|
| Zero ticks lost | UNTOUCHED — this pipeline never writes `ticks`/`candles_*`; the WS capture chain is not modified |
| WS never disconnects | UNTOUCHED — no WS change; REST legs are cold-path siblings |
| Never slow/locked/hanged | cold-path scheduled tasks; every request timeout-bounded; per-leg hard budgets; zero hot-path allocation added |
| QuestDB never fails | ABSORB: discard-pending + DEDUP-idempotent re-fetch; gaps loud, never silent |
| O(1) latency | N/A on hot path (no hot-path change); per-fire work is bounded-constant (3 indices + capped contract set) — flagged honestly, not claimed O(1) globally |
| Uniqueness + dedup | `(ts, security_id, exchange_segment, feed)`-class DEDUP keys on all three tables (I-P1-11 + feed-in-key) |
| Real-time proof | close-to-data histograms + per-row latency columns + the daily scorecard line = the measured, never-asserted freshness number |

Honest claim wording: any "100% guarantee" in these PRs is qualified "100% inside the tested envelope, with ratcheted regression coverage" per operator-charter §F.

## Scenarios

| # | Scenario | Expected |
|---|---|---|
| 1 | 09:16:00 fire, Groww serves the 09:15 candle on first poll | row persisted, close-to-data latency measured and recorded (per-row column + histogram) — never pre-asserted |
| 2 | Vendor seals the minute late (> ladder) | own-fire counted failed (honest), next fire's backfill or 15:31 sweep repairs; real delay stamped per-row |
| 3 | 429 mid-session | counted, edge feeds, no tighter polling |
| 4 | Token reset at ~06:00 IST | SSM re-read ≥60s pacing; never mint |
| 5 | QuestDB outage window | discard-pending + loud gap; DEDUP-idempotent backfill after recovery |
| 6 | Chain leg first live session | verified live → operator flips `[groww_option_chain_1m] enabled = true` with a dated note |
