# Groww 99 — CONSOLIDATED UNKNOWNS (the single open-questions file)

> **Source:** n/a — this file enumerates what the public Groww Trade API evidence genuinely does NOT state, consolidated from the 2026-07-13 research + refutation passes over the 2026-07-03 lossless 26-page capture, the `growwapi` 1.5.0 wheel, and same-day live cross-checks.
> **Fetched/Verified:** 2026-07-13 (via the 2026-07-03 lossless capture + 2026-07-13 live cross-checks; groww.in direct fetch proxy-blocked from the sandbox — provenance stated honestly)
> **Evidence tiers:** n/a — every row is by definition Unknown (or Assumed pending confirmation). Inline `U-<n>` flags in `11-historical-candles.md`, `14-option-chain.md`, `15-rate-limits-and-capacity.md` resolve here.
> **How to use:** rows marked *support* → ask Groww API support with the EXACT question wording; rows marked *probe* → answerable in the first live session with a real key; *both* → ask AND verify.
> **Blocking legend:** `pipeline` = blocks the per-minute REST pipeline design · `capacity` = blocks the capacity/co-tenancy verdict · `ops` = blocks operational planning · `none` = nice-to-know.

| # | Question (exact wording to ask / probe) | How | Why it matters | Blocking |
|---|---|---|---|---|
| U-1 | **Current-day serving:** "During market hours, does `GET /v1/historical/candles` return candles for the CURRENT in-progress trading day?" Probe: in-session, request `start_time=<today 09:15:00>`, `end_time=<now>`, `candle_interval=1minute` — are today's minutes returned, and up to which minute? The docs contain ZERO statements either way (grep-verified over all 26 pages, 2026-07-13); the only adjacent hint is the OHLC note pointing interval-candle users at the historical surface. | probe | the entire per-minute spot-1m design assumes same-day service; BruteX only proves post-close same-day service | pipeline |
| U-2 | **Just-closed-minute freshness:** at second T of minute M+1 (T≈1..10s), request 1minute candles ending at minute M — (i) is candle M present, (ii) at what T does it first appear, (iii) does the endpoint ever return an IN-PROGRESS candle for M+1, (iv) do candle M's OHLCV values mutate on re-poll within the next 60 s? Nothing documented anywhere (no latency, no SLA, no partial-candle statement). | probe | sets the poll-until-present ladder + the close-to-data histogram baseline | pipeline |
| U-3 | **Which caps table binds live, and how it fails:** "Do the Backtesting Data Limits (30/90/180 days per request) still bind `GET /v1/historical/candles` today, and does exceeding a cap return an error (which GA code?) or silently truncate?" Probe: request 31 days of 1minute data. (Caps rows are capture+mirror-verified but never surfaced in live snippets; the landing page's "up to 3 months" marketing pollutes search extraction.) | both | chunking math for any backfill; silent truncation would corrupt range assumptions | pipeline |
| U-4 | **Rate-limit family membership:** "Which rate-limit type do `/v1/historical/candles`, `/v1/historical/candle/range`, `/v1/historical/expiries`, `/v1/historical/contracts`, `/v1/option-chain/*`, `/v1/live-data/greeks/*`, `/v1/order-advance/*`, and `/v1/user/detail` count against — Orders, Live Data, Non Trading, or a separate bucket? What are that bucket's per-second/per-minute numbers?" Probe: burst `/historical/candles` >10/s and >20/s; after a 429, immediately issue one `get_ltp` (429 = shared Live Data; 200 = separate bucket). | both | the §3/§4 arithmetic in `15-rate-limits-and-capacity.md` rests on the Live-Data Assumption | capacity |
| U-5 | **Limit scope:** "Are the published limits enforced per API key, per access token, or per Groww account (client)? If two independent systems use the same account (different keys), do they share one budget?" | both | BruteX co-tenancy — shared-token evidence suggests one account pool, doc-unproven | capacity |
| U-6 | **Timezone:** confirm candle timestamps and `start_time`/`end_time` are IST wall-clock (Assumed; no timezone stated anywhere in the docs; no UTC mention on any captured page). Probe: compare one known session-open candle label against 09:15 IST. | probe | a hidden UTC convention would shift every time key by 5h30m | pipeline |
| U-7 | **429 mechanics:** "Does the 429 response carry a `Retry-After` header or a machine-readable error code/body? Is the window fixed or sliding? Is there any escalation (temporary block/cooldown) for sustained 429s?" Probe: capture one live 429's full headers + body. (SDK reads nothing from it; no body shape documented.) | both | retry-ladder design; the 26-page grep found zero cooldown/ban language | pipeline |
| U-8 | **REST-side adaptive throttle:** "We observed adaptive connection-capping on the WebSocket feed (33 fresh → 7 after churn, ~35–40 min penalty); does the REST surface apply any adaptive per-account throttling, penalty window, or fair-use enforcement beyond the published fixed limits?" | both | a WS-proven adaptive control makes fixed-window-only an optimistic assumption | capacity |
| U-9 | **Batch billing:** "Does a 50-symbol `get_ltp`/`get_ohlc` call count as 1 request or 50 against the Live Data budget?" (Assumed 1 — it is one HTTP request.) Also: does the raw endpoint accept both comma-joined and repeated `exchange_symbols` encodings (the SDK ships both paths)? | both | any batched fallback pattern's arithmetic | capacity |
| U-10 | **Per-day cap:** "Is there ANY daily request cap per type? (Your public table shows only per-second and per-minute.)" No per-day column exists officially; the circulating 5,000/day figure is proven 2025-stale and empirically implausible against BruteX volume — but an undocumented cap ABOVE that volume cannot be excluded. | both | all-day pipeline volume (~2.3–6.8K req/day TickVault + BruteX) | capacity |
| U-11 | **Decimal strike keys:** fetch a stock-option chain carrying a decimal strike and inspect the `strikes` key format (the doc example shows only integer-string rupee keys like `"23400"`). | probe | chain parser key typing | pipeline |
| U-12 | **Chain size:** `len(payload["strikes"])` + raw payload byte size for NIFTY / BANKNIFTY (NSE) + SENSEX (BSE) current expiry, repeated on an expiry day (worst case). No strike count is documented anywhere; ~90–110 strikes ≈ ~200 legs ≈ ~100–300 KB is an Assumed order of magnitude only. | probe | parse/persist budget for `option_chain_1m`-class tables | pipeline |
| U-13 | **Un-entitled failure shape:** call `get_option_chain` once with a key lacking the entitlement — does it fail 403, or a GA00x body, and with what message? (No Dhan-DH-902-style "not subscribed" code is documented.) | probe | entitlement-probe classification (the CHAIN-01 pattern needs an exact shape) | pipeline |
| U-14 | **WS 1000-subscription cap scope (pre-existing):** "Is the 'upto 1000 subscriptions at a time' Live-feed limit per CONNECTION or per ACCOUNT?" Docs state only the number; per-conn vs per-account has been unresolved since the 2026-07-03 capture. | both | Groww watch-set sizing headroom (~779 of 1000 used today) | ops |
| U-15 | **WS connection cap per account:** "How many concurrent Live-feed connections does one account support?" Nothing documented; the operative truth is the MEASURED adaptive cap (33 fresh / 7 after churn, ~35–40 min penalty, 2026-07-06). Ask for the official policy. | support | any future multi-connection design (currently locked out) | none |
| U-16 | **Expiries `year` range discrepancy:** the docs say `year` is "2020 - current year"; the SDK docstring says "between 2000 and 5000" (both byte-verified). Probe: request `year=2019` and `year=<next year>` — reject code vs empty list? | probe | expiry-warmup input validation | none |
| U-17 | **One-active-token semantics:** "Does minting a new access token invalidate previously issued tokens for the same key/account?" (Contrast: Dhan documents one-active-token explicitly; Groww documents nothing. Empirically BruteX + TickVault USE one shared minted token concurrently; whether a second MINT kills the first is undocumented — the shared-minter lock exists precisely to avoid finding out.) | both | token-minter safety; a stray mint must not kill the live feed's token | ops |
| U-18 | **Token-reset-instant 401 shape:** at/after the daily 06:00 AM expiry ("(Expires daily at 6:00 AM)" — official REST intro, capture 2026-07-03), what exactly does a REST call return — HTTP 401 vs 403, and which GA code/body? Probe once with an expired token. Also: is the reset exactly 06:00:00, and does a token minted at 05:59 survive 61 seconds? | probe | auth-failure classification (never body-text matching without a known shape) | ops |
| U-19 | **Token `expiry` response field + TOTP-flow contradiction:** the token-mint response documents `expiry` ("Expiry date-time (ISO format)") + `tokenRefId`/`sessionName`/`isActive` — the SDK discards all but `token`. Probe: read `expiry` once live — does it say next-06:00? And: the python-sdk page says the TOTP flow is "No Expiry" while the REST intro says the TOTP flow "Requires daily approval" — production has run since 2026-07-02 without daily human approval (empirical), so the python-sdk wording appears operative; confirm with support. | both | minter cron correctness; documented-vs-operative auth contract | ops |
| U-20 | **Instruments CSV refresh cadence + size:** "At what time IST is `https://growwapi-assets.groww.in/instruments/instrument.csv` regenerated daily?" Neither cadence nor size is documented. Probe: `curl -sI` for `Content-Length`/`Last-Modified`/`ETag` across a trading-day boundary. (Internal capture observed ~23.5 MB / ~164k rows, daily-changing — internal measurement, not a doc fact; TickVault's daily pre-market refresh is our convention.) | both | daily universe build timing (the ~08:45 fetch) | ops |
| U-21 | **MCX contradiction (pre-existing, three-way drift):** REST intro (capture 2026-07-03): "Commodities trading (MCX segment) is not available at this time" vs changelog v1.5.0 claiming MCX support vs the curl live-data page's "COMMODITY for commodity contracts" segment wording. "Is MCX/COMMODITY live on the Trade API today?" Irrelevant to the 4-index scope but shows the docs churn. | support | none (out of scope; churn indicator) | none |
| U-22 | **BSE F&O EOD URL (pre-existing, NSE/BSE-EOD side not Groww API):** the BSE F&O download URL recorded in `08-master-groww-nse-bse-context.md` §3.3 is flagged "🚫 UNVERIFIED — do not trust blind". Verify before any BSE EOD ingestion. | probe | none (dormant ingestion brief) | none |
| U-23 | **Quote depth level count:** the `get_quote` doc example shows 1 entry per side; 5 levels is Assumed (feed-side depth is 5-level). Probe: count `depth.buy` entries in one live response. | probe | quote-parser array sizing | none |
| U-24 | **Live freshness of quote/LTP:** measure `now_ms - last_trade_time` distribution on live `get_quote` calls for a liquid SID during market hours ("real-time snapshot" is the only qualitative claim; LTP/OHLC/chain responses carry NO timestamps at all). | probe | staleness measurement — the only staleness-measurable REST field | none |

## Priority ordering

- **Before the per-minute pipeline design freezes:** U-1, U-2, U-3, U-4, U-6 (all `pipeline`-blocking; U-1/U-2 are THE central questions the docs never answer).
- **First hour of the first live session:** U-1, U-2, U-6, U-11, U-12, U-13, U-18, U-23, U-24 + the §6 measurement plan in `15-rate-limits-and-capacity.md` (histograms, burst pacing, family inference).
- **Groww support email (one message, exact wordings above):** U-4, U-5, U-7, U-8, U-9, U-10, U-14, U-17, U-19, U-20.
- **Nice-to-know / dormant:** U-15, U-16, U-21, U-22.
