# Error-Code Runbooks

Per-error-code runbook files moved verbatim out of the always-auto-loaded
`.claude/rules/` tree (context-size incident 2026-07-20, Phase 2 — nothing
deleted, content unchanged). `ErrorCode::runbook_path()` in
`crates/common/src/error_code.rs` points here; the crossref test scans this
directory alongside `.claude/rules/`. Index pointer for sessions:
`.claude/rules/project/error-runbooks-index.md`.
