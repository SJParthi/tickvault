# GDF 99 — CONSOLIDATED UNKNOWNS (the single open-questions file)

> **Fetched:** 2026-07-13 · **Evidence tier:** n/a — this file enumerates what the public evidence genuinely does NOT state. Every `**Unknown**`/`U-n` flag in files 01–13 resolves here. Union of all research passes, deduped.
> **How to use:** rows marked *sales* → email/call GDF sales (sales@globaldatafeeds.in, +91-77210 80002) with the EXACT question wording; rows marked *probe* → answerable in the first hour of a trial key; *both* → ask AND verify.
> **Blocking legend:** `client` = blocks building the Rust client · `purchase` = blocks the buy/no-buy decision · `ops` = blocks operational planning · `none` = nice-to-know.
> **2026-07-13:** the browser-paste capture queue for closing the doc-derived unknowns below lives in [`00-PASTE-LIST.md`](./00-PASTE-LIST.md) (WAF re-verified total that date; operator paste is the only route).

| # | Question (exact wording to ask / probe) | How | Why it matters | Blocking |
|---|---|---|---|---|
| U-1 | **PARTIALLY CLOSED 2026-07-13 (LIVE-DOC operator paste of the stream-all-symbols page — see 10):** StreamAllSymbols wire format (Batch envelope, abbreviated keys, `T:"RT"` rows) + entitlement model (auth-only; firehose auto-starts for all exchanges enabled on the key) + scope (entitled exchanges, incl. untraded symbols) are now Verified. **STILL OPEN — ask/probe:** "Is StreamAllSymbols served on a dedicated endpoint/port or the standard WS endpoint? What is the Batch emission cadence and max rows per Batch frame? What is the expected bandwidth for a full NFO universe? Can request-response functions (GetLimitation, GetInstruments) be sent on the same streaming connection? Please share the StreamAllSnapshots response format. What does enabling an exchange for streaming cost?" | sales + probe | decides per-symbol-subscribe vs firehose design (10 §4; 13 §7.3) | client (if stream-all chosen), purchase |
| U-2 | "Is a TLS endpoint (`wss://` for WebSockets, `https://` for REST) available on request? All published samples show plaintext `ws://` — our API key would travel in cleartext." | sales | credential security; operator acceptance gate (13 §7.4) | purchase, ops |
| U-3 | "What is the INR pricing for the WebSocket realtime + historical API for an individual personal-use subscriber (state exchanges/symbol counts), what is the free-trial DURATION, and is the Fundamental Data API (bhavcopy) bundled or separate?" | sales | no API price is published anywhere; all INR figures found are desktop products | purchase |
| U-4 | "Is the FIX API true exchange tick-by-tick, or the same 1-second L1 feed over FIX transport? Does ANY of your API products carry order-book depth (5-level)?" | sales | the only TBT/depth hint in the product line; WS/REST are verified L1-only | purchase (none for WS client) |
| U-5 | "Does the personal-use API entitlement cover NSE NON-DISPLAY usage (automated/algorithmic order generation by the single subscriber)? Please answer in writing." | sales (in writing) | NSE non-display normally attracts separate exchange fees; unstated for GDF's retail tier — legal/compliance gate for any live-trading use | purchase |
| U-6 | Bar-stamp convention: subscribe `SubscribeSnapshot(Minute,1)` live and compare one push's wall-clock arrival time vs its `LastTradeTime` label — bar-OPEN or bar-CLOSE stamp? (Docs snippet says start-of-period; treat as Assumed until probed — 04 §2.) | probe | candle timestamp keys; off-by-one-minute corruption risk in any store | client (candle path) |
| U-7 | Is GetHistory TICK data exactly the archived 1-second realtime stream (1 row/sec max), or finer/coarser? Do quote-only rows (TradedQty=0) appear interleaved? | probe | replay/backfill semantics; cross-verify design | client (history path) |
| U-8 | Is history available for EXPIRED derivative contracts (via `onlyActive:"false"` identifiers)? For how long after expiry? | both | expiry-day analysis + SEBI-reconstruction workflows | none |
| U-9 | Actual per-key limits on the plan quoted to us: run `GetLimitation` on the trial key (AllowedInstruments per exchange, AllowedCallsPerHour/Month, MaxTicks/MaxIntraday/MaxEOD, AllowedFunctions, DataDelay). Also: what symbol caps do commercial WS plans typically carry? | probe (+sales for tiers) | 3-layer depth model (08 §3) — only GetLimitation is authoritative | purchase, ops |
| U-10 | "Please share the complete list of plain-text diagnostic messages the server can send (the …/diagnostic-api-responses/ page list), including the exact expired-key and invalid-key wordings, and the `AuthenticateResult` failure `Message` values." | sales (or re-crawl when network allows) + probe | error-classification table for the client's diagnostic channel (11 §3) | client (error handling polish; not the happy path) |
| U-11 | What is the exact wire envelope of "Access Denied. Key already in use by other session." — plain text or JSON, which MessageType, delivered to the old or the new session or both? | probe (two connects with one trial key) | duplicate-connect detection/alerting (new-connection-wins hazard, 02 §4) | client (session guard) |
| U-12 | Echo heartbeat: exact payload shape (any fields beyond MessageType?), cadence (the "every few seconds" number), and whether the server ALSO sends WS-protocol ping frames | probe | feed-stall watchdog threshold calibration | client (watchdog tuning) |
| U-13 | What arrives when a request is sent for a function/symbol the key is NOT entitled to (SubscribeRealtime with IsEnabled:false; symbol over the cap beyond "Reached instrument limitation")? Frame shape + channel? | probe | fail-loud handling of permission denials | client (error handling) |
| U-14 | Does Unsubscribe produce any acknowledgment, or silence? Is there ANY subscribe-ACK besides the first data frame? | probe | subscription-state bookkeeping across reconnects | client (nice-to-have) |
| U-15 | Pre-open window semantics: during `PreOpen:true`, what do Open/High/Low/volume fields carry (indicative equilibrium vs zeros)? And for NSE_IDX index ticks, are volume/OI/bid/ask zeroed? | probe | session-gating + index-tick validators | client (validator) |
| U-16 | Request-key case tolerance: does the server accept `unsubscribe`/`Unsubscribe`, `periodicity`/`Periodicity`, `accesskey`/`accessKey`, `optionType`/`OptionType` interchangeably (the official artifacts drift across all of these)? | probe | pick ONE canonical casing safely | client (cosmetic if tolerant) |
| U-17 | Under burst load, does EVERY subscribed symbol truly receive an update EVERY second (or silent degradation)? Measure the live `ServerTime − LastTradeTime` lag distribution across the watch set | probe (full trading day) | the core quality claim; scoreboard lag columns | purchase |
| U-18 | "Please confirm the scheduled maintenance windows (we have 02:00–02:30 and 08:00–08:45 IST from your FAQ). Is the WS feed down/degraded during them, and are instrument/reference pulls affected?" | sales + probe | collides with tickvault's 08:30–08:45 boot window (02 §8) | ops |
| U-19 | Real-world reliability: disconnect frequency, session-boundary behavior, recovery time — no independent measurements exist anywhere; run a multi-day trial capture | probe | reliability is entirely unassessed (12 §4) | purchase |
| U-20 | Wire shapes of the two server-side-only capability names in LimitationResult: `GetExchangeSnapshotAfterMarket` and `GetLastQuoteOptionChainWithGreeks` (alias of GetLastQuoteOptionGreeksChain?) | sales + probe | complete the function catalog | none |
| U-21 | Confirm `TokenNumber` == the exchange's own numeric token (NSE/BSE/MCX token space) and its churn behavior on re-listings | both | identity Option A soundness (13 §4) | client (identity) |
| U-22 | REST path for GetHistoryAfterMarket (no SDK class exists); and is the SDK's GetHistoryGreeks-posts-to-`GetHistory/` path a bug or server routing? Why is the REST GetHistoryGreeks response XML with DD-MM-YYYY timestamps? | probe | REST history integration | client (REST path only) |
| U-23 | Confirm epoch-UTC timestamp convention on LIVE ticks for EVERY entitled exchange (verified by doc arithmetic for NSE-family only): compare `LastTradeTime` vs wall clock on NSE, NSE_IDX, NFO, MCX, BSE | probe | a hidden per-exchange IST-shift would corrupt every time key | client (time keys) |
| U-24 | Delayed-API details (per-exchange delay values available, key model) · BSE_DEBT/NCDEX API coverage · .NET/COM docs (uncaptured) · the 2024 Python all-functions PDF (unfetched) · network-partition disconnect behavior (half-open socket detection beyond Echo silence) · **`GetHolidays`** — a top-level section in the CURRENT 2026 docs tree (LIVE-DOC nav, operator paste 2026-07-13) absent from every mined SDK/sample: wire shape uncaptured | both | completeness of secondary surfaces | none |

## Priority ordering

- **Before purchase:** U-1, U-2, U-3, U-4, U-5, U-9(tiers), U-17, U-19.
- **First hour of a trial key:** U-6, U-7, U-9(GetLimitation), U-11, U-12, U-13, U-14, U-15, U-16, U-23.
- **Before the first code PR:** U-2 (TLS verdict), U-6, U-16, U-21, U-23 + the operator decisions in 13 §7.
