# GDF 99 — CONSOLIDATED UNKNOWNS (the single open-questions file)

> **Fetched:** 2026-07-13 · **Evidence tier:** n/a — this file enumerates what the public evidence genuinely does NOT state. Every `**Unknown**`/`U-n` flag in files 01–13 resolves here. Union of all research passes, deduped.
> **How to use:** rows marked *sales* → email/call GDF sales (sales@globaldatafeeds.in, +91-77210 80002) with the EXACT question wording; rows marked *probe* → answerable in the first hour of a trial key; *both* → ask AND verify.
> **Blocking legend:** `client` = blocks building the Rust client · `purchase` = blocks the buy/no-buy decision · `ops` = blocks operational planning · `none` = nice-to-know.
> **2026-07-13:** the browser-paste capture queue for closing the doc-derived unknowns below lives in [`00-PASTE-LIST.md`](./00-PASTE-LIST.md) (WAF re-verified total that date; operator paste is the only route).
> **2026-07-14 RUNNER-DOC reconciliation:** the 215-page runner crawl (branch `docs-fetch/gdf-2026-07-14` @ `7d354a8d`) closed or advanced the doc-answerable rows below — **CLOSED: U-6, U-10, U-13, U-18 (+ U-24's GetHolidays item) · PARTIAL/ADVANCED: U-1, U-3, U-7, U-8, U-9, U-11, U-12, U-21, U-22, U-23 · STILL OPEN: U-2, U-4, U-5, U-14, U-15, U-16 (narrowed), U-17, U-19, U-20.** Rows are never renumbered; closed rows are kept with their evidence.

| # | Question (exact wording to ask / probe) | How | Why it matters | Blocking |
|---|---|---|---|---|
| U-1 | **PARTIALLY CLOSED 2026-07-13 (LIVE-DOC operator paste of the stream-all-symbols page — see 10):** StreamAllSymbols wire format (Batch envelope, abbreviated keys, `T:"RT"` rows) + entitlement model (auth-only; firehose auto-starts for all exchanges enabled on the key) + scope (entitled exchanges, incl. untraded symbols) are now Verified. **FURTHER CLOSED 2026-07-14 (RUNNER-DOC):** the StreamAllSnapshots response format is captured — `RealtimeSnapshotCollection` PascalCase envelope with key-level Exchange+Periodicity+Period (10 §2c, …/streaming_api/streamallsnapshots/); StreamAllSymbols re-fetched identical + full 17-row sample (STT−LTT ∈ {0,1}); the two streaming functions are sold "available individually" (api-pricing). **STILL OPEN — ask/probe:** "Is StreamAllSymbols/StreamAllSnapshots served on a dedicated endpoint/port or the standard WS endpoint? What is the Batch emission cadence and max rows per Batch frame? What is the expected bandwidth for a full NFO universe? Can request-response functions (GetLimitation, GetInstruments) be sent on the same streaming connection? What does enabling an exchange for streaming cost (incl. the Periodicity/Period axis for snapshots)?" New probe sub-residuals: snapshot collection cadence, per-bar `TradedQty` semantics | sales + probe | decides per-symbol-subscribe vs firehose design (10 §4; 13 §7.3) | client (if stream-all chosen), purchase |
| U-2 | "Is a TLS endpoint (`wss://` for WebSockets, `https://` for REST) available on request? All published samples show plaintext `ws://` — our API key would travel in cleartext." — *2026-07-14 nuance (still open): zero `wss://` in the full 215-page corpus; but the FD docs-portal test server IS https (`https://test.lisuns.com:4532`) and one REST doc example shows `https://endpoint:port` — TLS exists on the vendor's REST surfaces; the market-data WS question stands* | sales | credential security; operator acceptance gate (13 §7.4) | purchase, ops |
| U-3 | "What is the INR pricing for the WebSocket realtime + historical API for an individual personal-use subscriber (state exchanges/symbol counts), what is the free-trial DURATION, and is the Fundamental Data API (bhavcopy) bundled or separate?" — **PARTIAL 2026-07-14 (RUNNER-DOC):** pricing confirmed UNPUBLISHED/quote-only (negative closure — zero INR figures on api-pricing); the bundle axes are now documented (5-plan compare matrix + 13 named data packages; streaming sold individually); trial exists on every API family but its DURATION is still unpublished — the sales route stands for figures + trial length | sales | no API price is published anywhere; all INR figures found are desktop products (₹233/mo extra-computer is now first-party — nimbledatapro) | purchase |
| U-4 | "Is the FIX API true exchange tick-by-tick, or the same 1-second L1 feed over FIX transport? Does ANY of your API products carry order-book depth (5-level)?" | sales | the only TBT/depth hint in the product line; WS/REST are verified L1-only (re-confirmed corpus-wide 2026-07-14 — zero FIX documentation exists) | purchase (none for WS client) |
| U-5 | "Does the personal-use API entitlement cover NSE NON-DISPLAY usage (automated/algorithmic order generation by the single subscriber)? Please answer in writing." — *2026-07-14: zero "non-display" hits in the 215-page corpus; the documented tension sharpened (algo/HFT marketed on /apis/+FAQ vs single-subscriber charting/analysis T&C) — the written-answer requirement stands* | sales (in writing) | NSE non-display normally attracts separate exchange fees; unstated for GDF's retail tier — legal/compliance gate for any live-trading use | purchase |
| U-6 | **CLOSED 2026-07-14 (RUNNER-DOC) — bars are OPEN-STAMPED (bucket-start).** Doc-stated byte-verbatim on TWO pages: WS GetSnapshot ("In both cases, timestamp of the returned data will be start timestamp of the period. So when data between 9:15:00 to 9:20:00 is returned, it will have timestamp of 9:15:00" — …/websockets-api-documentation/function-getsnapshot-2/) and GetSnapshot (Delayed) (same rule, 14:25:00 example); the SubscribeSnapshot (Delayed) worked example labels the delivered bar with the completed minute's START. Residual: a first-live-session sanity check is cheap insurance, not a required probe (04 §2) | ~~probe~~ closed | candle timestamp keys; off-by-one-minute corruption risk in any store | ~~client~~ closed |
| U-7 | Is GetHistory TICK data exactly the archived 1-second realtime stream (1 row/sec max), or finer/coarser? Do quote-only rows (TradedQty=0) appear interleaved? — **PARTIAL 2026-07-14 (RUNNER-DOC):** the retention table labels tick history "Tick \| Every 1 Sec \| 1 Calendar Week" (…/introduction/type-of-data-available/) → officially a 1-second-granularity archive, never sub-second trades; official PDF TICK sample rows are irregularly spaced (2s/13s gaps) → rows only for ACTIVE seconds. Residual probe: byte-parity with the live stream + quote-only-row interleaving | probe | replay/backfill semantics; cross-verify design | client (history path) |
| U-8 | Is history available for EXPIRED derivative contracts (via `onlyActive:"false"` identifiers)? For how long after expiry? — **PARTIAL 2026-07-14 (RUNNER-DOC):** the MASTER side is doc-tier (`OnlyActive:false` returns "all (active + expired) instruments" — getinstruments page) and the contractwise history depth is bounded + short (futures 3 cal months / options 1 cal month contractwise; continuous since 2010 — type-of-data-available). Residual: expired-contract HISTORY availability itself is still undocumented | both | expiry-day analysis + SEBI-reconstruction workflows | none |
| U-9 | Actual per-key limits on the plan quoted to us: run `GetLimitation` on the trial key (AllowedInstruments per exchange, AllowedCallsPerHour/Month, MaxTicks/MaxIntraday/MaxEOD, AllowedFunctions, DataDelay). Also: what symbol caps do commercial WS plans typically carry? — **PARTIAL 2026-07-14 (RUNNER-DOC):** the LimitationResult SCHEMA + official doc-sample values (MaxIntraday 44 / MaxEOD 100000 / MaxTicks 5) are now doc-tier (…/websockets-api-documentation/function-getlimitation-2/); OUR key's live values still need the trial-key probe | probe (+sales for tiers) | 3-layer depth model (08 §3) — only GetLimitation is authoritative | purchase, ops |
| U-10 | **CLOSED 2026-07-14 (RUNNER-DOC) — the complete diagnostic list is captured verbatim:** 22 rows incl. `Key Expired.`, `Access Denied. Key Invalid.`, `IP address not allowed.`, `Function not enabled.`, `Bandwidth/Calls per hour are limited.`, monthly-limit + date-range messages (…/documentation-support/diagnostic-api-responses/ + FD-portal extras — 11 §3.1). Residual probe: whether a JSON `AuthenticateResult{Complete:false}` carries these strings in `Message`, or auth failures arrive text/plain only | ~~sales~~ closed (+ residual probe) | error-classification table for the client's diagnostic channel (11 §3) | ~~client~~ closed |
| U-11 | What is the exact wire envelope of "Access Denied. Key already in use by other session." — plain text or JSON, which MessageType, delivered to the old or the new session or both? — **PARTIAL 2026-07-14 (RUNNER-DOC):** envelope proven TEXT/PLAIN (the literal sits in the text/plain diagnostic table); doc wording "previous sessions are invalidated with this message" implies the OLD session receives it. Residual probe: old-vs-new delivery confirmation (two connects with one trial key) | probe | duplicate-connect detection/alerting (new-connection-wins hazard, 02 §4) | client (session guard) |
| U-12 | Echo heartbeat: exact payload shape (any fields beyond MessageType?), cadence (the "every few seconds" number), and whether the server ALSO sends WS-protocol ping frames — **PARTIAL 2026-07-14 (RUNNER-DOC):** payload confirmed exactly `{"MessageType":"Echo"}` (no other fields); cadence ~2–3 s OBSERVED in the two timestamped official transcripts (websockets-python/-nodejs sample pages). Residual probe: formal cadence + the WS-protocol-ping question | probe | feed-stall watchdog threshold calibration | client (watchdog tuning) |
| U-13 | **CLOSED 2026-07-14 (RUNNER-DOC):** a request for an unentitled function is refused with the `Function not enabled.` text/plain diagnostic (…/documentation-support/diagnostic-api-responses/); symbol-over-cap = "Reached instrument limitation." (FAQ verbatim) | ~~probe~~ closed | fail-loud handling of permission denials | ~~client~~ closed |
| U-14 | Does Unsubscribe produce any acknowledgment, or silence? Is there ANY subscribe-ACK besides the first data frame? — *2026-07-14: verified ABSENCE — no unsubscribe ACK documented anywhere in the 215-page corpus; official samples send it fire-and-forget; probe stands* | probe | subscription-state bookkeeping across reconnects | client (nice-to-have) |
| U-15 | Pre-open window semantics: during `PreOpen:true`, what do Open/High/Low/volume fields carry (indicative equilibrium vs zeros)? And for NSE_IDX index ticks, are volume/OI/bid/ask zeroed? — *2026-07-14: the glossary adds only `PREOPEN \| If Preopen is active` — no population semantics anywhere; probe stands* | probe | session-gating + index-tick validators | client (validator) |
| U-16 | Request-key case tolerance: does the server accept `unsubscribe`/`Unsubscribe`, `periodicity`/`Periodicity`, `accesskey`/`accessKey`, `optionType`/`OptionType` interchangeably (the official artifacts drift across all of these)? — *2026-07-14 (narrowed, still open): the DOCS THEMSELVES mix casings (doc `Unsubscribe` vs SDK `"unsubscribe"`; `accesskey`/`accessKey`; request `isShortIdentifier:"TRUE"` echoed back as `IsShortIdentifier:true` — server appears case/type-normalizing) — but no explicit vendor statement exists; probe stands* | probe | pick ONE canonical casing safely | client (cosmetic if tolerant) |
| U-17 | Under burst load, does EVERY subscribed symbol truly receive an update EVERY second (or silent degradation)? Measure the live `ServerTime − LastTradeTime` lag distribution across the watch set | probe (full trading day) | the core quality claim; scoreboard lag columns | purchase |
| U-18 | **CLOSED at doc level 2026-07-14 (RUNNER-DOC, FAQ verbatim):** maintenance windows = **02:00–02:30 + 08:00–08:30 IST**, with the BOD process "some times … till 08:45:00 AM"; symptom "Connection refused"; vendor advice "please connect after 8:45AM" (…/faqs/faqs/). Collision with tickvault's 08:30 IST boot stands — GDF connect must defer to ≥08:45. Residual probe: does an ALREADY-CONNECTED WS session degrade during the windows, or are only new connects refused | ~~sales~~ closed (+ residual probe) | collides with tickvault's 08:30–08:45 boot window (02 §8) | ops |
| U-19 | Real-world reliability: disconnect frequency, session-boundary behavior, recovery time — no independent measurements exist anywhere; run a multi-day trial capture | probe | reliability is entirely unassessed (12 §4) | purchase |
| U-20 | Wire shapes of the two server-side-only capability names in LimitationResult: `GetExchangeSnapshotAfterMarket` and `GetLastQuoteOptionChainWithGreeks` (alias of GetLastQuoteOptionGreeksChain?) — *2026-07-14: verified ABSENCE — zero mentions of either name in the 215-page corpus; the doc GetLimitation sample's AllowedFunctions lists only 9 functions and includes neither; probe stands* | sales + probe | complete the function catalog | none |
| U-21 | Confirm `TokenNumber` == the exchange's own numeric token (NSE/BSE/MCX token space) and its churn behavior on re-listings — **PARTIAL 2026-07-14 (RUNNER-DOC):** the field's existence + `detailedInfo` gating are doc-tier, but the doc glosses it only as "Token number of Symbol" (…/websockets-api-documentation/getinstruments/). "== exchange token" stays Assumed; churn = live probe | both | identity Option A soundness (13 §4) | client (identity) |
| U-22 | REST path for GetHistoryAfterMarket (no SDK class exists); and is the SDK's GetHistoryGreeks-posts-to-`GetHistory/` path a bug or server routing? Why is the REST GetHistoryGreeks response XML with DD-MM-YYYY timestamps? — **PARTIAL both halves 2026-07-14 (RUNNER-DOC):** (a) a REST GetHistoryAfterMarket doc page EXISTS (…/rest-api-documentation/gethistoryaftermarket-2/, params = REST GetHistory's; the `GetHistoryAfterMarket/` path follows the universal scheme but no verbatim URL is shown); (b) a dedicated REST GetHistoryGreeks page exists in the live nav (unfetched this run) — the SDK's `GetHistory/` path is re-assessed as an SDK bug. Residual: the XML-vs-JSON response-format question — live-verify | probe | REST history integration | client (REST path only) |
| U-23 | Confirm epoch-UTC timestamp convention on LIVE ticks for EVERY entitled exchange (verified by doc arithmetic for NSE-family only): compare `LastTradeTime` vs wall clock on NSE, NSE_IDX, NFO, MCX, BSE — **STRENGTHENED 2026-07-14 (effectively closed at doc level for WS + REST-REQUEST):** WS + REST-request From/To = plain-UTC epoch SECONDS with doc worked examples that are exact 00:00:00 UTC instants; the DAY 18:00-IST stamp re-confirmed (PDF, ARITH). NEW residual: the REST RESPONSE unit — doc PROSE says seconds, the same pages' samples show 13-digit ms — parse both defensively + live-verify; REST XML adds a HUMAN-datetime dialect | probe | a hidden per-exchange IST-shift would corrupt every time key | client (time keys) |
| U-24 | Delayed-API details (per-exchange delay values available, key model) · BSE_DEBT/NCDEX API coverage · .NET/COM docs (uncaptured) · ~~the 2024 Python all-functions PDF (unfetched)~~ · network-partition disconnect behavior (half-open socket detection beyond Echo silence) · ~~**`GetHolidays`** … wire shape uncaptured~~ — **PARTIAL 2026-07-14 (RUNNER-DOC):** the **GetHolidays item is CLOSED** (both WS + REST fully documented with verbatim request/response — 06 §6; rows enumerate 10 exchange codes incl. `NCX`); the AllFunctions PDF is FETCHED (25 pages, in the corpus); the delayed-twin pages are FETCHED (same wire MessageTypes, delay = per-key backend config); NCDEX negative strengthened (marketing-only, absent from every API exchange list — but present in holiday-calendar rows). Residual: .NET/COM docs, network-partition behavior, BSE_DEBT coverage detail | both | completeness of secondary surfaces | none |

## Priority ordering

- **Before purchase:** U-1, U-2, U-3, U-4, U-5, U-9(tiers), U-17, U-19.
- **First hour of a trial key:** U-6, U-7, U-9(GetLimitation), U-11, U-12, U-13, U-14, U-15, U-16, U-23.
- **Before the first code PR:** U-2 (TLS verdict), U-6, U-16, U-21, U-23 + the operator decisions in 13 §7.

> **2026-07-14 note on the lists above (kept as-written for history):** U-6 and U-13 are now CLOSED at doc level and U-10/U-18 likewise — their trial-hour items degrade to cheap sanity checks, not blockers; the remaining genuine first-hour probes are U-7, U-9(GetLimitation), U-11(delivery side), U-12(cadence), U-14, U-15, U-16, U-23(REST-response unit). The before-purchase list is unchanged except U-3's pricing-unpublished half is now a confirmed negative (ask sales for figures + trial duration directly).
