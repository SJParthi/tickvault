# Zerodha 99 — CONSOLIDATED UNKNOWNS (the single open-questions file)

> **Source:** n/a — this file enumerates what the assembled Kite Connect v3 evidence genuinely does NOT establish, consolidated + deduped from the OPEN QUESTIONS appendices of all 15 pack files after the 2026-07-13 adversarial review passes.
> **Fetched/Verified:** 2026-07-13. **Provenance, stated honestly:** every `kite.trade` / `zerodha.com` / `support.zerodha.com` page — and web.archive.org and every relay — was **403-blocked at the sandbox egress proxy**, so NOTHING in this pack is byte-verbatim from a live docs page; the strongest tiers are official SDK source + official mocks. Consequently this pack has an unusually LARGE operator-paste surface — see the dedicated URL list at the bottom (the gdf-ref operator-paste pattern).
> **Evidence tiers:** n/a — every row is by definition Unknown or conflicting-SEARCH pending confirmation. Inline flags in the pack files (`Z-ORD-n`, `Z-PB-n`, `Z-MF-n`, `Z-AL-n`, `U-P-n`, and prose "→ OPEN QUESTIONS") resolve to rows here.
> **How to use:** rows marked *paste* → the operator opens the named URL in a normal browser and pastes the named section verbatim into the pack; *probe* → answerable only in a first live session with a real api_key; *support/policy* → ask Zerodha/Kite forum; *both* → paste AND probe.
> **Blocking legend:** `pipeline` = blocks any future Kite feed/consumer design · `capacity` = blocks capacity/budget math · `ops` = blocks operational planning · `none` = completeness/nice-to-know.

| # | Question | How | Why it matters | Blocking |
|---|---|---|---|---|
| U-1 | **The live docs TOC/sidebar** — paste the complete section list of `https://kite.trade/docs/connect/v3/` and DIFF it against this pack's file index. The pack's coverage matrix is a RECONSTRUCTION (SDK routes + mocks + training-tier section names); the Alerts family (file 15) was invisible to pykiteconnect-only enumeration — proof a 16th undiscovered section could exist. | paste | the only way to prove the pack covers the whole official surface | pipeline |
| U-2 | **Access-token daily-flush wording, verbatim** — paste the "Authentication and token exchange" section of `/docs/connect/v3/user/`. The ~07:30 AM IST flush + "valid one whole day" + post-07:35 validity are forum-snippet SEARCH only; a Kite token-minter/feed schedule would be built on this number. | paste | THE load-bearing token-lifecycle fact (files 01 §7) | pipeline |
| U-3 | **`request_token` TTL + single-use semantics** — how long is a request_token exchangeable; does a second exchange fail? Same page. | paste | login-automation retry design (01) | pipeline |
| U-4 | **One-active-access-token semantics** — does a second `generate_session` invalidate token #1 (forum says yes)? Paste the official line if it exists; else probe: mint twice, test token #1. The dual-instance-lock design (Dhan burned this project before — `dual-instance-lock-2026-07-04.md`) depends on it. | both | token safety across two processes (01) | pipeline |
| U-5 | **Daily manual-login mandate + TOTP-automated browser login ToS status** — the exchange-mandated daily interactive login has no API bypass for individuals; is TOTP-automated browser login tolerated or a ToS breach? | support/policy | whether an unattended Kite feed is even lawful-by-terms (01) | pipeline |
| U-6 | **THE historical caps table, byte-verbatim** — paste ALL of `/docs/connect/v3/historical/`: the per-interval max-range table (minute 60d / 3-10min 100d / 15-30min 200d / 60min 400d / day 2000d — SEARCH-tier ×3 agreeing extractions, never byte-verbatim) AND the exact interval enum (`minute…60minute, day`; no `2minute`). The single most load-bearing numeric table of the pack. | paste | chunking-loop + backfill design (08 §3–§4) | pipeline |
| U-7 | **Just-closed-minute freshness + forming-candle behaviour** — undocumented everywhere (the Dhan SPOT1M-01 / Groww U-2 twin). Probe per 08 §10: at minute close T+1s request the T−1 candle; ladder re-polls ~0.7/1.5/3/6s; record first-appearance latency + whether an in-progress candle appears/mutates. | probe | gates any Kite per-minute REST leg (the `spot_1m_rest` pattern) | pipeline |
| U-8 | **`to` boundary inclusivity on historical** — is the candle at exactly `to` included? (Dhan's NON-inclusive `toDate` already caused off-by-one bugs.) Paste + probe with a known candle time. | both | off-by-one-candle correctness (08) | pipeline |
| U-9 | **Index + BFO historical coverage** — do index tokens (NIFTY 50 = 256265, SENSEX = 265) serve historical candles, and do NFO/BFO contracts (esp. SENSEX weekly options / futures) work at the same per-interval caps? Stated NOWHERE in fetched artifacts. | probe | the tickvault SENSEX-futures analog (14 §5) | pipeline |
| U-10 | **Minute-data retention: from ~2015-02-02 vs a "3-year rolling" window** — conflicting forum claims. Paste forum/14149 (retention policy) + probe a 2016 minute range on a liquid stock. | both | backfill depth design (08 §9) | capacity |
| U-11 | **WS limits current wording + enforcement** — paste the limits paragraphs of `/docs/connect/v3/websocket/`: 3000 instruments/connection + 3 connections/api_key (forum staff call the 3-conn cap a SOFT limit with abuse-flagging), and the exact error-frame shape on the 3001st token. | paste (+probe the error shape) | feed-capacity decision (10 §2, 02 §6d) | capacity |
| U-12 | **Rate-limit table, byte-verbatim** — paste the "API rate limits" section of `/docs/connect/v3/exceptions/`: Quote 1/s · Historical 3/s · Orders 10/s · others 10/s, per api_key, no daily cap outside orders — all SEARCH-tier (convergent ×several, never verbatim; the Groww stale-15/s hazard applies). | paste | every capacity computation in the pack (02 §6a) | capacity |
| U-13 | **Orders/minute: 200 or 400?** — recency evidence favors 400 as current (support order-rate-limits article + exceptions-docs extraction), 200 as the superseded FAQ-era figure. Paste both pages. | paste | burst pacing for any order engine (02 §6b) | capacity |
| U-14 | **Orders/day: 3,000 or 5,000, and the pooling scope** — 2026 extractions consistently return 5,000 as the live-FAQ figure (3,000 tied to a July-2023 change); AND the scope wording conflicts (forum: account-level across ALL platforms; FAQ: "a single user/API key"). Paste the FAQ + forum/12851. | paste | day-budget + multi-app design (02 §6b) | capacity |
| U-15 | **Quote-family per-call caps** — 500 (`/quote`) / 1000 (`/quote/ohlc`, `/quote/ltp`); one stray "250" extraction failed a targeted disconfirmation pass but only a live read kills it. Paste `/docs/connect/v3/market-quotes/`. Also: is one 500-instrument call billed as 1 request against the 1/s limit (Assumed 1)? | paste (+probe billing) | chain-composition + snapshot batching math (07 §3, 14) | capacity |
| U-16 | **429 wire shape** — status headers (any `Retry-After`?) + raw JSON body (`error_type` = NetworkException per two forum-title corroborations — Assumed). Capture the first live 429. | probe | backoff-ladder design, the DH-904 analog (02 §6e) | ops |
| U-17 | **Over-cap + invalid-instrument behaviour on the quote family** — >500/>1000 instruments (error? truncation?), and an unknown instrument in `i=` (omitted from `data` vs whole-request error). | probe | fail-loud batching + coverage accounting (07) | ops |
| U-18 | **`ohlc.close` = previous-day close + timestamp timezone** — paste the `/quote` field-description table (the Dhan Ticket #5525125 bug class; IST is Assumed on all naive timestamps). | paste | any prev-close consumer + candle bucketing (07 §4) | pipeline |
| U-19 | **WS default mode after a bare `subscribe`** — py bookkeeps `quote`, go assumes nothing. Paste the modes paragraph of the websocket page. | paste | native client that subscribes without a mode message (10 §4) | pipeline |
| U-20 | **Index-packet bytes 24–27 official label** — unread by both SDKs; presumed "price change". Paste the index-packet table (`#quote-packet-structure`). | paste | native index-packet parsing — field must be named, not guessed (10 §9) | none |
| U-21 | **Heartbeat exact cadence + dead-socket threshold** — "every couple seconds" is SEARCH wording; a native stall watchdog needs the real number (or a first-session measurement). | both | stall-watchdog threshold (10 §3/§14) | pipeline |
| U-22 | **SENSEX/BFO end-to-end delivery** — (a) SENSEX weekly options present in the BFO dump? (b) `GET /quote?i=BFO:…` returns data? (c) token 265 + a BFO token stream WS ticks? (d) strike counts per index expiry (replaces the Assumed ~200-legs/chain magnitude). | probe | the §36-FUTIDX-on-SENSEX analog cannot be planned on implied coverage (14 §5) | pipeline |
| U-23 | **BCD (BSE currency) divisor 10,000** — SDK-sourced only (the docs snippet surfaced only the CDS 1e7 divisor). Paste the price-conversion paragraph. | paste | exact decode on BCD tokens, if ever subscribed (10 §7) | none |
| U-24 | **Other WS text-frame `type` values** (broadcast/admin "message"?) — both SDKs silently ignore unknown types. Paste the "Postbacks and non-binary updates" section. | paste | not dropping operator-relevant notices (10 §13) | ops |
| U-25 | **Instruments dump: auth requirement + publish time** — does `GET /instruments` need the Authorization header (forum suggests direct download); at what IST time is the daily dump regenerated ("~08:30 AM" is forum-attributed, unpinned)? Probe with `curl -sI` across a trading-day boundary too. | both | boot ordering (pre-auth master fetch?) + fetch scheduling (09 §1/§6) | ops |
| U-26 | **Segment/instrument_type full enums + `name` population on live F&O rows** — mock attests only `NSE/BSE/NFO-OPT` + `EQ/CE/PE`, and option rows have EMPTY `name`. Paste the CSV-columns table + one live NFO/BFO sample row. | both | fail-closed CSV validation + underlying-filter correctness (09 §3, 14 §2) | pipeline |
| U-27 | **Token-reuse wording verbatim** — the docs reportedly recommend keying on `exchange:tradingsymbol` because instrument_tokens are REUSED for different derivatives after expiry (the I-P1-11 analog). Paste the sentence. Also: is the `×256+segment` token relationship documented as a stable contract or SDK detail? | paste | leg-identity key design (14 §2, 09 §5) | pipeline |
| U-28 | **Option tradingsymbol grammar** — official symbology for weekly vs monthly encodings (`NIFTY24D0524000CE` vs `BANKNIFTY18JAN23500CE`). Paste from docs/support if documented. | paste | prefix-parsing robustness (14) | pipeline |
| U-29 | **Index rows in `/instruments/NSE` vs full dump only + current dump size/row count** — measure on first live fetch. | probe | endpoint choice for a spot-indices consumer + download budget (09 §4/§7) | ops |
| U-30 | **Modify-order `quantity`: total or remaining?** — the exact Dhan foot-gun class (Dhan documents TOTAL). Paste the modify section of `/docs/connect/v3/orders/`. | paste | getting it wrong resizes live orders (03 Z-ORD-8) | ops |
| U-31 | **Market-protection mandatory-ness + default band** — is `market_protection` required for MARKET/SL-M in F&O, is `0` rejected, what band does `-1` apply? SEARCH-tier today. | paste | MARKET orders fail-closed if mishandled (03 Z-ORD-7) | ops |
| U-32 | **Exhaustive order interim-state list + AMO windows** — `TRIGGER PENDING` / `AMO REQ RECEIVED` / `CANCEL PENDING` literals + AMO queue hours per exchange. Paste the order-status + AMO paragraphs. | paste | OMS state-machine mapping (03 Z-ORD-2) | ops |
| U-33 | **Orders misc bundle** — CO variety current status + which param drives the CO stop leg (Z-ORD-1); `disclosed_quantity` min-% (Z-ORD-3); `algo_id` semantics (Z-ORD-4); product×segment matrix incl. MTF (Z-ORD-5); iceberg floors + cancel-the-leg semantics (Z-ORD-6). All on the orders page + support articles. | paste | order-validation completeness (03) | ops |
| U-34 | **GTT bundle** — trigger-distance validation rules; allowed leg `order_type`s (go hard-codes LIMIT); `expires_at` client-settable? (one mock is exactly +1y, one is not); current max active GTTs (500 vs older 250/100 — conflicting); `disabled`/`cancelled`/`deleted` semantics; `parent_trigger` + populated `meta` shape; GTT rate bucket; does a GTT fire emit a postback/WS event (the result meta `{"gtt":105099}` tag suggests yes)? Paste `/docs/connect/v3/gtt/` + the support validity/cap articles. | paste | SL-management design on GTT (04) | ops |
| U-35 | **Exceptions-page full taxonomy** — is the mock-attested `MarginException` documented; does `HoldingException` exist; permission type wire spelling `PermissionException` (py) vs `PermissionError` (go)? Paste `/docs/connect/v3/exceptions/`. | paste | error-taxonomy port (02 §5, 03 §14) | none |
| U-36 | **Portfolio field semantics bundle** — holdings `price` (always 0 in mocks), `authorisation` object shape, `collateral_type` enum, `short_quantity` doc status (mock/Go drift), summary/compact doc status, `m2m` vs `pnl` exact definitions, pledge surface beyond `collateral_*`, conversion-failure envelope. Paste `/docs/connect/v3/portfolio/`. | paste | P&L reconciliation + parser leniency (05) | ops |
| U-37 | **Funds/margins bundle** — per-field descriptions of `GET /user/margins` (`live_balance` vs `net`, composition of `debits` — mock proves components don't sum); `/margins/orders` documented param table + `mode=compact` doc status; basket top-level `charges` meaning (Go struct drops it; mock aggregate shows `brokerage 40, total 0`); `consider_positions` wire contract; `/charges/orders` input domain (executed-only?); the dead py route `GET /margins/{segment}`. Paste `/docs/connect/v3/margins/` + the user funds anchor. | paste | margin pre-check + cost math (06) | ops |
| U-38 | **Rate family for `/margins/*`, `/charges/orders`, `/gtt/*`, `/alerts`** — no fetched source names their buckets. Paste the limits table (if enumerated) or probe bursts. | both | budget math for pre-trade check loops (06 §7, 04 §8) | capacity |
| U-39 | **Postbacks bundle** — verbatim coverage wording (api_key-scoped HTTP vs user-scoped WS — Z-PB-1); registration mechanics + localhost policy (Z-PB-2); Content-Type/retry/timeout (Z-PB-3); exhaustive trigger matrix incl. per-partial-fill UPDATEs (Z-PB-4); WS order-update payload field delta vs HTTP postback (Z-PB-5); delivery guarantees across reconnects (Z-PB-6); checksum edge cases (null `order_timestamp`? — Z-PB-7). Paste `/docs/connect/v3/postbacks/` + the websocket page's postback section. | paste | OMS event-driven fill tracking + webhook idempotency (11) | ops |
| U-40 | **MF placement liveness** — forum says MF order placement was disabled ~2020; SDKs + mocks still ship it. Paste `/docs/connect/v3/mutual-funds/` (any deprecation banner) + the BUY-by-amount/SELL-by-quantity matrix, SIP `status` modify literals, `instalment_day` legal set (the SEARCH enum 1/5/10/15/20/25 is CONTRADICTED by the official mock's monthly `instalment_day: 30`), `/mf/instruments` serving details. | paste | whether the MF write surface is historical-only (12) | none |
| U-41 | **Alerts docs page verbatim** — attribute enum beyond `LastTradedPrice`, alert caps, evaluation cadence, notification channels, `GetAlerts` filter keys, ATO entitlement + fired-ATO `order_meta` shape (Z-AL-1/2/4). Paste the Alerts section (URL from the U-1 TOC). | paste (+probe one ATO) | the whole file 15 is SDK+mock-derived | none |
| U-42 | **Current pricing liveness** — is ₹500/mo (+ free Personal) the live price today? Prices moved twice in 2025 alone. Paste the support charges article + `https://zerodha.com/products/api/` (U-P1); GST inclusive or +18% (U-P5); app-creation mechanics — is Personal an app TYPE, is the monthly debit now 500 credits (U-P3); bundled-historical scope ("10 years intraday" is single-extraction Assumed — U-P4). | paste | cost modeling for any Kite lane (13) | ops |
| U-43 | **Entitlement failure shape** — exact HTTP status/error a data-less Personal key gets on `GET /quote` or WS connect (the Dhan DH-902/806 analog). | probe | distinguish "unentitled" from "auth-dead" (13 U-P7) | ops |
| U-44 | **"2023-09 free API" hypothesis** — four targeted searches found ZERO 2023 source; every dated source says 2025. Only a dated 2023 article could resurrect it; otherwise dead (U-P2). | paste (if it exists) | timeline hygiene — do NOT cite 2023 anywhere (13 §3) | none |
| U-45 | **Startup/mass-retail free tier** — "free of cost for mass-retail-targeted platforms" is a SINGLE extraction. Paste `https://kite.trade/startups/` in full (U-P6). | paste | would change the commercial calculus entirely if real (13 §7) | none |
| U-46 | **`trigger_range` liveness + real route shape** — the two SDKs ship DIFFERENT paths; the official mock exists; CO-legacy suspect. Paste any docs mention or probe both shapes once. | both | completeness only (07 §10) | none |
| U-47 | **Session-response field semantics** — `public_token`, `enctoken`, `silo`; `login_time` timezone; redirect-URL extra params; login-URL alias `kite.trade/connect/login`. Paste the `/docs/connect/v3/user/` response-attributes table. | paste | parser completeness (01) | none |
| U-48 | **Gzip on REST responses** — "responses may be Gzipped" is SEARCH; confirm transport-level `Content-Encoding` on `/instruments` and whether identity is honored. | probe | hand-rolled client correctness (02 §3c, 09 §2) | none |
| U-49 | **MCXSX (segment 8) liveness** — defunct exchange, still in both SDK segment maps; do tokens ending in 8 occur in the live dump? | probe | cosmetic (10) | none |

## Priority ordering

- **Day-0 operator pastes (before ANY Kite design freezes):** U-1 (TOC diff), U-2/U-3/U-4 (token lifecycle), U-6 (historical caps — the #1 table), U-11 (WS limits), U-12/U-13/U-14/U-15 (rate limits + quote caps), U-18 (prev-close semantics), U-27 (token-reuse identity rule).
- **First hour of the first live session (probes):** U-7 (just-closed-minute ladder), U-8, U-9, U-16 (capture a 429), U-17, U-21 (heartbeat measurement), U-22 (SENSEX/BFO end-to-end), U-25 (dump headers), U-29, U-43, U-48.
- **Before any ORDER-path design (not currently in tickvault scope):** U-30, U-31, U-32, U-33, U-34, U-38, U-39.
- **Nice-to-know / completeness:** U-20, U-23, U-24, U-35, U-36, U-37, U-40, U-41, U-42, U-44, U-45, U-46, U-47, U-49.

---

## 📋 OPERATOR-PASTE URL LIST (ordered — open in a normal browser; the sandbox proxy blocks ALL of these)

> Mirrors the gdf-ref operator-paste pattern (`docs/gdf-ref/README.md` — two GDF pages were operator-pasted and upgraded to LIVE-DOC tier). Each paste upgrades the named SEARCH/Assumed rows to the pack's strongest attainable tier. Paste the raw page text (or a full-page screenshot) back into the session; the assembler will re-tier the affected claims and re-run the reconciliation.

| Order | URL | What to paste | Resolves |
|---|---|---|---|
| 1 | `https://kite.trade/docs/connect/v3/` | the complete left sidebar / TOC section list | U-1 (coverage diff — do this FIRST) |
| 2 | `https://kite.trade/docs/connect/v3/historical/` | the ENTIRE page (caps table + interval list + continuous + oi) | U-6, U-8, U-9(partial), U-10 |
| 3 | `https://kite.trade/docs/connect/v3/user/` | "Authentication and token exchange" + the session-response attribute table + funds section | U-2, U-3, U-4, U-47, part of U-37 |
| 4 | `https://kite.trade/docs/connect/v3/exceptions/` | the exception table + the API-rate-limits section | U-12, U-13, U-14, U-16(shape), U-35, U-38 |
| 5 | `https://kite.trade/docs/connect/v3/market-quotes/` | the per-call caps sentences + the full-quote field table + the "retrieving full market instruments" section | U-15, U-17, U-18, U-25, U-26, U-46 |
| 6 | `https://kite.trade/docs/connect/v3/websocket/` | the limits paragraph + modes paragraph + `#quote-packet-structure` tables (esp. the index packet) + heartbeat + postback-frames section | U-11, U-19, U-20, U-21, U-23, U-24 |
| 7 | `https://kite.trade/docs/connect/v3/orders/` | varieties table, order-status section, AMO, iceberg, market-protection, modify section | U-30, U-31, U-32, U-33 |
| 8 | `https://kite.trade/docs/connect/v3/gtt/` | the whole page | U-34 |
| 9 | `https://kite.trade/docs/connect/v3/portfolio/` | holdings + positions field tables, conversion, holdings-auth | U-36 |
| 10 | `https://kite.trade/docs/connect/v3/margins/` | order/basket/charges param + response tables | U-37, U-38 |
| 11 | `https://kite.trade/docs/connect/v3/postbacks/` | the whole page (short) | U-39 |
| 12 | `https://kite.trade/docs/connect/v3/mutual-funds/` | intro (any deprecation banner) + order/SIP param tables | U-40 |
| 13 | the Alerts docs section (URL visible in the paste-1 TOC; Assumed `https://kite.trade/docs/connect/v3/alerts/`) | the whole page | U-41 |
| 14 | `https://support.zerodha.com/category/trading-and-markets/general-kite/kite-api/articles/kite-connect-api-faqs` | the rate-limit + order-cap Q&As | U-12, U-14 |
| 15 | `https://support.zerodha.com/category/trading-and-markets/alerts-and-nudges/kite-error-messages/articles/order-rate-limits-on-kite` | the whole article | U-13 |
| 16 | `https://support.zerodha.com/category/trading-and-markets/general-kite/kite-api/articles/what-are-the-charges-for-kite-apis` + `https://zerodha.com/products/api/` + `https://kite.trade/` (pricing block) | the current plan/price table | U-42 |
| 17 | `https://kite.trade/forum/discussion/12851/changes-to-per-day-order-limits` + `/14806/…` + `/14868/…` + `/15015/…` | the first posts + publish dates | U-14, pricing timeline byte-verification (13 §2) |
| 18 | `https://developers.kite.trade/apps/new` (after login) | the create-app form text + Billing tab | U-42 (app types + credits) |
| 19 | `https://kite.trade/startups/` | the whole page | U-45 |
| 20 | `https://support.zerodha.com/category/trading-and-markets/kite-features/gtt/articles/what-is-the-validity-of-a-gtt-order` + the GTT-feature article | validity + max-GTT numbers | U-34 |
