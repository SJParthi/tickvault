# Zerodha 99 — CONSOLIDATED UNKNOWNS (the single open-questions file)

> **Source:** n/a — this file enumerates what the assembled Kite Connect v3 evidence genuinely does NOT establish, consolidated + deduped from the OPEN QUESTIONS appendices of all pack files (01–17).
> **Fetched/Verified:** 2026-07-13 (original build) · **2026-07-14 — the GH-runner live crawl landed** (`docs-fetch.yml`, 148 URLs, 147× HTTP 200 — ledger in `00-COVERAGE-MANIFEST.md`) and every paste-class row was adjudicated against the live pages. **Closed rows are RETAINED for audit** (✅ with the answer + citation inline — never deleted); the surviving open surface is almost entirely **live-session probes** (needs a real api_key) plus a handful of support/policy asks.
> **Status legend:** ✅ **CLOSED (2026-07-14 runner fetch)** = answered by the live pages (any residual is cosmetic and noted inline) · ◐ **NARROWED (2026-07-14)** = the live fetch answered part; the surviving sub-question is restated · **OPEN** = untouched by the crawl (probe/support-class).
> **How to use:** *probe* → answerable only in a first live session with a real api_key; *support/policy* → ask Zerodha/Kite forum or support; *runner* → re-dispatch the `docs-fetch.yml` workflow (the old operator-paste path is obsolete — see the history note at the bottom).
> **Blocking legend:** `pipeline` = blocks any future Kite feed/consumer design · `capacity` = blocks capacity/budget math · `ops` = blocks operational planning · `none` = completeness/nice-to-know.
> Inline flags in the pack files (`Z-ORD-n`, `Z-PB-n`, `Z-MF-n`, `Z-AL-n`, `U-P-n`, "→ OPEN QUESTIONS") resolve to rows here.

| # | Status | Question → answer / what survives | How | Blocking |
|---|---|---|---|---|
| U-1 | ✅ CLOSED (2026-07-14 runner fetch) | **The live docs TOC/sidebar.** ANSWER: the live nav enumerates **19 sections** — the pack's derived 15, **minus** `market-data-and-instruments/` which dropped OUT of the live nav (legacy, replaced by `market-quotes/`; still served, so 20 primary docs PAGES counting it), **plus 5 it had never seen** (`apps/`, `basket/`, `publisher/`, `sdks/`, `changelog/` — the predicted "16th undiscovered section" risk realized ×5), now covered by files 16–17. Cite: https://kite.trade/docs/connect/v3/ (fetch-log). Residual = a maintenance duty, not an unknown: re-diff the nav on future runner refreshes. | runner (refresh) | ~~pipeline~~ closed |
| U-2 | ✅ CLOSED (2026-07-14 runner fetch) | **Access-token expiry wording, verbatim.** ANSWER: "Unless this is invalidated using the API, or invalidated by a master-logout from the Kite Web trading terminal, it'll expire at **6 AM on the next day** (regulatory requirement)." — https://kite.trade/docs/connect/v3/user/ . **The prior ~07:30 AM forum figure was WRONG and is refuted** (README Refuted table). Residual: "6 AM" carries no timezone label — Assumed IST. | — | ~~pipeline~~ closed |
| U-3 | ✅ CLOSED (2026-07-14 runner fetch) | **`request_token` TTL + single-use.** ANSWER: "The one-time token … lifetime is only a few minutes and it is meant to be exchanged for an access_token immediately after being obtained." — https://kite.trade/docs/connect/v3/user/ . Residual: exact minute count unstated (cosmetic — design for exchange-immediately). Bonus fact: the redirect also carries a `status` param (apps/ page). | — | ~~pipeline~~ closed |
| U-4 | OPEN | **One-active-access-token semantics** — does a second `generate_session` invalidate token #1 (forum says yes)? The live user/ page is SILENT (checked 2026-07-14), so the paste half is exhausted — probe only: mint twice, test token #1. The dual-instance-lock design (`dual-instance-lock-2026-07-04.md`) depends on it. | probe | pipeline |
| U-5 | OPEN | **Daily manual-login mandate + TOTP-automated browser login ToS status** — no API bypass for individuals (live-confirmed); is TOTP-automated browser login tolerated or a ToS breach? | support/policy | pipeline |
| U-6 | ✅ CLOSED (2026-07-14 runner fetch) | **THE historical caps table.** ANSWER: the live docs page carries **NO caps table at all** (Verified-absence — https://kite.trade/docs/connect/v3/historical/ ); the caps are server-enforced and the full current table (minute 60d · 3/5/10minute 100d · 15/30minute 200d · 60minute 400d · day 2000d) is confirmed via the server's own `interval exceeds max limit: N days` errors quoted on live-fetched forum 14149 (June 2024; corroborated 15111 + 11460). Interval enum live-verbatim: the documented 8 (`minute…60minute, day`) + the server recognizes undocumented `2minute/4minute/hour/2hour/3hour/4hour/week` (the old "no 2minute" claim corrected). Residual (ops hygiene): one cap+1 probe per interval before hard-coding — the strongest artifact is user-quoted error text, not a docs table. | probe (residual) | ~~pipeline~~ closed |
| U-7 | OPEN — **the pack's #1 surviving unknown** | **Just-closed-minute freshness + forming-candle behaviour** — undocumented everywhere incl. the live docs page (the Dhan SPOT1M-01 / Groww twin). Probe per 08 §10: at minute close T+1s request the T−1 candle; ladder re-polls ~0.7/1.5/3/6s; record first-appearance latency + whether an in-progress candle appears/mutates. Gates any Kite per-minute REST leg (the `spot_1m_rest` pattern). | probe | pipeline |
| U-8 | ✅ CLOSED (2026-07-14 runner fetch) | **`to` boundary inclusivity on historical.** ANSWER: INCLUSIVE — the live docs' own examples show the candle whose open time equals `to` IS returned (09:15→09:20 returns 6 candles incl. 09:20) — https://kite.trade/docs/connect/v3/historical/ . Residual: demonstrated at `minute` interval; `day`-boundary Assumed same (one trivial live request confirms). | probe (residual) | ~~pipeline~~ closed |
| U-9 | OPEN | **Index + BFO historical coverage** — do index tokens (NIFTY 50 = 256265, SENSEX = 265 — both now LIVE-DOC as quote examples) serve historical candles, and do NFO/BFO contracts (esp. SENSEX weekly options/futures) work at the same caps? The live historical page is silent on both (confirmed 2026-07-14). | probe | pipeline |
| U-10 | ✅ CLOSED (2026-07-14 runner fetch) | **Minute-data retention conflict.** ANSWER: minute data from **~2015-02-02 / early-2015** (staff-confirmed, forum 8722 Oct-2020 + 14149 June-2024, both live-fetched); **NO rolling window** — the "3-year" phrase was a stale 2018 FAQ; day candles have no fixed start (some NSE stocks to late 1990s). https://kite.trade/forum/discussion/8722/ | — | ~~capacity~~ closed |
| U-11 | ✅ CLOSED (2026-07-14 runner fetch) | **WS limits wording + enforcement.** ANSWER: "up to 3000 instruments per connection" + "up to 3 WebSocket connections per api_key" live-verbatim ( https://kite.trade/docs/connect/v3/websocket/ ); enforcement staff-confirmed Dec 2025 (forum 15708, live-fetched): >3000 → error for the ADDITIONAL tokens only, connection unaffected; the 3-conn cap is a SOFT limit (abuse-flag/suspension). Residual: the exact wire shape of the over-limit error frame (probe). | probe (residual) | ~~capacity~~ closed |
| U-12 | ✅ CLOSED (2026-07-14 runner fetch) | **Rate-limit table, byte-verbatim.** ANSWER: Quote **1 req/s** · Historical candle **3 req/s** · Order placement **10 req/s** · All other endpoints **10 req/s** — the official "API rate limit" table, live-verbatim: https://kite.trade/docs/connect/v3/exceptions/ ; per-api_key + no daily cap outside orders per staff forum 8577/13397 (live-fetched). | — | ~~capacity~~ closed |
| U-13 | ✅ CLOSED (2026-07-14 runner fetch) | **Orders/minute: 200 or 400?** ANSWER: **400/min current** (live exceptions docs note + order-rate-limits-on-kite support article); 200 = the pre-2024 superseded figure (forum 13397's Nov-2023 docs quote). https://support.zerodha.com/category/trading-and-markets/alerts-and-nudges/kite-error-messages/articles/order-rate-limits-on-kite | — | ~~capacity~~ closed |
| U-14 | ✅ CLOSED (2026-07-14 runner fetch) | **Orders/day + pooling scope.** ANSWER: **5,000/day across all segments and varieties**, pooled at **client-account level, not per app/api_key** (FAQ verbatim "per client account, not per app"; forum 12851 staff: account-level, ALL platforms; RMS-blocked requests count; iceberg 5 legs = 5 toward the day cap; modify/cancel/GTT-place excluded; at the cap even exits are blocked). 3,000 was the July-2023 figure. Residuals split out: raise date → U-58; quote/historical-class pooling → U-50. | — | ~~capacity~~ closed |
| U-15 | ✅ CLOSED (2026-07-14 runner fetch) | **Quote-family per-call caps.** ANSWER: **500 (`/quote`) / 1000 (`/quote/ohlc`) / 1000 (`/quote/ltp`)** — live Limits table + per-endpoint prose + changelog corroboration ( https://kite.trade/docs/connect/v3/market-quotes/ ). The stray "250" is EXPLAINED: the stale legacy `market-data-and-instruments/` page (out of nav, © 2015-2020, still HTTP 200 — never cite it). Residual: whether one 500-instrument call bills as 1 request against the 1/s limit (Assumed 1 — probe). | probe (residual) | ~~capacity~~ closed |
| U-16 | OPEN | **429 wire shape** — status headers (any `Retry-After`?) + raw JSON body (`error_type` value). The live pages document the 429 STATUS ("Too many requests to the API (rate limiting)") but never show a 429 BODY. Capture the first live 429 — the DH-904 backoff-ladder analog. | probe | ops |
| U-17 | OPEN (narrowed 2026-07-14) | **Over-cap + malformed-`i` behaviour on the quote family** — >500/>1000 instruments (error? truncation?), and a syntactically malformed `i=` value. NARROWED: a valid-but-no-data key is now LIVE-DOC "silently absent from `data`" (check key existence) — only the over-cap/malformed arms remain. | probe | ops |
| U-18 | ✅ CLOSED (2026-07-14 runner fetch) | **`ohlc.close` semantics + REST timestamp timezone.** ANSWER: `ohlc.close` = "Closing price of the instrument from the **last trading day**" (previous session — the Dhan Ticket #5525125 class closed for Kite) — https://kite.trade/docs/connect/v3/market-quotes/ ; and response-structure/ documents REST timestamps as **IST (UTC+5.5)** strings. Residual: the quote `timestamp`/`last_trade_time` rows themselves say only "exchange timestamp" — the blanket IST rule is one page removed (cosmetic; a live tick cross-check settles it forever). | — | ~~pipeline~~ closed |
| U-19 | OPEN | **WS default mode after a bare `subscribe`** — the live websocket page (read in full 2026-07-14) states NO server default; py bookkeeps `quote`, go assumes nothing. Paste half exhausted — only a live-wire probe (bare subscribe, measure packet length) settles it. | probe | pipeline |
| U-20 | ✅ CLOSED (2026-07-14 runner fetch) | **Index-packet bytes 24–27 official label.** ANSWER: **"Price change"** (int32; the index quote packet ends after it at 28 bytes) — https://kite.trade/docs/connect/v3/websocket/ #index-packet-structure. Both SDKs still leave it unread. Residual (scaling/signedness undemonstrated) → U-57. | — | ~~none~~ closed |
| U-21 | ◐ NARROWED (2026-07-14) | **Heartbeat exact cadence + dead-socket threshold** — live wording confirmed: "1 byte 'heartbeat' every couple seconds", sent **when there is no data to stream**, "can be safely ignored". The exact second count remains unstated — a native stall-watchdog threshold still needs a first-session measurement (py pings 2.5s/drops ~5–7.5s, go reconnects after 5s silence — SDK policies, not server contract). | probe | pipeline |
| U-22 | ◐ NARROWED (2026-07-14) | **SENSEX/BFO end-to-end delivery** — NARROWED: index REST-quote spellings + tokens (`NSE:NIFTY 50` 256265, `BSE:SENSEX` 265) and the WS index packet (28/32B, "indices such as NIFTY 50 and SENSEX") are now LIVE-DOC; BFO is a live user-profile `exchanges` value. STILL OPEN (day-0 probes): (a) SENSEX weekly options present in the BFO dump? (b) `GET /quote?i=BFO:…` returns data (+ the FULL `/quote` index payload shape)? (c) does a BFO token stream WS ticks? (d) strike counts per index expiry (replaces the Assumed ~200-legs magnitude). | probe | pipeline |
| U-23 | ◐ NARROWED (2026-07-14) — now a recorded DISCREPANCY | **BCD (BSE currency) divisor** — the live divisor paragraph is in hand and does NOT mention 1e4: its blanket "currencies ÷ 10,000,000" **contradicts** both SDKs' BCD ÷10,000 special case (both recorded, 10 §7). Only a live BCD tick settles the wire scaling. Matters only if BCD tokens are ever subscribed. | probe | none |
| U-24 | ✅ CLOSED (2026-07-14 runner fetch) | **Other WS text-frame `type` values.** ANSWER: a third documented type **`message`** — "Messages and alerts from the broker" (string `data`) — https://kite.trade/docs/connect/v3/websocket/ #message-types. Both official SDKs silently DROP it (SDK-vs-doc gap flagged); a native client should surface it. | — | ~~ops~~ closed |
| U-25 | ◐ NARROWED (2026-07-14) | **Instruments dump: auth requirement + publish time** — NARROWED: the docs curl on both live pages PRESCRIBES the `Authorization` header (documented contract = authenticated), and the "~08:30 AM" advisory is the docs page's own Warning admonition (attribution closed). STILL OPEN: whether the server also serves a keyless GET (probe), and the actual server-side dump generation/publish time + timezone (diff an early-morning fetch across a trading-day boundary). Matters: pre-auth boot ordering + fetch scheduling. | probe | ops |
| U-26 | ◐ NARROWED (2026-07-14) | **Segment/instrument_type enums + `name` on F&O rows** — CLOSED halves: `instrument_type` is live-enumerated **exactly `EQ, FUT, CE, PE`**; `name` is "Name of the company (for equity instruments)" and EMPTY on the docs' own NFO sample rows — never filter options by `name`. STILL OPEN: the full `segment` enum (live-observed set grew to NSE/BSE/NFO-FUT/NFO-OPT/MCX; `INDICES`/`BFO-…`/`CDS-…` remain Assumed) — enumerate from a live authenticated dump. | probe | pipeline |
| U-27 | ✅ CLOSED (2026-07-14 runner fetch) | **Token-reuse wording + ×256 documentation status.** ANSWER: live Note verbatim — use "a combination of exchange and tradingsymbol as the unique key … Exchanges may reuse instrument tokens for different derivative instruments after each expiry" ( https://kite.trade/docs/connect/v3/market-quotes/ ; the historical page adds "exchanges flush the instrument_token … for every expiry"). And NO — the `×256+segment` relationship is NOT documented narratively anywhere live: it remains an SDK-implementation + ARITH contract; treat tokens as day-scoped opaque values. | — | ~~pipeline~~ closed |
| U-28 | OPEN | **Option tradingsymbol grammar** — weekly vs monthly encodings (`NIFTY24D0524000CE` vs `BANKNIFTY18JAN23500CE`) are NOT documented on any fetched live page (confirmed 2026-07-14). Ask support/forum, or reverse from a live dump + tolerant parsing. | support/policy + probe | pipeline |
| U-29 | OPEN | **Index rows in `/instruments/NSE` vs full dump only + current dump size/row count** — live docs say only "large amounts of data". Measure on first live fetch. | probe | ops |
| U-30 | OPEN | **Modify-order `quantity`: total or remaining?** — the live modify section (fetched 2026-07-14) does NOT state it (Z-ORD-8). The exact Dhan foot-gun class (Dhan documents TOTAL); getting it wrong resizes live orders. Ask forum/support or probe with a resting order. | support/policy + probe | ops |
| U-31 | ◐ NARROWED (2026-07-14) | **Market-protection mandatory-ness + default band** — CLOSED halves: the automatic band table (EQ/FUT 2%/1%/0.5%; OPT 5%/3%/2%/1% by price band) is LIVE-DOC (market-price-protection support article), and the API semantics (−1 auto / >0..100, MARKET+SL-M only, converts to limit, LPP may reject) are LIVE-DOC (orders page). STILL OPEN: whether `market_protection` is mandatory anywhere and whether `0`/omission is rejected — the live pages do NOT say (the old SEARCH mandatory-ness reading is UNSUPPORTED). | probe | ops |
| U-32 | ◐ NARROWED (2026-07-14) | **Interim order-state list + AMO windows** — CLOSED half: 8 interim literals live-enumerated (`PUT ORDER REQ RECEIVED, VALIDATION PENDING, OPEN PENDING, MODIFY VALIDATION PENDING, MODIFY PENDING, TRIGGER PENDING, CANCEL PENDING, AMO REQ RECEIVED`), explicitly non-exhaustive — keep an open-world fallback arm. STILL OPEN: AMO placement windows / per-exchange eligibility (support-portal AMO article, not crawled). | runner/support | ops |
| U-33 | ◐ NARROWED (2026-07-14) | **Orders misc bundle** — CLOSED: CO is CURRENT (`variety=co` glossary + CO-modify table + `parent_order_id` multi-leg section; `bo` absent — dead). STILL OPEN: place-time CO stop-leg param (Go-only `stoploss` SDK-attested); `disclosed_quantity` min-% floor; `algo_id` semantics (Go-only, not on the live page); full product×segment matrix incl. MTF constraints; iceberg minimum order value/leg + cancel-the-leg semantics (support iceberg article is in the crawl bundle — mine it). | runner/support + probe | ops |
| U-34 | ◐ NARROWED (2026-07-14) | **GTT bundle** — CLOSED: max active = **500** (what-is-gtt article, dateModified 2026-07-03); validity = **1 year** (validity article + ARITH); all 7 status one-line semantics live-doc'd; `order_type` documented LIMIT-only; list window = active + 7-day others, by-id age-unlimited; products CNC/NRML/MTF; corporate-action system-cancels; gap-open triggering. STILL OPEN: trigger-distance validation rules; whether the API accepts MARKET legs (Kite-web UI says "limit price or market price"); `expires_at` client-settability (one live example is NOT +1y); `parent_trigger` + populated `meta`/rejection shape; GTT rate bucket (catch-all 10/s presumed per U-38); postback/WS event on fire (result meta `{"gtt":105099}` suggests tagging). NEW sub-rows → U-51 (TSL), U-52 (TPIN). | probe + support | ops |
| U-35 | ✅ CLOSED (2026-07-14 runner fetch) | **Exceptions-page full taxonomy.** ANSWER: exactly **9 documented types** incl. `MarginException` AND `HoldingException` ( https://kite.trade/docs/connect/v3/exceptions/ ); NO permission type on the live page — the `PermissionException` (py) vs `PermissionError` (go) wire spelling stays unarbitrated but non-blocking (unknown-name → GeneralException fallback absorbs). | — | ~~none~~ closed |
| U-36 | ◐ NARROWED (2026-07-14) | **Portfolio field-semantics bundle** — CLOSED: `short_quantity` = example-only/undocumented; `holdings/summary`+`compact` absent from the live page (Go/mock-only); `m2m` = "mark to market returns (last close vs LTP)", `pnl` = "net returns", `unrealised/realised` = intraday; pledge surface = NONE (zero live hits); `collateral_type` typed `null,string` (nullable). STILL OPEN: holdings `price` semantics (live description cell is EMPTY, value 0 in every row — support ticket only); `authorisation` populated shape; `collateral_type` value set; conversion-failure envelope; dual-listed row shape. NEW live fact recorded: unauthorised eDIS sell → `POST /orders` **HTTP 428**. | probe + support | ops |
| U-37 | ◐ NARROWED (2026-07-14) | **Funds/margins bundle** — CLOSED: all 20 funds field descriptions live-doc'd; `/margins/orders` param table live; `mode=compact` documented on BOTH margin routes (py gap); basket `charges` = documented "Final charges block" with official ignore-note; `/charges/orders` accepts IMAGINARY orders (corrects "executed-only"); rate family = the 10/s catch-all; dead py route `GET /margins/{segment}` = live-absent/undocumented-legacy. STILL OPEN: exact server formulas for `utilised.debits` + `net` (the live doc contradicts its own sample — never derive client-side, → U-55); `consider_positions` default/casing; compact-mode response SHAPE. | probe | ops |
| U-38 | ✅ CLOSED (2026-07-14 runner fetch) | **Rate family for `/margins/*`, `/charges/orders`, `/gtt/*`, `/alerts`.** ANSWER: the official exceptions-page table has NO endpoint-specific rows for these → the documented **"All other endpoints 10req/second"** catch-all applies — https://kite.trade/docs/connect/v3/exceptions/ . (Burst-probe confirmation optional hygiene.) | — | ~~capacity~~ closed |
| U-39 | ◐ NARROWED (2026-07-14) | **Postbacks bundle** — CLOSED: coverage wording verbatim (api_key-scoped HTTP vs user-scoped WS — Z-PB-1); raw-HTTP-POST-body delivery (Z-PB-3 half); trigger set COMPLETE/CANCEL/REJECTED/UPDATE with "UPDATE = modified or partial fill" (Z-PB-4); WS `order` frame = the FULL postback payload incl. checksum (Z-PB-5 — corrected); checksum operand = `order_timestamp` (Z-PB-7 half); works-while-logged-out. STILL OPEN: registration mechanics/localhost policy for `postback_url` (developers console login-gated — Z-PB-2); POST Content-Type + retry/timeout policy (Z-PB-3); one-POST-per-partial-fill granularity (Z-PB-4); delivery guarantees across reconnects (Z-PB-6 — poll `/orders` to reconcile per forum); null-`order_timestamp` checksum edge (Z-PB-7). | probe + support | ops |
| U-40 | ✅ CLOSED (2026-07-14 runner fetch) | **MF placement liveness.** ANSWER: **DISABLED** — live-verbatim "Order placement can't be done, as order placement needs payment from the user's bank account"; the documented surface is exactly **5 read-only GETs**; SDK write routes reclassified historical ( https://kite.trade/docs/connect/v3/mutual-funds/ ). Also closed: the SIP `instalment_day` enum (1/5/10/15/20/25) is refuted by live samples (days 10, 30); SIP read statuses ACTIVE/PAUSED/CANCELLED; `/mf/instruments` = gzipped 16-col CSV. Residuals moot/minor: modify-status literals (route no longer documented), master refresh cadence. | — | ~~none~~ closed |
| U-41 | ✅ CLOSED (2026-07-14 runner fetch) | **Alerts docs page.** ANSWER: live at https://kite.trade/docs/connect/v3/alerts/ (changelog-dated 2025-06-12) — **500 active alerts/user cap**, `lhs_exchange` ∈ {NSE, BSE, NFO, CDS, BCD, MCX, **INDICES**}, GET filters `status/page/page_size`, statuses enabled/disabled/deleted (SOFT delete), multi-delete `DELETE /alerts?uuid=…`, full condition algebra + ATO basket structure — all matching the SDK/mock derivation byte-for-byte (zero contradictions). Residuals split out → U-53. | — | ~~none~~ closed |
| U-42 | ◐ NARROWED (2026-07-14) | **Pricing liveness + console mechanics** — CLOSED: **₹500/mo per API key incl. live + historical data; Personal FREE (no data); Publisher free** — live-verbatim ×5 pages (U-P1); the create-app FORM half (sign-up article: My Apps → Create New App fields, localhost redirect OK, optional postback URL); billing via Razorpay or account-debit, invoices, cancel/delete semantics; **no sandbox**; bundled-historical requires the Connect subscription (U-P4). STILL OPEN: GST — ₹500 inclusive or +18%? (stated NOWHERE live — a real invoice line settles it; the repo quotes costs GST-inclusive — U-P5); whether the legacy credits ledger (1 credit = ₹1) persists inside the login-gated console (U-P3 residual). | support (invoice) | ops |
| U-43 | OPEN (evidence improved 2026-07-14) | **Entitlement failure shape** — exact HTTP status/error_type a data-less Personal key gets on `GET /quote` or WS connect (the Dhan DH-902/806 analog). Forum-user reports on live-fetched 14868 show "Insufficient permission for that call" (March 2025) — status code + error_type still unconfirmed officially. | probe | ops |
| U-44 | ✅ CLOSED (2026-07-14 runner fetch — REFUTED) | **"2023-09 free API" hypothesis.** ANSWER: NO such event — the live z-connect update (24 Apr 2025) proves ₹2000+₹2000 was still charged into 2025, and the 2025-05-06 forum OP says execution APIs "free since March 2025". Do NOT cite a 2023 date anywhere. | — | ~~none~~ closed |
| U-45 | ✅ CLOSED (2026-07-14 runner fetch) | **Startup/mass-retail free tier.** ANSWER: real — "If your platform is targeted at the mass retail market, the Kite Connect APIs are available free of cost" live-verbatim from https://kite.trade/startups/ ; contact `talk@rainmatter.com` (decoded); Rainmatter linkage confirmed. Residual note (not blocking): detailed partnership/revenue-share terms are email-only. | — | ~~none~~ closed |
| U-46 | ◐ NARROWED (2026-07-14) | **`trigger_range` liveness + route shape** — CLOSED half: UNDOCUMENTED on live v3 docs (zero hits across all 148 fetched pages) — CO-legacy confirmed at docs level; do not build against it. STILL OPEN: which of the two divergent SDK route shapes (if any) the server accepts — one probe, completeness only. | probe | none |
| U-47 | ◐ NARROWED (2026-07-14) | **Session-response field semantics** — CLOSED: `public_token` = "public session validation where requests may be exposed to the public"; `refresh_token` = "long standing read permissions … certain approved platforms"; redirect carries `request_token`+`status`. STILL OPEN: `enctoken` + `silo` (in the live sample JSON but with NO row in the live attributes table — Verified-absence of documentation); `login_time` timezone (no label — Assumed IST); the `kite.trade/connect/login` alias (cosmetic, untested). | probe/support | none |
| U-48 | ◐ NARROWED (2026-07-14) | **Gzip on REST responses** — CLOSED half: "The responses may be Gzipped" is live-verbatim on the INTRODUCTION page ( https://kite.trade/docs/connect/v3/ — attribution corrected from response-structure/). STILL OPEN (minor): transport-level confirmation on `/instruments` + whether `Accept-Encoding: identity` is honored. | probe | none |
| U-49 | OPEN | **MCXSX (segment 8) liveness** — defunct exchange, still in both SDK segment maps; the live websocket page doesn't enumerate segments. Do tokens ending in 8 occur in the live dump? Cosmetic. | probe | none |
| U-50 | OPEN — NEW (2026-07-14) | **Quote/Historical rate-class pooling scope** — order limits are settled account-level (U-14), and the general 10/s is per-api_key (staff), but whether the Quote 1/s and Historical 3/s classes pool per KEY or per ACCOUNT when one account holds multiple API keys is stated nowhere live. Matters only for multi-app designs. | probe/support | capacity |
| U-51 | OPEN — NEW (2026-07-14) | **GTT Trailing Stoploss (TSL) API exposure** — TSL exists on Kite web (what-is-gtt article, 2026-07-03) but has NO parameter on the live GTT API page and neither SDK models it. Expressible via `/gtt/triggers`? Does a TSL GTT surface via `parent_trigger` in GET responses? | probe/support | none |
| U-52 | OPEN — NEW (2026-07-14) | **Sell-GTT TPIN/DDPI interaction with API placement** — the CDSL TPIN authorization requirement for sell GTTs on holdings (LIVE-DOC support article): create-time rejection vs trigger-time failure for API-placed sell GTTs on a non-DDPI account? Silent trigger-time failure would be the dangerous arm. | probe/support | ops |
| U-53 | OPEN — NEW (2026-07-14, split from U-41) | **Alerts residual bundle** — `lhs_attribute` enum beyond `LastTradedPrice` (the only value named anywhere — Z-AL-5); ATO entitlement + fired-ATO `order_meta` shape (Z-AL-2); evaluation cadence + notification channels; `page_size` default/max; `weight` semantics (live example decouples weight 10000 from quantity 1); why pykiteconnect still has no alerts surface (Z-AL-3, recording only). | probe | none |
| U-54 | OPEN — NEW (2026-07-14) | **Offsite/Publisher warts** — the live site's own `tag`-cap self-conflict (basket page: max 20; publisher page: max 8 — which does the server enforce offsite? Z-PB-2 of file 16); whether the post-basket redirect param is literally `request_token` (the page's `request_key`/`redirect_login` typos — Z-PB-3 of file 16); is `iceberg` truly unavailable offsite or just undocumented (Z-PB-4 of file 16)? | probe | none |
| U-55 | OPEN — NEW (2026-07-14) | **Funds `debits`/`net` exact server formulas** — the live user page's own `utilised.debits` composition formula does not match its own sample arithmetic (141731.95 vs 145706.55) and the `net` parenthetical cannot be complete. NEVER reconstruct either client-side — read the fields. Only Zerodha support can state the formulas. | support | ops |
| U-56 | OPEN — NEW (2026-07-14) | **SDK ecosystem residue** — hosted pykiteconnect docs link is `/v4/` while the repo changelog shows v5.x (stale hosted doc or current major line? — Z-SDK-1); Java/PHP/.NET SDKs were never mined (possible Go-style exclusive families / divergent defaults — Z-CL-2). Recording only. | runner/probe | none |
| U-57 | OPEN — NEW (2026-07-14, residual of U-20) | **Index-packet "Price change" (bytes 24–27) scaling/signedness** — the field is now officially labeled but undemonstrated (no mock index packet; both SDKs ignore it). A native parser naming the field should not guess its divisor. | probe | none |
| U-58 | OPEN — NEW (2026-07-14, residual of U-14) | **Date of the 3,000 → 5,000 orders/day raise** — unannounced in every fetched source (the changelog logs additions only). Timeline hygiene, nothing more. | support | none |

## Priority ordering (re-prioritized 2026-07-14 — the paste class is gone)

- **First hour of the first live session (probes):** U-7 (just-closed-minute ladder — THE gating probe), U-4 (dual-mint token test), U-16 (capture a 429), U-17 (over-cap/malformed-i), U-19 (bare-subscribe default mode), U-21 (heartbeat measurement), U-22 (SENSEX/BFO end-to-end), U-25 (dump headers + keyless GET + publish-time diff), U-26 (segment enum from the dump), U-29 (dump size/index rows), U-43 (Personal-key entitlement reject), U-48, U-9 (index/BFO historical), U-6/U-8/U-15 residual confirms, U-23 (a BCD tick), U-49, U-57.
- **Before any ORDER-path design (not currently in tickvault scope):** U-30 (modify quantity semantics — the Dhan foot-gun), U-31 (market-protection mandatory-ness), U-32 (AMO windows), U-33 (CO stop-leg param, iceberg floors), U-34 (GTT residuals), U-39 (postback delivery mechanics), U-52 (sell-GTT TPIN), U-54.
- **Support/policy asks:** U-5 (TOTP-login ToS), U-28 (tradingsymbol grammar), U-55 (funds formulas), U-42 residual (GST invoice line), U-58.
- **Nice-to-know / completeness:** U-36/U-37 residuals, U-46, U-47, U-50, U-51, U-53, U-56.

---

## 📋 Operator-paste URL list — OBSOLETE (history note)

> The 2026-07-13 build carried a 20-row ordered operator-paste URL list here (the gdf-ref pattern), because the research sandbox's egress proxy blocked every kite.trade/zerodha.com page. **The 2026-07-14 GH-runner crawl (`docs-fetch.yml`, run 29316744448) fetched every one of those URLs — and 100+ more discovered from the site's own nav — making the paste list obsolete.** It is retained only in git history (this file @ 2026-07-13). The refresh path going forward is the **`docs-fetch.yml` workflow_dispatch** with the URL list in `00-COVERAGE-MANIFEST.md`; re-diff the docs nav on each refresh (U-1's residual maintenance duty). The one paste-like surface left is the **login-gated developers.kite.trade console** (billing/credits tab, postback-URL registration form) — rows U-39/U-42 — which no unauthenticated runner can reach.
