# Zerodha Kite Connect v3 — API Reference Pack

> **Vendor:** Zerodha (Kite Connect v3 — `api.kite.trade` REST + `wss://ws.kite.trade` streaming), India's largest retail broker's official trading/data API.
> **Fetched:** 2026-07-13 (SDK/mock/search research) + **2026-07-14 live-verbatim via GH-runner** (`docs-fetch.yml` crawl, run 29316744448, 2026-07-14T08:04–08:11Z — 148 URLs, 147× HTTP 200; per-URL sha256 + UTC timestamp in the crawl fetch-log, ledger in `00-COVERAGE-MANIFEST.md`).
> **Purpose:** research-only reference pack so a future implementation session needs ZERO re-research. Mirrors the `docs/dhan-ref/` + `docs/groww-ref/` + `docs/gdf-ref/` patterns.
>
> **🎯 SCOPE BANNER — REFERENCE ONLY.** This pack is documentation, NOT a feed-scope grant: **no feed code, no order code, no credentials, no Zerodha account exists in this project.** It mirrors the read-only DhanHQ-skill pattern (CLAUDE.md — "READ-ONLY API reference … never used to place orders"). Adding Zerodha as a feed/broker requires its own dated operator scope lock per `.claude/rules/project/websocket-connection-scope-lock.md` re-approval protocol + `no-rest-except-live-feed-2026-06-27.md` FIRST.
>
> **Single source of open questions:** `99-UNKNOWNS.md` — closed rows stay for audit (✅ CLOSED with the answer + citation); the surviving open surface is almost entirely **live-session probes** (needs a real api_key), no longer operator pastes.
> **Per-URL coverage ledger:** `00-COVERAGE-MANIFEST.md` — one row per docs page with fetch status, sha256 and UTC timestamp from the 2026-07-14 runner crawl.

---

## ✅ PROVENANCE — the pack is now LIVE-DOC-grade

**On 2026-07-14 a GitHub-Actions runner fetched every kite.trade / zerodha.com / support.zerodha.com page this pack cites, live and verbatim** — all 19 live docs-nav sections (20 primary docs pages counting the out-of-nav legacy `market-data-and-instruments/` mirror; including 5 sections the 2026-07-13 pack had never seen: `apps/`, `basket/`, `publisher/`, `sdks/`, `changelog/` — now files 16–17), all 7 pricing/product pages, 31 support articles, 17 forum threads, and the site's own discovered nav links (148 URLs total, sole failure a favicon asset). Every pack file was then re-verified claim-by-claim against the live HTML: **200+ claims upgraded to the new top tier, 17 claims corrected where the live page contradicted the earlier evidence** (the live page always wins — see the Refuted section).

**History note (why older tier labels exist):** the original 2026-07-13 research sandbox was egress-proxy-blocked from every official page (and every archive/relay), so the first pack build was assembled from official SDK source + official mocks + search-backend extraction, with a large operator-paste TODO surface. The runner fetch obsoleted that constraint; the ARCHIVE-DOC / MIRROR-LIVE tiers defined then were never used and are retired. The SDK/mock evidence is RETAINED throughout as cross-check (and remains the sole authority where the docs are silent — e.g. WS endianness, the token×256 segment math).

## Evidence-tier legend (header block on every file)

| Tier | Meaning | Status in this pack |
|---|---|---|
| **`Verified (LIVE-DOC runner 2026-07-14)`** | claim confirmed against the runner-fetched live official page (per-URL sha256 + UTC timestamp in the fetch-log; cite the live URL + the LIVE-DOC label) | **THE TOP TIER — the pack's backbone since 2026-07-14** |
| `LIVE-DOC (forum)` / `LIVE-FORUM` | Zerodha **staff** answers on live-fetched kite.trade forum threads (same runner crawl) — one notch below a docs-page table | limits history, historical caps confirmation, pricing timeline |
| `CLIENT-LIB-SOURCE` | official Zerodha SDK source (pykiteconnect / gokiteconnect / kiteconnectjs @ pinned commits, fetched 2026-07-13) | cross-check; sole authority where docs are silent (endianness, segment low-byte, SDK route shapes) |
| `OFFICIAL-MOCK` | `zerodha/kiteconnect-mocks@main` c7a8123 (canonical response shapes; incl. the two real `.packet` wire captures decoded + reproduced in-sandbox) | cross-check; the pack's empirical protocol proof |
| `SEARCH` | search-backend extraction of a named live page, 2026-07-13; sub-Verified — never a bare "Verified" prefix | RESIDUAL ONLY — survives only where no live page covers the claim |
| `ARITH` | derived arithmetic, derivation shown (token maths, epoch decodes, date decodes) | supporting |

Confidence overlay: **Verified** / **Verified-absence** (grep-proven absent from a named complete artifact — scope always stated; since 2026-07-14 this includes "absent from every fetched live docs page") / **Assumed** / **Unknown** (→ `99-UNKNOWNS.md`).

## File index (20 files)

| File | Contents |
|---|---|
| `00-COVERAGE-MANIFEST.md` | per-URL fetch ledger — **20/20 primary docs-nav pages live-verbatim** (runner 2026-07-14), sha256 + timestamps |
| `01-overview-auth-and-token-lifecycle.md` | product, base URLs, headers, login flow + checksum, session shape, **daily token lifecycle (6 AM expiry — live-verbatim)**, refresh-token reality, logout, profile |
| `02-rest-conventions-errors-and-rate-limits.md` | envelopes, form-vs-JSON bodies, 9-type documented exception taxonomy, HTTP codes, **the official rate-limit table (live-verbatim) + settled order caps** |
| `03-orders.md` | full order surface, varieties, autoslice (flat-array response), iceberg 2–50, market protection + band table, 8 interim states, trades, broker caps |
| `04-gtt.md` | GTT single/two-leg, wire format (JSON-strings in form fields), 7 statuses w/ live semantics, 1-year validity, 500-cap, triggered≠executed |
| `05-portfolio.md` | holdings, positions {net,day}, conversion, auctions, eDIS authorise (now live-documented, HTTP 428 sell error) |
| `06-margins-and-charges.md` | funds/margins per-field live table, order/basket margin calculators, virtual contract note (imaginary orders OK), the shared charges object |
| `07-market-quotes.md` | `/quote` family, `i=` param, per-call caps **500/1000/1000 (live-verbatim)**, prev-day `ohlc.close`, silent-absence semantics, 5-level depth |
| `08-historical-candles.md` | the historical endpoint, per-interval range caps (server-enforced, forum-confirmed), candle arrays, +0530 timestamps, continuous/oi, freshness still Unknown |
| `09-instruments-master.md` | gzipped CSV master, 12 columns (NO ISIN), token=exch_token×256+segment (SDK-only), **token-reuse-after-expiry Note (live)**, index tokens |
| `10-websocket-streaming.md` | **THE feed-decision file** — binary protocol (live packet tables byte-matching the SDK offsets + empirical mock decode), modes, framing, divisors, index "Price change" field, limits |
| `11-postbacks-and-order-updates.md` | HTTP postback + checksum formula (live-verbatim), WS text-frame order updates (full postback payload), the `message` frame type |
| `12-mutual-funds.md` | **read-only live surface (placement disabled)** — 5 GET endpoints; SDK write routes reclassified historical |
| `13-pricing-and-access-model.md` | ₹500/mo model live-verified, Personal free, dated 2016→2026 timeline, startups free tier, billing flow, no sandbox |
| `14-option-chain-composition.md` | **no chain endpoint (live-verified absence)** — the composition recipe + §5 the consolidated India-F&O/BFO coverage note |
| `15-alerts.md` | Alerts API + ATO — now live-documented (`/alerts/`, 500-cap, pagination, INDICES pseudo-exchange) |
| `16-apps-publisher-and-basket.md` | **NEW (live nav)** — webview login pattern, offsite basket execution, Publisher JS plugin, API-key lifecycle |
| `17-sdks-and-changelog.md` | **NEW (live nav)** — the 6 official SDKs, header versioning, the full dated changelog (additions-only caveat) |
| `99-UNKNOWNS.md` | consolidated open questions — closed rows retained for audit; surviving surface = live-session probes |

## Reconciled master claims table (the load-bearing facts)

First adjudicated 2026-07-13 (SDK/mock/search + 5 adversarial review passes); **re-tiered row-by-row 2026-07-14 against the live runner crawl**. Rows [Z#1–60] keep their 2026-07-13 numbers (files cross-reference them); [Z#61–67] are new rows from the 5 newly-discovered nav sections + the settled numbers. Per-file CLAIMS appendices carry the full citation detail — this table is the index. Cite rows as **[Z#n]**.

| # | Claim | Tier | Source file |
|---|---|---|---|
| 1 | REST root `https://api.kite.trade`; login `https://kite.zerodha.com/connect/login?v=3&api_key=…` (kite.zerodha.com, NOT kite.trade); optional `redirect_params` round-trips; redirect returns `request_token` **+ `status`** | Verified (LIVE-DOC runner 2026-07-14 — user/ + apps/) + CLIENT-LIB-SOURCE ×2 | 01 §2/§5 |
| 2 | Every authenticated request: `X-Kite-Version: 3` (major or full, e.g. `1.3`) + `Authorization: token <api_key>:<access_token>` (composite, lowercase `token` scheme — a third scheme vs Dhan's `access-token` and Groww's `Bearer`) | Verified (LIVE-DOC — user/ "Signing requests" + sdks/) + CLIENT-LIB-SOURCE ×2 | 01 §3, 02 §1, 17 §2 |
| 3 | Token exchange: `POST /session/token`, form-encoded, `checksum` = "SHA-256 hash of (api_key + request_token + api_secret)" (hex); `request_token` is ONE-TIME, lives "only a few minutes" — exchange immediately | Verified (LIVE-DOC — user/, verbatim) + CLIENT-LIB-SOURCE byte-exact | 01 §5 |
| 4 | The session response carries NO expiry/TTL field (only `login_time`, 19-char naive string) — the client must KNOW the daily-expiry rule; worse than Dhan/Groww which return machine-readable expiry. `enctoken`/`silo` appear in the sample but have NO documented row (Verified-absence) | Verified (LIVE-DOC sample + attributes table) + OFFICIAL-MOCK | 01 §6 |
| 5 | **Access token "will expire at `6 AM` on the next day (regulatory requirement)"** — unless invalidated earlier via the API or by a **master-logout from the Kite Web terminal** (a documented third death path). CORRECTED 2026-07-14: the earlier "~07:30 AM flush" forum figure was WRONG. Timezone on "6 AM" unlabeled (Assumed IST) | Verified (LIVE-DOC — user/, verbatim) | 01 §7 |
| 6 | **NO unattended token path for individuals:** daily interactive login on kite.zerodha.com; `refresh_token` = "long standing read permissions … only available to certain approved platforms" (empty for individuals); no programmatic request_token exists; `POST /session/refresh_token` exists in the SDKs but is absent from the live endpoint table | Verified (LIVE-DOC + Verified-absence) + CLIENT-LIB-SOURCE | 01 §5/§7/§8 |
| 7 | One-active-token-per-key (second mint kills the first) | Assumed (live page silent; probe — the Dhan dual-instance lesson) | 01 §7 |
| 8 | Session-death wire signature: HTTP **403** + `error_type == "TokenException"` (fires pykiteconnect's `session_expiry_hook`) | Verified (CLIENT-LIB-SOURCE; live pages silent on the exact pair) | 01 §4, 02 §5c |
| 9 | Versioning is HEADER-based (no `/v3/` in any path); POST/PUT bodies form-urlencoded EXCEPT exactly 3 JSON routes (`/margins/orders`, `/margins/basket`, `/charges/orders`); GTT nests JSON as STRINGS inside form fields; responses may be Gzipped; endpoints not CORS-enabled | Verified (LIVE-DOC — root + response-structure/ + margins/ + gtt/) + CLIENT-LIB-SOURCE | 02 §1–§2, 04 §3 |
| 10 | Envelopes: `{"status":"success","data":…}` / `{"status":"error","error_type","message"[,"data"]}` (`data` optional) — NO numeric machine error codes (vs Dhan's DH-9xx); dispatch by `error_type` NAME, unknown names → GeneralException | Verified (LIVE-DOC — response-structure/ + exceptions/) + CLIENT-LIB-SOURCE + OFFICIAL-MOCK | 02 §3–§5 |
| 11 | Documented exception taxonomy = exactly **9 types** incl. `MarginException` + `HoldingException` (both in NEITHER SDK — unknown-name fallback absorbs); NO permission type documented (SDK-only variant, wire spelling unarbitrated); live HTTP-code table adds 404/405/410 | Verified (LIVE-DOC — exceptions/) + CLIENT-LIB-SOURCE + OFFICIAL-MOCK | 02 §4–§5, 03 §14 |
| 12 | REST timestamps are IST (UTC+5.5) strings `yyyy-mm-dd hh:mm:ss` (19-char); dates `yyyy-mm-dd` | Verified (LIVE-DOC — response-structure/ "Data types", verbatim) + CLIENT-LIB-SOURCE | 02 §3c |
| 13 | **API rate limits (official table, live-verbatim): Quote 1/s · Historical candle 3/s · Order placement 10/s · all other endpoints 10/s**; per api_key (forum staff); no daily cap outside orders; breach → HTTP 429 | Verified (LIVE-DOC — exceptions/ table) + LIVE-FORUM 8577/13397 staff | 02 §6a |
| 14 | **Order RMS caps SETTLED: 10/s · 400/min · 5,000/day (across all segments/varieties) · 25 modifications/order** — pooled at CLIENT-ACCOUNT level, not per app ("per client account, not per app"); RMS-blocked requests count; iceberg 5 legs = 5 toward day cap, 1 toward OPS; modify/cancel/GTT-place excluded; at the cap even exits are blocked. Previously conflicting 200/min + 3,000/day = superseded pre-2024/July-2023 figures | Verified (LIVE-DOC — exceptions/ + FAQ + order-rate-limits support articles + forum 12851 staff) | 02 §6b, 03 |
| 15 | NEITHER official SDK retries, backs off, rate-limits, or reads `Retry-After` — pacing is entirely caller-owned | Verified-absence (grep, both SDKs) | 02 §2/§6e |
| 16 | Quote-family per-call caps: **500** on `/quote`, **1000** on `/quote/ohlc` + `/quote/ltp` — the stray "250" is the stale legacy `market-data-and-instruments/` page (out of live nav, still HTTP 200 — do not cite it) | Verified (LIVE-DOC — market-quotes/ Limits table + prose + changelog corroboration) | 07 §3 |
| 17 | Full `/quote` payload: OHLC, volume, OI (+day high/low), circuit limits, **5-level bid/ask depth** — keyed by the requested `exchange:tradingsymbol`, repeated `i=` params; a no-data/expired key is **SILENTLY ABSENT from `data`** (check key existence) | Verified (LIVE-DOC — market-quotes/) + OFFICIAL-MOCK + CLIENT-LIB-SOURCE | 07 §2/§4 |
| 18 | `ohlc.close` in quotes = "Closing price of the instrument from the **last trading day**" (previous session — the Dhan Ticket #5525125 semantics class, now closed for Kite) | Verified (LIVE-DOC — market-quotes/ attribute tables) | 07 §4 |
| 19 | Historical endpoint: `GET /instruments/historical/{instrument_token}/{interval}` with `from`/`to` (`yyyy-mm-dd hh:mm:ss`), `continuous`, `oi`; the candle whose open time equals `to` IS included (docs examples) | Verified (LIVE-DOC — historical/) + CLIENT-LIB-SOURCE ×2 | 08 §1–§2 |
| 20 | Historical response: positional arrays `[ts, o, h, l, c, volume(, oi)]`, 6 elements (7 with `oi=1`); timestamps ISO-8601 with explicit `+0530` offset — the least timezone-ambiguous of the three brokers | Verified (LIVE-DOC examples byte-identical to OFFICIAL-MOCK + both SDK parsers) | 08 §5–§6, §8 |
| 21 | Documented interval set: `minute, 3minute, 5minute, 10minute, 15minute, 30minute, 60minute, day`. CORRECTED 2026-07-14: the server ALSO recognizes undocumented `2minute/4minute/hour/2hour/3hour/4hour/week` (per its own error list) — the documented 8 are the supported contract | Verified (LIVE-DOC — historical/) + LIVE-FORUM 14149 | 08 §3 |
| 22 | **Per-request range caps: minute 60d · 3/5/10minute 100d · 15/30minute 200d · 60minute 400d · day 2000d** — server-enforced (error `interval exceeds max limit: N days`); NO caps table exists on the live docs page (Verified-absence); the 2018 staff table is superseded | Verified (LIVE-FORUM — 14149 server-error quotes + 15111 + 11460) + Verified-absence (LIVE-DOC) | 08 §4 |
| 23 | **1-minute history available from ~2015-02-02** (staff-confirmed 2020 + 2024); day candles further back (some NSE stocks to late 1990s); NO rolling retention window — the "3-year" claim was a stale 2018 FAQ | Verified (LIVE-FORUM — 8722 + 14149 staff; conflict RESOLVED) | 08 §9 |
| 24 | Just-closed-minute availability/latency + forming-candle behaviour: UNDOCUMENTED everywhere incl. the live docs page — the gating fact for any Kite per-minute REST leg | Unknown (probe ladder specified; the pack's #1 surviving unknown) | 08 §10 |
| 25 | `continuous=1` = expired FUTURES contracts' candles via the live contract's token — **NFO + MCX futures only, `day` interval only**, expired-futures depth from JUL 2011; expired options unavailable (the SDK docstring's "futures and options" is stale) | Verified (LIVE-DOC — historical/ + LIVE-FORUM 3018/6292/14806 staff) | 08 §7 |
| 26 | Historical data included with the base subscription: the ₹2,000/mo add-on REMOVED effective **2025-02-08** (announced 2025-02-10, forum 14806 OP) | Verified (LIVE-FORUM 14806 + LIVE-DOC support articles) | 08 §11, 13 §2/§4 |
| 27 | Instruments master: `GET /instruments` (+ `/instruments/{exchange}`) returns a **gzipped CSV**, exactly 12 columns, **NO ISIN column** (cross-vendor joins need a symbol bridge, unlike Dhan/Groww); "generated once everyday", `last_price` stale by design; `instrument_type` ∈ EQ/FUT/CE/PE | Verified (LIVE-DOC — market-quotes/, header + column table verbatim) + OFFICIAL-MOCK + Go struct | 09 §2–§3 |
| 28 | **`instrument_token = exchange_token × 256 + segment_id`** (low byte = WS segment: nse 1, nfo 2, cds 3, bse 4, bfo 5, bcd 6, mcx 7, mcxsx 8, indices 9) — SDK-hard-coded + ARITH-proven (99/99 mock rows + all 4 live docs sample rows), **NOT narratively documented on the live pages**; AND the live Note warns exchanges "may reuse instrument tokens for different derivative instruments after each expiry" — key on `exchange:tradingsymbol`, treat tokens as day-scoped | Verified (CLIENT-LIB-SOURCE ×2 + ARITH) / token-reuse Note Verified (LIVE-DOC — market-quotes/) | 09 §5, 14 §2 |
| 29 | Fetch the dump once daily "ideally at around 08:30 AM" — the docs page's own Warning admonition (attribution closed); derivative tokens churn on expiry — NEVER hardcode tokens; actual server publish time still unpublished | Verified (LIVE-DOC — Warning verbatim) + LIVE-FORUM 3059 staff | 09 §6 |
| 30 | Index tokens: `NSE:NIFTY 50` = 256265 and `BSE:SENSEX` = 265 appear in a docs page's OWN quote examples — but ONLY on the out-of-nav legacy `market-data-and-instruments/` page (the current `market-quotes/` page's examples request those quote keys yet show only INFY token values); example values, not a normative table, cross-checked by SDK sources + ARITH; `NSE:NIFTY BANK` = 260105 stays forum-sourced; resolve from the daily CSV at runtime regardless | Verified (LIVE-DOC — legacy page, example values only; the ONE salvage from the poison-source page) + CLIENT-LIB-SOURCE/ARITH cross-check / SEARCH + ARITH (NIFTY BANK) | 09 §8, 14 §5 |
| 31 | Exchange enum: NSE, BSE, NFO, BFO, CDS, BCD, MCX (BFO first-class in both SDKs) — NOT enumerated on the live docs; the live orders-page exchange list omits BFO | Verified (CLIENT-LIB-SOURCE) + LIVE-DOC (orders/ list) | 09 §4, 03 |
| 32 | WS endpoint `wss://ws.kite.trade?api_key=<key>&access_token=<token>` — auth entirely in the query string, no auth frame | Verified (LIVE-DOC — websocket/) + CLIENT-LIB-SOURCE ×2 | 10 §1 |
| 33 | Client WS messages: text JSON `{"a": "subscribe"/"unsubscribe"/"mode", "v": …}` with NUMERIC tokens (opposite of Dhan's string SecurityIds); modes `ltp`/`quote`/`full`; resubscribe-after-reconnect is CLIENT-side | Verified (LIVE-DOC — websocket/ request table) + CLIENT-LIB-SOURCE ×2 | 10 §4–§5 |
| 34 | Binary framing: u16 packet-count + per-packet u16 length (LIVE-DOC); "all prices in paise", ÷100, currencies ÷1e7 (LIVE-DOC). CORRECTED 2026-07-14: the live page **never states endianness** — big-endian unsigned rests on SDK source + the real mock packet decoding only as BE; and BCD ÷1e4 (SDK) **disagrees** with the live blanket "currencies ÷1e7" — discrepancy recorded, live BCD tick needed | Verified (LIVE-DOC framing/divisors) + CLIENT-LIB-SOURCE ×2 + mock decode (endianness) | 10 §6–§7 |
| 35 | **Packet type = body LENGTH: 8 (ltp) / 28 (index quote) / 32 (index full) / 44 (quote) / 184 (full)**; segment = `token & 0xFF` (SDK-only); indices non-tradable | Verified (LIVE-DOC — mode table 8/44/184, index table 28/32) + CLIENT-LIB-SOURCE ×2 | 10 §7 |
| 36 | Quote packet (44B): token/ltp/ltq/atp/volume/buyq/sellq @0–27, OHLC as **O,H,L,C** @28–43 (`close` = prev session); NO timestamp in quote mode — live table byte-for-byte matches the SDK offsets | Verified (LIVE-DOC table) + empirical mock decode | 10 §10 |
| 37 | Full packet (184B): + ltt@44, oi@48, oi_day_high@52, oi_day_low@56, exchange_timestamp@60, 10 × 12-byte depth entries (5 bid @64, 5 offer @124; qty u32 + price u32 + orders u16 + 2B pad "which should be skipped") | Verified (LIVE-DOC tables) + empirical mock decode (all 10 levels) | 10 §11–§12 |
| 38 | **Index packets order OHLC as H,L,O,C** (opposite of quote packets); bytes 24–27 = **"Price change"** (official live label — RESOLVED; both SDKs leave it unread and compute change client-side; scaling/signedness undemonstrated) | Verified (LIVE-DOC — #index-packet-structure) + CLIENT-LIB-SOURCE ×2 | 10 §9 |
| 39 | **Kite WS timestamps are STANDARD UNIX epoch seconds (UTC-anchored)** — mock raw 1625461887 ↔ official "2021-07-05T10:41:27+05:30". THE most dangerous cross-broker difference: Dhan's WS epoch is IST-anchored (−19800 for UTC); porting a parser corrupts every timestamp by 5h30m. Live page silent on the anchor | Verified (OFFICIAL-MOCK) + ARITH | 10 §12 |
| 40 | The derived `change` field has THREE divergent semantics (py = percent, go = absolute, official mock = absolute; the quote mock's value matches NEITHER formula) — not on the wire, never validate it against the mocks | Verified (adversarial cross-check) | 10 §12 note |
| 41 | Heartbeat = 1-byte binary frame "every couple seconds", sent **when there is no data to stream**, "can be safely ignored" (both SDKs ignore <2-byte frames); exact cadence unstated — py pings 2.5s/drops ~5–7.5s, go passively reconnects after 5s silence | Verified (LIVE-DOC wording) + CLIENT-LIB-SOURCE (SDK policies) | 10 §3/§14 |
| 42 | **WS caps: 3,000 instruments/connection × 3 connections/api_key (≈9,000)** — live-verbatim; enforcement staff-confirmed Dec 2025: >3,000 → error for the EXCESS tokens only, connection unaffected; the 3-conn cap is SOFT (abuse-flag/suspension) | Verified (LIVE-DOC — websocket/ + forum 15708 staff) | 10 §2, 02 §6d |
| 43 | Order updates + errors arrive as TEXT frames on the SAME WS as binary ticks ("Market data is always binary and Postbacks and other updates are always text"); THREE documented types: `order` / `error` / **`message`** (broker messages/alerts — both SDKs silently DROP it); no separate order socket (vs Dhan's dedicated order-update WS) | Verified (LIVE-DOC — websocket/ #message-types) + CLIENT-LIB-SOURCE ×3 + OFFICIAL-MOCK | 10 §13, 11 §5 |
| 44 | Postback (HTTP webhook) checksum = "the SHA-256 hash of (order_id + order_timestamp + api_secret)" (live-verbatim) — Kite is the only broker of the three with an authenticated webhook; works even when the user is logged out; payload is a raw JSON POST body; only the JS SDK ships the validator | Verified (LIVE-DOC — postbacks/ Checksum section) + CLIENT-LIB-SOURCE kiteconnectjs | 11 §4 |
| 45 | Coverage asymmetry (live-verbatim Note): HTTP postbacks fire ONLY for "orders placed using the app's api_key"; WS order updates cover ALL the user's orders "anywhere, for instance, web, mobile, or desktop platforms" — and the WS `order` frame carries **the FULL postback payload structure** (incl. checksum/app_id — CORRECTED from the earlier no-checksum assumption) | Verified (LIVE-DOC — postbacks/ + websocket/) | 11 §1/§5 |
| 46 | Order surface: `POST/PUT/DELETE /orders/{variety}(/{order_id})`; varieties `regular/amo/co/iceberg/auction` (`bo` absent from the live page — Go legacy); `MTF` product live; validity `DAY/IOC/TTL` (`validity_ttl` in MINUTES). CORRECTED 2026-07-14: autoslice response = a **FLAT `data[]` array** of per-slice `{order_id}`/`{error}` entries (not parent+children), max 10 slices; **iceberg_legs = 2–50** (not 2–10) | Verified (LIVE-DOC — orders/) + OFFICIAL-MOCK cross-check | 03 |
| 47 | Order states are OPEN-ENDED: live page enumerates 8 interim literals (`PUT ORDER REQ RECEIVED, VALIDATION PENDING, OPEN PENDING, MODIFY VALIDATION PENDING, MODIFY PENDING, TRIGGER PENDING, CANCEL PENDING, AMO REQ RECEIVED`) and says "There may be other values as well"; common = OPEN/COMPLETE/CANCELLED/REJECTED — vs Dhan's closed 9-value enum | Verified (LIVE-DOC — orders/) + OFFICIAL-MOCK | 03 §12 |
| 48 | GTT: `/gtt/triggers` family; types `"single"`/`"two-leg"`; form-encoded with `condition`/`orders` as JSON STRINGS; `orders[i]` pairs with `trigger_values[i]` (live-verbatim); documented leg `order_type` is LIMIT-only; **`triggered` ≠ executed** — the live page's own example shows a triggered leg whose order FAILED (circuit limit); truth in `orders[].result.order_result` | Verified (LIVE-DOC — gtt/) + CLIENT-LIB-SOURCE + OFFICIAL-MOCK | 04 |
| 49 | GTT validity = **1 year** from placement (live support article + ARITH on the live example); **max 500 active GTTs** per account (SETTLED — older 250/100 superseded); list shows active + previous-7-days others, by-id fetch age-unlimited; placing GTTs free | Verified (LIVE-DOC — gtt/ + validity + what-is-gtt support articles) | 04 §7–§8 |
| 50 | Positions = `{net: [], day: []}` with SIGNED `quantity` (no LONG/SHORT enum); conversion = `PUT /portfolio/positions`, plain-int quantity → `data: true`; **NO exit API** ("There are no special API calls…" — exit via opposite order with the SAME product) and NO pledge surface (live-absence + SDK-absence); `holdings/summary`+`compact` absent from the live page (Go/mock-only) | Verified (LIVE-DOC — portfolio/) + OFFICIAL-MOCK + CLIENT-LIB-SOURCE | 05 |
| 51 | Margin calculators take JSON ARRAYS and return per-order margin components + a FULL statutory charges breakdown (stt/ctt, exchange/SEBI charges, brokerage, stamp, GST split) — unique among the three brokers; basket top-level `charges` = the documented "Final charges block" with an official ignore-note (Go struct still drops it); `/charges/orders` explicitly accepts **imaginary orders** (CORRECTED from "executed-only"); `mode=compact` documented on both margin routes | Verified (LIVE-DOC — margins/) + OFFICIAL-MOCK ×3 + CLIENT-LIB-SOURCE | 06 §3–§6 |
| 52 | **Kite Connect has NO option-chain, expiry-list, or greeks endpoint** — live-verified absence (19-entry nav + 5-endpoint market-quotes table + zero chain/greeks/IV hits across all 148 fetched pages); the chain is COMPOSED client-side (daily NFO/BFO master → filter → batched `/quote` ≤500 legs at 1/s → ~1 Hz per index chain); greeks self-computed — vs Dhan's and Groww's single-call chains with greeks | Verified-absence (LIVE-DOC + complete `_routes` + Go grep) + LIVE-FORUM staff | 14 |
| 53 | **India-F&O coverage:** NSE/BSE index REST quotes (tokens 256265/265) + the named WS index packet are now live-documented; NFO end-to-end documented; **BFO delivery + index/BFO HISTORICAL remain undocumented** (BFO has zero live example row/quote/tick/candle) — day-0 probes | Verified (LIVE-DOC upgrades) / Unknown (BFO + historical probes) | 14 §5 |
| 54 | Official **Alerts API** live-documented at `/docs/connect/v3/alerts/` (added 2025-06-12 per the changelog): `/alerts` family, UUID identity, **500 active alerts/user**, `status/page/page_size` filters, `simple` + **`ato`** types, `lhs_exchange` ∈ {NSE,BSE,NFO,CDS,BCD,MCX,INDICES}; still ABSENT from pykiteconnect | Verified (LIVE-DOC — alerts/) + CLIENT-LIB-SOURCE go + OFFICIAL-MOCK ×7 + Verified-absence (py) | 15 |
| 55 | **MF surface is READ-ONLY live: exactly 5 GET endpoints; order/SIP placement DISABLED** ("Order placement can't be done, as order placement needs payment from the user's bank account") — the SDK write routes are historical/undocumented; instruments master = gzipped 16-column CSV | Verified (LIVE-DOC — mutual-funds/) — SETTLES the 2026-07-13 liveness question | 12 |
| 56 | **Current pricing (live-verified 2026-07-14): Kite Connect = ₹500/month per API key incl. live WebSocket + historical data; Kite Connect Personal = FREE (orders/GTT/alerts/margin-compute/portfolio, NO market data); Kite Publisher = free**; no sandbox; no refunds; developers.kite.trade is login-gated | Verified (LIVE-DOC — kite.trade root + zerodha.com/products/api + support articles) | 13 §1 |
| 57 | Pricing timeline (dated, live-verified): launched **2016-04-20** (CORRECTED from 2015); pre-2025 = ₹2,000/mo/app + ₹2,000/mo historical add-on; add-on removed **2025-02-08**; Personal launched free **2025-03-14**; fee cut ₹2,000→₹500 **2025-05-06** (NSE retail-algo circular); the "2023-09 free API" story is REFUTED | Verified (LIVE-DOC — z-connect posts + forum 14806/14868/15015 OPs w/ timestamps) | 13 §2–§3 |
| 58 | Billing flow: developers.kite.trade signup → plan → create app → api_key+api_secret; payment via Razorpay or Zerodha-account debit, invoices downloadable; cancel preserves config, delete kills credentials. The legacy credits ledger (1 credit = ₹1) appears on NO live page — Unknown whether it persists inside the login-gated console | Verified (LIVE-DOC — support articles) / Unknown (credits residual) | 13 §5–§6, 16 §3 |
| 59 | `trigger_range` (CO-legacy): UNDOCUMENTED on live v3 docs (zero hits across all 148 fetched pages); the two official SDKs ship DIFFERENT route shapes; official mock exists; server shape probe-only — do not build against it | Verified-absence (LIVE-DOC) + CLIENT-LIB-SOURCE + OFFICIAL-MOCK | 07 §10 |
| 60 | eDIS: `POST /portfolio/holdings/authorise` → `{request_id}` + kite.zerodha.com web-view URL, completion redirect `…/finish?status=success\|error` — now LIVE-DOCUMENTED (added 2021-03-12 per changelog); authorisations valid one session (till 5:30 PM); cumulative sells ≤ authorised qty; an unauthorised sell fails `POST /orders` with **HTTP 428**; not in pykiteconnect | Verified (LIVE-DOC — portfolio/) + CLIENT-LIB-SOURCE go + OFFICIAL-MOCK | 05 §6 |
| 61 | **Webview login pattern (`apps/`):** desktop/mobile apps without a backend open the login in a webview, poll the URL for `request_token`+`status`; 3rd-party cookies REQUIRED; `127.0.0.1` redirect_url officially sanctioned for personal desktop apps; **api_secret must NEVER be embedded in distributed apps** (server-side exchange) | Verified (LIVE-DOC — apps/) | 16 §2 |
| 62 | **Offsite basket execution:** form-POST `api_key` + JSON basket (form field `data`) to `https://kite.zerodha.com/connect/basket` — exchange-approved order UI, NO prior login needed, redirect returns `status` + `request_token`; varieties regular/amo/co only; page carries `redirect_login`/`request_key` typos + a 20-vs-8 `tag`-cap conflict with the publisher page | Verified (LIVE-DOC — basket/) | 16 §4 |
| 63 | **Publisher JS plugin:** `https://kite.trade/publisher.js?v=3`, `<kite-button>`/`data-*`/dynamic-JS styles, basket **max 10**, `KiteConnect.ready()` mandatory, `finished(status, request_token)` callback, `authHoldings()` = the eDIS browser leg; officially **dead in iOS WebViews** (offsite execution recommended for ALL Publisher use cases) | Verified (LIVE-DOC — publisher/) | 16 §5 |
| 64 | **Six official SDKs** (Python, Go, Java, PHP, Node.js, .NET/C# — no Rust); `X-Kite-version` header takes major (`3`) or full (`1.3`) values, explicit pinning recommended for production | Verified (LIVE-DOC — sdks/) | 17 §1–§2 |
| 65 | **Dated changelog (live):** Alerts 2025-06-12 · MTF 2025-01-17 · virtual contract 2023-07-18 · basket margin 2023-07-11 · auctions 2022-12-22 · iceberg+TTL 2022-03-19 · rate-limit update 2021-09-28 · holdings auth 2021-03-12 · margin calcs 2020-08-24 · 3.1 (2019-12-10: GTT, meta, circuit limits, hist-OI) · 3.0 (2018-01-17: header auth, wss://ws.kite.trade, WS binary OI/LTT fields). **The changelog logs ADDITIONS only** — BO removal, MF disablement, and every pricing change are absent; unsorted with duplicate rows | Verified (LIVE-DOC — changelog/) | 17 §3–§5 |
| 66 | **API-key lifecycle:** developers.kite.trade signup → choose plan → My Apps → Create New App (name, Zerodha Client ID, redirect URL, optional postback URL) → API Key + Secret; localhost redirect fine for testing; **NO sandbox environment**; support = forum only | Verified (LIVE-DOC — sign-up + FAQ + api-sandbox support articles) | 16 §3, 13 §5 |
| 67 | **Startups/mass-retail free tier is real:** "If your platform is targeted at the mass retail market, the Kite Connect APIs are available free of cost" — contact `talk@rainmatter.com`; data-vending/redistribution PROHIBITED ("primarily an execution suite, not a data vending service") | Verified (LIVE-DOC — kite.trade/startups/ + support articles) | 13 §7 |

## Refuted / commonly-misstated claims (do NOT resurrect)

| Refuted / misstated claim | Truth |
|---|---|
| "The access token is flushed daily at **~07:30 AM IST**" | **REFUTED by the live page (2026-07-14):** the official wording is "it'll expire at **6 AM on the next day** (regulatory requirement)" — plus two more documented death paths (API invalidation; Kite Web master-logout). The 07:30 figure was a forum artifact (01 §7). |
| "The OHLC/LTP quote endpoints cap at **250** instruments" | The live Limits table reads **500/1000/1000**; the 250s live ONLY on the stale legacy `market-data-and-instruments/` page (out of the live nav, © 2015-2020, but still HTTP 200 — a **stale-mirror poison source**; it also uses pre-3.1 field names). Never cite that page for limits or field names — its one salvage is the index-token EXAMPLE values (Z#30), which are SDK/ARITH cross-checked (07 §3). |
| "iceberg_legs is 2–10" | Live orders page: **2–50**. The 10 belongs to AUTOSLICE (max 10 slices) — the two were conflated (03). |
| "Autoslice returns a parent order_id + `children[]`" | The live-documented response is a **FLAT `data[]` array** of per-slice `{order_id}`/`{error}` entries (03). |
| "Orders/min = 200, orders/day = 3,000 (current)" | Superseded: current = **400/min, 5,000/day** (live exceptions docs + support articles); 200 and 3,000 were the pre-2024/July-2023 figures (02 §6b). |
| "Kite Connect launched in 2015" | **2016-04-20** per Nithin Kamath's own launch post (live-fetched) — 2015 was the Kite platform, not Connect (13 §2). |
| "Zerodha made Kite Connect free in **September 2023**" | POSITIVELY REFUTED: the live 2025 posts prove ₹2000+₹2000 was still charged into 2025; execution went free March 2025, Personal launched 2025-03-14 (13 §3). |
| "Monthly SIP `instalment_day` ∈ {1, 5, 10, 15, 20, 25}" | Absent from the live MF page and contradicted by the live samples (days 10 and 30) — closed in the live page's favour (12 §4). |
| "MF orders/SIPs are placeable via the API" | The live page: "Order placement can't be done…" — the documented MF surface is 5 read-only GETs; the SDK write routes are historical (12 §1). |
| "The WS docs state big-endian" | The live page never states endianness (types are named "int16"/"int32") — BE unsigned rests on SDK source + the real mock packet decode (which only works as BE) (10 §6). |
| "`market_protection` is mandatory for MARKET/SL-M and `0` is rejected" | UNSUPPORTED by the live pages — the docs give −1/auto + >0..100 semantics and the band table, nothing about mandatory-ness (03 Z-ORD-7). |
| "`MarginException` is a pykiteconnect exception class" | It IS a documented wire type (live exceptions page, 2026-07-14) but exceptions.py still has exactly 8 classes — pykiteconnect dispatches it to GeneralException (02 §5, 03 §14). |
| "The mock `change` values validate the SDK formula" | The full-mock `change` is ABSOLUTE (matches go, not py's percent); the quote-mock `change` matches NEITHER formula. Never validate derived `change` against the mocks (10 §12). |
| "Kite WS timestamps are IST-anchored epoch (like Dhan)" | ARITH-refuted: raw 1625461887 = the official JSON's `10:41:27+05:30` only under STANDARD UTC epoch semantics. Dhan is the IST-anchored one (10 §12). |
| "Bracket orders (`bo`) are placeable" | `bo` appears NOWHERE on the live orders page (2026-07-14); the gokiteconnect `VarietyBO` constant is legacy residue (03 §2). |
| "The official Python client is v4" | The README H1 title is stale; the changelog in the same file shows v5/v5.2 — and the live sdks/ page still links the v4 hosted docs (01 §1, 17 Z-SDK-1). |
| "WS root is `wss://websocket.kite.trade`" | A kiteconnectjs JSDoc wart; the code default in all three SDKs AND the live changelog is `wss://ws.kite.trade` (11 §5, 17). |
| "`holdings.json` shows the same ISIN listed on both exchanges" | The holdings mock contains zero duplicate ISINs; the per-exchange-token fact rides on CROSS-mock evidence (05 §2). |

## Compare-at-a-glance (the three-broker deltas that bite)

| Dimension | Kite | Dhan (`docs/dhan-ref/`) | Groww (`docs/groww-ref/`) |
|---|---|---|---|
| Token life | expires **6 AM next day** (LIVE-DOC, regulatory); NO unattended mint for individuals (daily interactive login) | 24h JWT, TOTP API mint | daily 06:00 IST expiry, API mint |
| WS binary | BIG-endian scaled uint32, length-typed packets | LITTLE-endian, f32 prices, code-typed packets | protobuf over NATS |
| WS tick timestamp | UTC epoch seconds | IST-anchored epoch seconds | epoch milliseconds |
| Option chain | NONE — compose it yourself, no greeks (live-verified absence) | 1 call/underlying (1-per-3s), greeks incl. | 1 call/underlying+expiry, greeks incl. |
| Quote budget | 1/s × 500 (full, with depth) — LIVE-DOC | 1/s × 1000 | 10/s+300/min × 50 (LTP/OHLC) |
| Historical | 1 endpoint, path-typed interval, +0530 ISO rows, per-interval day caps (server-enforced, no docs table) | POST, columnar arrays, epoch secs, 90d intraday | GET, row arrays, 30/90/180d caps, from-2020 |
| Master ISIN | **absent** (12-col CSV) | present | present |
| Order webhook | HTTP postback with **SHA-256 checksum** + WS text frames (full payload) | postback, no checksum + dedicated order-update WS | no HTTP webhook |
| Pricing (2026-07) | ₹500/mo per key incl. data; Personal free (no data) | data APIs paid, trading APIs free | shared-account token, no per-key fee |

## Coverage

**20/20 primary docs-nav pages live-verbatim** (runner fetch 2026-07-14 — 148 URLs, 147× HTTP 200; the only failure is a favicon asset). Full per-URL ledger with sha256 + UTC timestamps: `00-COVERAGE-MANIFEST.md`. Refresh path: re-dispatch the `docs-fetch.yml` workflow with the manifest's URL list.

---

*Assembled 2026-07-13; upgraded to LIVE-DOC grade 2026-07-14. Per-file CLAIMS appendices carry the full citation detail behind every row above; `99-UNKNOWNS.md` carries what the live pages could NOT establish (closed rows retained for audit).*
