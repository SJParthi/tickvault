# DhanHQ v2 — Upstream Documentation Snapshot (2026-07-03)

**Source:** operator-provided full docs export, 2026-07-03. The pack was captured from
`https://docs.dhanhq.co/api/v2/` via DhanHQ's official "Export .md (for LLMs)" file
(`docs-export.md`, generated by DhanHQ on 2026-06-30). Per the pack's own `00-INDEX.md`,
all 105 non-schema pages under `/api/v2/` in the site's sitemap are covered, plus 3 bonus
files (Agent Skills, MCP, Cloud).

This directory follows the dated-snapshot convention of the sibling
`../dhanhq-v2-upstream-2026-06-02/` (previous audit). See the pack's `00-INDEX.md` for the
per-file index; files are kept verbatim as delivered.

**Curation notes (2026-07-03 docs-sync PR):**
- 29 markdown files kept as-is (~412 KB total). One file was dropped: an inner
  `dhanhq-api-v2-docs-pack.zip` that was a byte-duplicate of the same 29 files (binary,
  redundant).
- Security scrub performed before adding: no real JWTs, API keys, client IDs, or UCCs
  found — only DhanHQ's documented placeholders (`eyxxxxx`, `100xxxxxxx`).
- This snapshot CONFIRMS the deltas recorded in the dated "2026-07-03 Upstream Update"
  sections of `../08-annexure-enums.md` and `../04-full-market-depth-websocket.md`:
  currency segments 3/7 removed; ExpiryCode listed as 1=Near/2=Next/3=Far (still
  UNVERIFIED-LIVE — docs-only evidence); 8 instrument types (FUTCUR/OPTCUR dropped);
  200-level depth URL shown as `/twohundreddepth`; depth unsubscribe code 25; and the
  "Day Close … only sent post market close" wording that Ticket #5525125 (2026-04-10)
  established as WRONG (the field carries the PREVIOUS session's close, streamed live).
- Ticket-verified facts in `../03-live-market-feed-websocket.md` and
  `.claude/rules/dhan/live-market-feed.md` remain authoritative wherever this upstream
  snapshot contradicts them.
