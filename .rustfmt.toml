# =============================================================================
# dhan-live-trader — Rust Formatter Configuration
# =============================================================================
# Used by `cargo fmt`. Matches CLAUDE.md coding standards.
# IntelliJ reads this automatically when using the Rust plugin.
# =============================================================================

edition = "2024"
style_edition = "2024"
max_width = 100
tab_spaces = 4
use_small_heuristics = "Default"
newline_style = "Unix"
